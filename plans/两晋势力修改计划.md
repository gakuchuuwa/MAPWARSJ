# 两晋势力最终修改计划

> 根据用户决策，对游戏数据进行修改。涉及5个文件：factions.ts、cities_v2.ts、GameApp.ts、CityAssetManager.ts、EventParser.ts

---

## 一、不修改的内容（确认用户决策）

| 项目 | 决策 |
|------|------|
| 梁(liang_d) | 保持现状，首都东平(city_dongping)，不变 |
| 刘表(liubiao) | 保持襄阳(city_xiangyang)，不变 |
| 凉(liang) | 保持武威(city_wuwei)，前凉保留，后凉舍弃 |
| 西凉/李暠(li_lx_d) | 取消添加（已有李） |
| 前赵 | 取消添加 |
| 南燕 | 取消添加（已有燕） |
| 后凉 | 取消添加（武威已归凉） |
| 刘宋/萧齐 | 不加 |

---

## 二、修改现有数据（3处）

### 2.1 陈(chen) — 首都改为韶关

**文件: `src/data/cities_v2.ts:710-717`**
- 将 `city_shaoguan` 的 `factionId` 从 `'panjun'` 改为 `'chen'`
- 更新 note 添加陈霸先始兴郡根据地描述

```diff
- factionId: 'panjun',
- note: '古称韶州(今广东韶关), 控扼南北交通, 岭南重镇'
+ factionId: 'chen',
+ note: '古称韶州(今广东韶关), 陈霸先始兴郡根据地, 控扼南北交通, 岭南重镇'
```

**文件: `src/core/GameApp.ts:86`**
```diff
- 'chen': 'city_huaiyang',
+ 'chen': 'city_shaoguan',
```

### 2.2 朝阳(city_chaoyang) — 改为慕容势力

**文件: `src/data/cities_v2.ts:1047-1055`**
- 将 `city_chaoyang` 的 `factionId` 从 `'xianbei'` 改为 `'murong'`
- note 保持不变（已提及鲜卑慕容氏）

```diff
- factionId: 'xianbei',
+ factionId: 'murong',
```

### 2.3 陈霸先事件映射

**文件: `src/core/EventParser.ts`**
- 添加陈/陈霸先相关的 factionMap 映射

---

## 三、新增势力（2个）

### 3.1 慕容(murong) — 旗号"慕容"

| 属性 | 值 |
|------|-----|
| factionId | `'murong'` |
| 旗号 | `'慕容'`（2字 ✓，无重复 ✓） |
| 颜色 | `'#6A0DAD'`（深紫罗兰 — 鲜卑慕容龙城之气） |
| 首都 | 朝阳(city_chaoyang, 41.57, 120.45) |
| 历史背景 | 鲜卑慕容氏，建立前燕/后燕/南燕/西燕，龙城(朝阳)为前燕国都 |

**涉及修改：**

**`src/data/factions.ts`** — 新增一行（在两晋相关势力区域）
```typescript
{ id: 'murong', name: '慕容', color: '#6A0DAD' },
```

**`src/data/cities_v2.ts:1047-1055`** — 已在2.2中修改，朝阳改为慕容

**`src/core/GameApp.ts:STARTING_CAPITALS`** — 新增
```typescript
'murong': 'city_chaoyang',
```

**`src/core/CityAssetManager.ts`**
- factionFlagMap: `'murong': 'RANDOM',`
- sandboxDisplayNames: `'murong': '慕容',`

**`src/core/EventParser.ts`** — 新增映射
```typescript
'慕容': 'murong', '慕容皝': 'murong', '慕容垂': 'murong', '慕容恪': 'murong',
'前燕': 'murong', '后燕': 'murong', '龙城': 'murong',
```

### 3.2 尔朱(erzhu) — 旗号"尔朱"

| 属性 | 值 |
|------|-----|
| factionId | `'erzhu'` |
| 旗号 | `'尔朱'`（2字 ✓，无重复 ✓） |
| 颜色 | `'#8B2500'`（铁锈红 — 契胡部落彪悍之风） |
| 首都 | 秀容川(新建城市, 38.42, 112.73) |
| 历史背景 | 契胡部落，尔朱荣据秀容川起兵，掌控北魏朝政 |

**涉及修改：**

**`src/data/factions.ts`** — 新增一行
```typescript
{ id: 'erzhu', name: '尔朱', color: '#8B2500' },
```

**`src/data/cities_v2.ts`** — 新增城市（在末尾新增城市区域）

**`src/core/GameApp.ts:STARTING_CAPITALS`** — 新增
```typescript
'erzhu': 'city_xiurongchuan',
```

**`src/core/CityAssetManager.ts`**
- factionFlagMap: `'erzhu': 'RANDOM',`
- sandboxDisplayNames: `'erzhu': '尔朱',`

**`src/core/EventParser.ts`** — 新增映射
```typescript
'尔朱': 'erzhu', '尔朱荣': 'erzhu', '尔朱兆': 'erzhu', '契胡': 'erzhu',
'秀容川': 'erzhu', '秀容': 'erzhu',
```

---

## 四、新增城市（2个）

### 4.1 秀容川 — 尔朱氏首都

```typescript
{
    id: 'city_xiurongchuan',
    name: '秀容川',
    factionId: 'erzhu',
    lat: 38.42, lng: 112.73,
    type: 'medium_city',
    troops: 10000,
    tier: 1,
    note: '契胡尔朱氏根据地(今山西忻州), 尔朱荣据秀容川起兵, 雄踞北魏北疆'
},
```

**距离验证：**
| 对比城市 | 坐标 | 距离 | 判定 |
|---------|------|------|------|
| 太原(city_taiyuan) | 37.87, 112.55 | ≈63km | ✅ >50km，不重叠 |
| 平阳(city_linfen) | 36.08, 111.52 | ≈270km | ✅ 极远 |
| 雁门关(city_yanmenguan) | ~39.2, 112.9 | ≈87km | ✅ >50km |

### 4.2 怀朔镇 — 高氏第二城

```typescript
{
    id: 'city_huaishuo',
    name: '怀朔镇',
    factionId: 'gao_d',
    lat: 40.95, lng: 109.95,
    type: 'medium_city',
    troops: 10000,
    tier: 1,
    note: '北魏六镇之一(今内蒙古固阳西南), 高欢(高氏)曾为怀朔镇兵户, 为北齐政权根基'
},
```

**距离验证：**
| 对比城市 | 坐标 | 距离 | 判定 |
|---------|------|------|------|
| 固阳塞(city_guyangsai) | 41.10, 110.08 | ≈19km | ⚠️ <50km，但固阳塞为关塞(pass, tier 2, 叛军)，怀朔镇为城镇(medium_city, tier 1, 高氏)，类型不同，且怀朔镇知名度更高，可共存 |
| 盛乐(city_shengle) | 40.38, 111.82 | ≈168km | ✅ >50km |
| 大同(city_datong) | 40.08, 113.30 | ≈290km | ✅ 极远 |

> **说明：** 关塞(pass)与城镇(medium_city)在游戏机制中功能不同（关塞为军事防御点，城镇为可发展城市），已有先例可共存。

---

## 五、5个文件变更汇总

### 文件1: `src/data/factions.ts`
**变更：** +2行（新增慕容、尔朱势力定义）
```typescript
{ id: 'murong', name: '慕容', color: '#6A0DAD' },
{ id: 'erzhu', name: '尔朱', color: '#8B2500' },
```

### 文件2: `src/data/cities_v2.ts`
**变更：** 2处修改 + 2处新增
1. `city_shaoguan` (行710): factionId `panjun→chen`，更新note
2. `city_chaoyang` (行1047): factionId `xianbei→murong`
3. **新增** `city_xiurongchuan` (秀容川)
4. **新增** `city_huaishuo` (怀朔镇)

### 文件3: `src/core/GameApp.ts`
**变更：** 1处修改 + 2处新增
1. 行86: `'chen': 'city_huaiyang'` → `'chen': 'city_shaoguan'`
2. 新增: `'murong': 'city_chaoyang'`
3. 新增: `'erzhu': 'city_xiurongchuan'`

### 文件4: `src/core/CityAssetManager.ts`
**变更：** factionFlagMap +2行, sandboxDisplayNames +2行
1. factionFlagMap: `'murong': 'RANDOM',` + `'erzhu': 'RANDOM',`
2. sandboxDisplayNames: `'murong': '慕容',` + `'erzhu': '尔朱',`

### 文件5: `src/core/EventParser.ts`
**变更：** factionMap 新增多组映射
- `'慕容'` 系列 → `'murong'`
- `'尔朱'` 系列 → `'erzhu'`
- `'陈'`, `'陈霸先'`, `'韶关'`, `'始兴'` → `'chen'`

---

## 六、实施顺序

1. **`src/data/factions.ts`** — 添加慕容、尔朱势力定义
2. **`src/data/cities_v2.ts`** — 修改韶关/朝阳，新增秀容川/怀朔镇
3. **`src/core/GameApp.ts`** — 更新首都映射
4. **`src/core/CityAssetManager.ts`** — 添加旗号和显示名
5. **`src/core/EventParser.ts`** — 添加事件解析映射
6. **验证** — 确认游戏加载无报错，新势力旗号正常显示
