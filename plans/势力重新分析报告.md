# 十六国/南北朝势力重新分析报告

> 基于用户修正意见重新分析，2026-05-25

## 数据源

- [`factions.ts`](../src/data/factions.ts) — 现有势力定义（~140个）
- [`cities_v2.ts`](../src/data/cities_v2.ts) — 现有城市数据（~200+个）
- 距离验证：Haversine 公式，要求 **>50km**

---

## 一、基础城市数据现状

### 已存在的关键城市（凉州/中原）

| 城市 | 坐标 | 当前归属 | 说明 |
|------|------|----------|------|
| 安阳（邺城） | (36.10, 114.39) | `shang`（商） | note标注"三国魏邺城/北朝邺城" |
| 姑臧 | (37.93, 102.64) | `liang`（凉） | 五凉古国都城 |
| 张掖 | (38.93, 100.45) | `panjun` | 河西四郡 |
| 酒泉 | (39.71, 98.51) | `panjun` | 河西四郡 |
| 敦煌 | (40.14, 94.66) | `panjun` | 河西四郡 |
| 郢城 | (30.35, 112.18) | `chu`（楚） | note标注"古称...江陵" |
| 襄阳 | (32.01, 112.12) | `panjun` | |

### 不存在的城市（需新建）

以下城市在 [`cities_v2.ts`](../src/data/cities_v2.ts) 中 **没有独立条目**，仅出现在 note 中：

- **邺城**（作为独立城市不存在，复用安阳）
- **江陵/武昌/广陵/建康/白帝**（只有名称注记，没有独立城市实体）
- **乐都/西平/湟中**（南凉首都，完全不存在）

---

## 二、用户意见逐条验证

### 1. 桓氏（谯国桓氏）→ ❌ 跳过

用户已确认：郢城已归楚，不再添加。**已跳过。**

### 2. 冉魏（冉闵·邺城）→ ⚠️ 需要讨论

**距离验证：**
```
安阳(邺城) → 邯郸(赵):  57.4 km → ✓ >50km
安阳(邺城) → 牧野(panjun): 99.2 km → ✓ >50km
安阳(邺城) → 开封(魏): 144.8 km → ✓ >50km
安阳(邺城) → 新郑(韩): 199.6 km → ✓ >50km
安阳(邺城) → 真定(中山): 228.5 km → ✓ >50km
```

**距离完全满足。** 但问题在于：

> 安阳(36.10, 114.39) 目前归属 **商(shang)**。

如果要添加冉魏，有两个方案：
- **方案A**：将安阳改属冉魏 → 但商失去核心城市
- **方案B**：在安阳附近新建邺城 → 但历史邺城与安阳仅相距 ~20km，违反 >50km 规则

**建议：放弃冉魏。** 除非用户愿意让 冉魏 使用其他城市（如信都/襄国等后赵旧地）。

### 3. 南齐 → ✅ 已取消

用户已确认：与战国齐国共用「齐」旗号，取消南齐。**已处理。**

### 4. 五凉 → 需具体分析 ⬇️

### 5. 燕国 → ✅ 维持现状

用户已确认：燕国都是慕容，最多一个燕一个慕容。**不做添加。**

### 6. 成汉 → ✅ 维持现状

用户已确认：有成、汉、巴、李，不再添加。**已处理。**

---

## 三、五凉详细分析（核心内容）

### 3.1 目标分配方案

| 历史政权 | 旗号 | factionId | 状态 | 建议都城 |
|---------|------|-----------|------|---------|
| 前凉 | 张 | `zhang3_d`（新建） | **新建势力** | 姑臧？ |
| 后凉 | 吕 | `lyu_d`（复用） | 已有，可改城池 | 姑臧？ |
| 南凉 | 秃发 | `tufa_d`（新建） | **新建势力** | 需新建乐都 |
| 北凉 | 沮渠 | `juqu_d`（新建） | **新建势力** | 张掖 |
| 西凉 | 李 | `li_lx_d`（复用） | 已有，可改城池 | 敦煌/酒泉 |

### 3.2 现有相关势力对照

```typescript
// 已存在的相关势力
{ id: 'liang', name: '凉', color: '#2E8B57' },     // 现有凉（姑臧）
{ id: 'li_lx_d', name: '李', color: '#E65100' },    // 陇西李氏 → 可作西凉
{ id: 'lyu_d', name: '吕', color: '#C62828' },      // 定陶吕氏 → 可作后凉
{ id: 'zhang2_d', name: '章', color: '#BF360C' },   // 河间章氏（⚠️ 不是张！）
```

**注意**：[`zhang2_d`](../src/data/factions.ts) 是「**章**」（河间章氏），不是「**张**」（前凉张氏）。前凉需要新建 `zhang3_d`（或类似 ID）。

### 3.3 城市距离验证

```
姑臧(37.93,102.64)[凉] → 张掖(38.93,100.45)[panjun]: 220.8 km ✓
张掖(38.93,100.45)     → 酒泉(39.71,98.51)[panjun]:   188.1 km ✓
酒泉(39.71,98.51)      → 敦煌(40.14,94.66)[panjun]:   331.7 km ✓
姑臧(37.93,102.64)     → 兰州(36.06,103.83)[panjun]:  233.2 km ✓
```

所有凉州城市间距均远 >50km。✅

### 3.4 五凉具体方案

#### 方案A：最少改动（推荐）

只改动城池归属，不新建势力：

| 目标 | 操作 | 说明 |
|------|------|------|
| 后凉（吕） | 将姑臧改属 `lyu_d` | 吕光据姑臧建后凉 |
| 西凉（李） | 将酒泉/敦煌改属 `li_lx_d` | 李暠据敦煌/酒泉建西凉 |
| 北凉（沮渠） | 新建 `juqu_d`，张掖改属 | 沮渠蒙逊据张掖/姑臧 |
| 前凉（张） | 不动 | 现有 `liang` 对应前凉？ |
| 南凉（秃发） | 放弃（无合适城池+新建太多） | |

**优点**：不新增 faction，只改城池归属，改动量小。
**缺点**：用户要求用姓氏区分，但前凉/南凉无合适城池。

#### 方案B：中等改动

| 步骤 | 操作 | 说明 |
|------|------|------|
| 1 | 新建 `juqu_d`（沮渠），旗号「沮渠」 | 北凉 |
| 2 | 新建 `tufa_d`（秃发），旗号「秃发」 | 南凉 → 新建乐都(36.6, 101.8)？需查距离 |
| 3 | 新建 `zhang3_d`（张），旗号「张」 | 前凉 |
| 4 | 姑臧改属 `zhang3_d`（前凉）或 `lyu_d`（后凉） | 择一 |
| 5 | 张掖改属 `juqu_d`（北凉） | |
| 6 | 酒泉/敦煌改属 `li_lx_d`（西凉） | |
| 7 | 现有 `liang` 保留或移除 | |

**南凉（秃发）所需新城距离验证：**
乐都大致位置 (36.5, 102.4)
```
乐都(36.5, 102.4) → 姑臧(37.93, 102.64): 159 km ✓
乐都(36.5, 102.4) → 兰州(36.06, 103.83):  130 km ✓（注意兰州是 panjun）
```

但是用户说「有成，有汉，有巴，有李，就不要再添加了」——意思是不要加太多。新建3个势力可能过多。

---

## 四、完整建议汇总

### ✅ 可以添加的

| 势力 | 旗号 | factionId | 城池 | 操作 |
|------|------|-----------|------|------|
| 沮渠（北凉） | 沮渠 | `juqu_d` | 张掖 → 改属 | 新建势力 + 改城池归属 |
| 李（西凉） | 李 | `li_lx_d`（复用） | 敦煌/酒泉 → 改属 | 只改城池归属 |

### ⚠️ 可选的

| 势力 | 旗号 | 问题 |
|------|------|------|
| 吕（后凉） | 吕 | 姑臧已在凉手下，要改属需先决定凉的去留 |
| 张（前凉） | 张 | 需要新旗号「张」(zhang2_d是章)，且姑臧已占 |
| 冉魏 | 冉 | 距离够，但安阳已归商，无合适新城 |
| 秃发（南凉） | 秃发 | 需要新建乐都城 |

### ❌ 已排除的

| 势力 | 原因 |
|------|------|
| 桓氏 | 郢城已给楚 |
| 南齐 | 和战国齐国共用「齐」 |
| 燕国全部 | 都是慕容，已有燕/慕容 |
| 成汉 | 有成/汉/巴/李 |

---

## 五、最终推荐（最小改动方案）

只做 **两项改动**，符合用户"不要再添加了"的指示：

1. **新建 `juqu_d`（沮渠）** → 旗号「沮渠」，据点 **张掖**，代表北凉
2. **将敦煌/酒泉改属 `li_lx_d`（李）** → 代表西凉（陇西李氏 → 西凉李氏，同姓可复用）

> **现有凉(liang) 保留不变**，可视为前凉/后凉的复合代表。

**无需新建势力**：吕(lyu_d)已有，李(li_lx_d)已有，凉(liang)已有。
**只需新建一个势力**：沮渠(juqu_d)。

---

## 六、实施步骤（待用户确认）

1. 新建 [`factions.ts`](../src/data/factions.ts) 条目 `{ id: 'juqu_d', name: '沮渠', color: '#' }`
2. 修改 [`cities_v2.ts`](../src/data/cities_v2.ts) 中 `city_zhangye` 的 `factionId` → `juqu_d`
3. 修改 `city_jiuquan` 和/或 `city_dunhuang` 的 `factionId` → `li_lx_d`
4. 在 [`CityAssetManager.ts`](../src/core/CityAssetManager.ts) 的 [`factionFlagMap`](../src/core/CityAssetManager.ts:11) 中添加 `juqu_d` 的旗帜映射
5. 编译验证
