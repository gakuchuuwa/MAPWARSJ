/**
 * 战场表 —— 🔴 [2026-09-12 主人定] 战场**独立于据点体系**，不是 `city_*`。
 *
 * > 主人原话：「要不然把战场独立出来，不做为据点，**就叫战场**。一个地名而已。
 * >           **类似奇观**，万一缺少武将了，可以安置一个武将，但是**不能出征**，纯属剧情需要。」
 *
 * ── 为什么独立（血训） ────────────────────────────────────────────────
 *   原先战场是 `cities_v2.ts` 里带 `battlefield: true` 的**真据点行**，后果：
 *   · 它**在 `cityManager.getCities()` 里**，于是全项目 30+ 处消费者都看得见它
 *     （AI 目标池 / 任务系统 / 行军寻路 / 复国系统 / 存档 / 军团寻路…），
 *     只能靠 `GameAppCityLoader`、`TerritorySystem`、`RecruitmentSystem`、
 *     三个审计脚本到处打补丁绕开 —— 全是「因为它混在据点里」才需要的补丁。
 *   · 还得把 `City.troops` 改成可选（为「没有兵力」），连带 6 处 `?? 0`。
 *   · 更要命的是它带着一个 `factionId`（照抄守方），AI 会把它当敌方据点；
 *     剧本 `cityUpdates` 还能给它「易主」（主人：「**战场没有主人，易什么主？**」）。
 *   → 独立后这些补丁与隐患**整类消失**：战场不进 `getCities()`，没有势力、没有兵力、
 *     不能攻占、不参与 50km 间距与四公理审计。
 *
 * ── 显示规矩（保留主人 2026-09-12 定案，只是换了载体） ─────────────────
 *   「**打完了才叫战场，没打的不叫战场**」：
 *   · **开战前**：只显示**地名**（不显示战场形态）
 *   · **打完之后**：显示战场形态（拒马／尸体／骨骸／火把…）
 *   见 `src/map/BattlefieldLayer.ts` 与 `src/events/battlefieldState.ts`。
 *
 * 🔴 [2026-09-12 主人令「**怎么战场还显示武将名字呢，删除，别乱加**」]
 *   战场标牌**只许显示地名**，不再有任何「剧情武将」名字行 —— 原先的 `storyGeneralId`
 *   字段与渲染已整段删除。剧本里的武将该出场就在剧本里出场：
 *   例如 -333 伊苏斯之战的守方主帅 = `defenderGeneralId: 'daliushi_iii'`（走野战军团，不是战场标牌）。
 */

import type { CityType } from '../types/core';
import { CITIES_V2 } from './cities_v2';

export interface BattlefieldData {
    /** 🔴 战场 id（**不是** `city_*`，不进据点表）：`bf_` + 拼音 */
    id: string;
    /** 战场地名（一个地名而已） */
    name: string;
    /** 真实坐标。🔴 **必须与该场剧本 `fieldBattleData.location` 一字不差** */
    lat: number;
    lng: number;
    /** 哪一年的剧本打完它才上图（对应 `HistoricalEventScript` 的 `year`） */
    scriptYear: number;
    note?: string;
    /**
     * 🔴 [2026-09-16 主人定] 赶路背景播报 —— 玩家**在奔赴这个战场的路上**逐段播的背景介绍。
     * 与「事件播报」「战役播报」都不是一回事：那两条是战役本身的说明，这条是路上的铺垫。
     * 空行分段，每段一条 toast 依次推送（见 `PlayerQuestSystem.startJourneyBriefing`）。
     */
    briefing?: string;
    /**
     * 🔴 [2026-09-19 主人定] **在场人物** —— 「缺少的人物，做成战场人物。人物和战场点绑定。」
     *
     * 存的是 `BattlefieldCharacters.ts` 里那些**只在场战上出现**的人物 id
     * （窦建德 / 王世充 / 保卢斯 / 瓦罗 / 埃提乌斯…）。他们不属于任何据点，只属于这一块战场；
     * 玩家在选边对话框里看到的那两位主帅，就是这里的人。
     *
     * ⚠️ 与 `HistoricalEventScript` 的 `generalId`（**这一仗是谁的**）不是一回事：
     *    那个回答「玩家找谁接这一仗」，这个回答「这一仗现场有谁」。
     */
    roster?: string[];
    /**
     * 🔴 [2026-09-19 主人定] **这个战场打的是哪座城**（只有攻城战要用）。
     *
     * 主人原话：「战场和现有据点不是一回事，所有战场都新建。」
     *   → 战场标牌标在**史实地点**（一之谷 34.64,135.10，今神户须磨区），
     *     而攻城战打的是那座**据点**（最近的姬路城 34.8394,134.6939，相距 43km）。
     *   两者本来就不是一个坐标，所以**不能拿坐标去配对**：第一批实测一之谷的经纬差是
     *   0.1994 / 0.4061，任何"合理的"度容差都盖不住这 43km，放宽到 0.5 度又会串到别的城。
     *
     * 故攻城战改成**显式指名**：本字段写被攻据点 id。运行时据此
     *   ① `findHistoricalEventOfGeneral` 把战场配回战役（不再比坐标）；
     *   ② 玩家的军团赶路终点（见 `PlayerQuestSystem.joinGeneralEvent`）。
     * 野战不用本字段（野战的契约仍是「战场坐标 === 条目 location」）。
     */
    eventCityId?: string;
    /**
     * 🔴 [2026-09-19 主人定「建立一个一之谷战场」] **这块战场本身就是攻城目标**（战场要塞）。
     *
     * 一之谷这种要塞：它**不是据点**（不进 `cities_v2`、不算任何势力的城），
     * 但史实上它就是被强攻拿下的。故攻城战写成「打这块战场」——
     * `SiegeData.targetBattlefieldId` 填本战场的 id，引擎据此用战场记录合成攻城目标。
     *
     * 与 `eventCityId` **二选一**：
     *   · 打**据点**（推罗战役）→ `eventCityId`（值 = `city_*`）
     *   · 打**战场要塞**（一之谷战役）→ 本字段（值 = `bf_*`）
     */
    eventBattlefieldId?: string;
    /**
     * 🔴 [2026-09-19 主人定] **战场攻城战的标注套用据点样式**。
     *
     * 主人原话：「这种战场攻城战，标注上，套用大中小城寨哪个据点的样式就行。」
     *   → 战场平时画「战场形态」（拒马/尸体/骨骸/火把…）；但**攻城战**是打一座**砦/城**，
     *     它该长成据点那样 —— 本字段写用哪一档：
     *     `big_city` 大城 ／ `medium_city` 中城 ／ `small_city` 小城 ／ `stockade` 城寨 ／ `pass` 险要。
     *
     * 留空 = 仍画普通战场形态（**野战一律留空**）。
     * 样式本身由 `TerritorySystem.buildSiegeCastleStackHtml()` 用**据点同一套组装**画出来
     * （风格判据、石/木墙、中心城堡全部与据点一致，没有第二套画法）。
     */
    siegeCastleType?: CityType;
}

export const BATTLEFIELDS: BattlefieldData[] = [
    // ── 前 334 年 格拉尼库斯河战役（马其顿 vs 波斯） ──
    //    剧本：`HistoricalEventScript` `-334`（`type: 'field_battle'`）。
    {
        id: 'bf_gelanikusihe',
        name: '格拉尼库斯河',
        lat: 40.3167,
        lng: 27.2811,
        scriptYear: -334,
        note: '格拉尼库斯河战役战场，前334年5月，亚历山大亲率伙伴骑兵强渡急流击溃波斯联军；坐标与剧本 -334 的 location 一字不差；战场无势力、无兵力、不可攻占',
        // 🔴 [2026-09-23 主人改] 播报文案用概念词（数万/数千），不落精确兵数——2000 年战争史精确兵数常查无实据、硬编易错。
        //    年龄、器械名、地理、谋略保留具体（教材价值）。兵额见 HistoricalEventScript 的 attackerTroops（数据层结算用）。
        briefing: '公元前334年暮春，二十二岁的马其顿国王亚历山大统率大军渡过赫勒斯滂海峡，正式挥师东征。面对入侵，波斯小亚细亚诸总督拒绝了名将门农“坚壁清野”的避战计，尽起麾下骑兵与希腊雇佣军，抢先扼守在格拉尼库斯河东岸的高地上，意图借湍急水流与陡峭河滩将远征军迎头痛击。两军隔水相望，暮色沉沉。老将帕曼纽力主扎营待旦，亚历山大却拒绝延误战机。波斯甲骑沿坡严密布防，马其顿将士临水肃立，杀机已随冰冷的急流漫过河滩。',
    },

    // ── 前 333 年 伊苏斯战役（马其顿 vs 波斯） ──
    {
        id: 'bf_yisusi',
        name: '伊苏斯',
        lat: 36.7525,
        lng: 36.1923,
        scriptYear: -333,
        // 🔴 [2026-09-12 主人令「怎么战场还显示武将名字呢，删除，别乱加」]
        //    原先这里挂 `storyGeneralId: 'daliushi_iii'`（标牌上多印一行大流士三世）——已删除。
        //    大流士三世照旧在剧本里当主帅：`HistoricalEventScript` -333 的 `defenderGeneralId`。
        note: '伊苏斯战役战场，前333年，皮纳鲁斯河畔，亚历山大击溃大流士三世；坐标取英文维基百科 Battle of Issus 信息框 36.7525,36.1923，与剧本 -333 的 location 一字不差；战场无势力、无兵力、不可攻占',
        briefing: '公元前333年深秋，亚历山大率数万大军南下，大流士三世亲率大军绕袭其后，切断补给。两军在背山面海、宽仅两英里的狭长平原相遇，波斯兵力难以铺展。大流士背靠北岸列阵，亚历山大以萨里沙方阵居中、伙伴骑兵坐镇右翼。马其顿右翼压向浅滩，杀机一触即发。',
    },

    // ── 前 331 年 高加米拉战役（马其顿 vs 阿契美尼德波斯） ──
    //    剧本：`HistoricalEventScript` `-331`（`type: 'field_battle'` 野战）。
    {
        id: 'bf_gaojiamila',
        name: '高加米拉',
        lat: 36.56,
        lng: 43.444,
        scriptYear: -331,
        note: '高加米拉战役战场，前331年秋10月，摩苏尔以东平原。亚历山大斜线战术与楔形骑兵突击击溃大流士三世，阿契美尼德波斯帝国瓦解。史料：阿里安《亚历山大远征记·第三卷》。坐标沿用本表既有条目 36.3628,43.25。主人 2026-09-19 定：不碰已有数据。',
        briefing: '公元前331年深秋，底格里斯河以东的高加米拉平原干燥苍茫。为了一雪伊苏斯之耻，波斯帝国万王之王大流士三世召集了横跨帝国行省的大军，两百辆装有锋利铁镰的轮式战车和印度战象在阵前列队，甚至将整片战场预先铲平，只为彻底将马其顿入侵者踏为齑粉。四万余名马其顿老兵在亚历山大的统帅下渡过河流，驻扎在距离波斯大营数英里外的山丘之上。黑夜笼罩大地，波斯军营中数十万盏火把如繁星延绵，年轻的亚历山大拒绝部将夜袭的提议，令全军饱食安眠。拂晓将至，马其顿伙友骑兵与萨里沙长矛方阵已步步走下高坡，迎着地平线上无边无际的波斯军阵缓缓展开阵型。',
    },

    {
        id: 'bf_wukexiya',
        name: '乌克西亚隘口',
        lat: 32.0457,
        lng: 48.8506,
        scriptYear: -331,
        note: '乌克西亚隘口战役战场，前331年冬。⚠️ 英文维基信息框所给坐标即苏萨城本身，与其正文「苏萨以东」「苏萨与波斯波利斯之间的山脉」自相矛盾，故按正文推定战场为苏萨以东约六十公里、去波斯波利斯大道上进入扎格罗斯山的第一处山口，坐标取该大道实际走线上的山口点。乌克西亚人是扎格罗斯山中的半游牧部落联盟，向来往军队索取买路钱；亚历山大应约纳贡而暗中分兵，克拉特鲁斯据高地断其退路，自率精锐袭其村落并强夺隘口，部落战士退往高地遭四面合围。史料：英文维基百科 Battle of the Uxian Defile。',
        briefing: '公元前331年深秋，高加米拉平原上的血渍尚未干透，马其顿大军已转向东南。亚历山大没有直取波斯腹地，因为巴比伦的城墙高大厚重，墙垣之内尽是农田，足以长期自守，他唯恐波斯人在那里重新整顿军备。\n\n可当他抵达城下时，城门是敞开的。波斯总督马扎欧斯献城归顺，亚历山大依居鲁士以来的旧例，仍让他留任原职。大军在巴比伦休整数日，用城中府库的财宝犒赏将士，又把这里设为东征的第二基地。随后挥师东进，走了二十天才抵达波斯帝国的冬都苏萨，从那里取得大笔金银，并拨出一批送回马其顿，供安提帕特与斯巴达作战之用。\n\n苏萨以东就是扎格罗斯山的门径。山里住着乌克西亚人：一个半游牧的部落联盟，靠放牧与劫掠过活，连历代波斯大王过境，也得向他们交纳买路钱。他们派使者来见亚历山大，要他也照波斯人的旧例纳贡。亚历山大应下了，说定某日走大道、如约付钱。\n\n到了那一天，马其顿人却分兵而行：克拉特鲁斯带着盾卫抢先占据高地，堵住乌克西亚人设想的退路；另有数千将士走北路，直扑他们的村落。隘口之侧，杀机已起。',
    },
    {
        id: 'bf_bosimen',
        name: '波斯门',
        lat: 30.7083,
        lng: 51.5986,
        scriptYear: -330,
        note: '波斯门战役战场，前330年冬，扎格罗斯山脉波斯门隘口。隘口最窄处只容数人并行，波斯总督阿里奥巴赞斯据石垒扼守，北坡滚石、南坡弓矢，马其顿军初攻受挫、弃尸而退；亚历山大后由俘虏或当地牧羊人引路，夜间翻山绕至守军背后，托勒密与佩尔狄卡斯分路合围而破之。史料：英文维基百科 Battle of the Persian Gate，坐标取该条目信息框 30°42′30″N 51°35′55″E。',
        briefing: '乌克西亚隘口既破，通往波斯腹地的山门已开。亚历山大自苏萨继续东进，这一次他兵分两路：帕曼纽率一半人马沿御道直趋波斯波利斯，他自己取道波斯本土方向的山路。沿途他先平定了乌克西亚人，再向波斯门进发。波斯门是一条狭窄的山口，两侧是扎格罗斯山的绝壁，路到这里被挤成只能容数人并行的一道缝隙——守在这里的，是波斯总督阿里奥巴赞斯。\n\n亚历山大的军队一路没有遇到任何抵抗。他以为前路再无敌军，便没有派斥候前出探路，全军人马沿着宽阔的谷地大摇大摆地走进了隘口。\n\n等到马其顿人深入窄道，北面山坡上滚下巨石，南面山坡上箭如雨下。全军被压在只能容数人并行的窄道里，前军进退不得，后军还在源源涌入，整个队列堵死在山中。马其顿人成队成队地被砸死射死，亚历山大被迫丢下阵亡者的尸体才把余部撤出——对马其顿人来说这是莫大的耻辱，他们素来看重收殓与安葬阵亡者。\n\n阿里奥巴赞斯据垒死守了整整一个月。最后是俘虏，或是一名当地的牧羊人，把一条绕到波斯军背后的山路指给了亚历山大。亚历山大亲率精兵趁夜翻山，摸到隘口守军的头顶，同时令克拉特鲁斯率一部在营中虚张声势，托勒密与佩尔狄卡斯分路合围。山道两头一夹，波斯军再也挡不住这道山口。',
    },
    {
        id: 'bf_xierhe',
        name: '锡尔河',
        lat: 40.2833,
        lng: 69.6167,
        scriptYear: -329,
        note: '前329年锡尔河战役，亚历山大渡河大破斯基泰游牧骑兵。B 档并入背景：依 §零之二「主角不在场的偏师之战不单列，写进相邻那一场的播报」，前329年冬帕尔努克斯率偏师往救马拉坎达、全军覆没一事写在本文的播报里，不另立事件。史料：英文维基百科 Spitamenes 与 Battle of Jaxartes。',
        briefing: '公元前330年波斯波利斯陷落后，亚历山大大军继续向东疾进，追击波斯末王大流士三世。大流士终为巴克特里亚总督贝苏斯所害，贝苏斯自立为王。\n\n大军翻越崇山峻岭进入中亚，平定阿里亚与巴克特里亚，跨过阿姆河直取粟特首府撒马尔罕。粟特贵族斯皮塔米尼斯举兵反叛，围攻驻军，更引北方斯基泰大军隔锡尔河对峙。\n\n亚历山大当时正在药杀水畔择地筑城，一时抽不开身，便派帕尔努克斯率一支偏师前往解马拉坎达之围。这支人马被斯皮塔米尼斯的骑兵诱入旷野，全军覆没，步卒骑兵无一生还——这是马其顿东征以来最惨的一次败绩，也让中亚诸城看清了这场叛乱的分量。\n\n消息传到药杀水畔，亚历山大抱病疾驰忽毡，决意先以水陆合击强渡锡尔河，迎击这支纵横欧亚大草原的剽悍骑兵。',
    },
    // ── 前 326 年 海达斯佩斯河战役（马其顿 vs 保拉瓦人波鲁斯） ──
    //    剧本：`HistoricalEventScript` `-326` 夏（`type: 'field_battle'` 野战）。
    {
        id: 'bf_haidasipaisihe',
        name: '海达斯佩斯河',
        lat: 32.8278,
        lng: 73.6389,
        scriptYear: -326,
        note: '海达斯佩斯河战役战场，前326年5月，今杰赫勒姆河畔、巴基斯坦旁遮普省。波鲁斯据南岸列阵拒渡，亚历山大连日沿河佯动后于雷雨之夜自上游多林河岛偷渡，先以骑射手扰其右翼、伙伴骑兵破其左翼、科伊诺斯抄其后路击溃印度骑兵，再以方阵萨里沙长矛顶住战象；波鲁斯力战被俘，仍得保有其国。坐标取英文维基百科 Battle of the Hydaspes 信息框 32°49′40″N 73°38′20″E。',
        briefing: '奥诺斯岩既下，通往印度河的道路再无阻碍。前326年初，亚历山大进抵印度河，架起浮桥渡河。河对岸的尼萨城派使者来迎，说此城为狄奥尼索斯所建，求他不要攻城；亚历山大答应了，只让他们派些骑兵随军。\n\n大军接着东进塔克西拉。那里的王与邻邦波鲁斯有仇，主动献城结盟，还把自己的军队交给亚历山大合用。波鲁斯却不肯降——他带着战象与大军，守在杰赫勒姆河对岸，等着马其顿人来攻。\n\n杰赫勒姆河又深又急，正面强渡无异送死。亚历山大把营扎在北岸，日夜派骑兵沿河上下走动，让波鲁斯跟着来回奔波；等到对方松懈下来，他摸到上游一处多林的河岛，连夜偷渡。那晚恰好雷雨交加，雷声把渡河的动静盖了个干净。\n\n天一亮，马其顿军已经在河的南岸列成阵势，正对着波鲁斯的营盘。',
    },
    // ── 前 324 年 科塞亚战役（马其顿 vs 扎格罗斯山科塞亚人） ──
    //    剧本：`HistoricalEventScript` `-324` 冬（`type: 'field_battle'` 野战）。
    {
        id: 'bf_kesaiya',
        name: '科塞亚',
        lat: 33.49,
        lng: 48.36,
        scriptYear: -324,
        note: '科塞亚战役战场，前324年冬。科塞亚人据英文维基百科 Cossaei 为扎格罗斯山地部落、善射穴居，靠劫掠与向来往军队索取买路钱为生，波斯历代诸王都未能征服；亚历山大以轻装部队征讨，先夺入山要道、屡战皆胜，科塞亚人被迫以臣服换回被俘者，这是其 324/323 年冬季的最后一役。条目未给坐标，战场取该山地中今伊朗卢里斯坦霍拉马巴德一带 33.49,48.36。',
        briefing: '马利都城破了以后，亚历山大沿印度河南下，一路收服沿岸诸城，直到河口的帕塔拉；其间在一座婆罗门人的城下中了一支毒箭，几乎送命。他随后把舰队交给尼阿库斯，让他沿海岸西航，自己带着大军走格德罗西亚的沙漠——那是一片没有水、没有粮的荒原，大军在沙里走了两个月，死伤之重，比任何一仗都惨。\n\n前324年，他回到波斯腹地：在苏萨娶了波斯贵族的女儿，把将士们的旧债一并免除，又追查了一批不法的总督。到欧皮斯，他宣布让老兵们回乡，士兵哗变，闹到最后他才把讲台上带头的人抓起来，重新把军心拢住。\n\n秋天，他最亲近的伙伴赫费斯提翁在米底的埃克巴坦那病死，亚历山大为此大恸，整军停战致哀。等到哀礼办完，他动身回巴比伦——路上要穿过扎格罗斯山里的科塞亚境。',
    },
];

/** 按 id 取战场 */
export function getBattlefield(id: string): BattlefieldData | undefined {
    return BATTLEFIELDS.find((b) => b.id === id);
}

/**
 * 🔴 [2026-09-25 主人「有的战场离据点太近了，根本无法连接，怎么办。能不能归为一个点」]
 * 离某座据点不超过这个距离（公里）的战场，在**路网里归为那座据点**：不另立节点、不用画支线，
 * 道路连到这个战场就等于连到那座城。战场自己的坐标、标牌、剧本位置一概不变，只是「走路」共用那座城。
 * 实测（2026-09-25）：格拉尼库斯河距格拉尼库斯 0.4 km、锡尔河距忽毡 5.3 km → 归并；
 * 其余最近的海达斯佩斯河距蒙格 23 km，照常画支线。
 */
export const BATTLEFIELD_MERGE_KM = 10;

const mergedCityCache = new Map<string, string | null>();
/** 这个战场在路网里归到哪座据点（null = 不归并，是独立的路网端点） */
export function battlefieldMergedCityId(bfId: string): string | null {
    if (mergedCityCache.has(bfId)) return mergedCityCache.get(bfId)!;
    const bf = getBattlefield(bfId);
    let best: string | null = null;
    if (bf) {
        const r = Math.PI / 180;
        let bestKm = BATTLEFIELD_MERGE_KM;
        for (const c of CITIES_V2) {
            const h = Math.sin((c.lat - bf.lat) * r / 2) ** 2
                + Math.cos(bf.lat * r) * Math.cos(c.lat * r) * Math.sin((c.lng - bf.lng) * r / 2) ** 2;
            const km = 12742 * Math.asin(Math.sqrt(h));
            if (km <= bestKm) { bestKm = km; best = c.id; }
        }
    }
    mergedCityCache.set(bfId, best);
    return best;
}

/**
 * 🔴 [2026-09-19 主人定] 某位武将的史实战役 → 它对应的战场（战役**打在哪**）。
 *
 * 主人原话：「每个武将一个真实的历史事件」。战役条目上新增的 `generalId` 就是「这一仗是谁的」，
 * 本函数把它翻成战场记录（`bf_*`，含坐标与地名），玩家要赶路去的就是这个坐标。
 *
 * ⚠️ 与 `matchesBattlefield`（把战场配回战役）方向相反：那个是**战场 → 战役**（跑步时反查），
 *    这个是**武将 → 战场**（玩家接活时正查），两个函数都必须走同一条数据契约，
 *    即「战役与战场同年 + 坐标接近」，故此处复用 `matchesBattlefield`，不另写一套判据。
 *
 * @returns 找不到配对的战场时返回 null（＝这条事件还没在 `Battlefields.ts` 里落战场，玩家接不了）
 */
export function findBattlefieldOfGeneralEvent(
    eventYear: number,
    loc: { lat: number; lng: number } | null | undefined,
): BattlefieldData | null {
    if (!loc) return null;
    return BATTLEFIELDS.find((b) => matchesBattlefield(b, eventYear, loc)) ?? null;
}

/**
 * 这条战役事件**打在哪**（战场坐标）。
 *
 * 🔴 [2026-09-19] 与运行时 `findBattleForBattlefield`、编辑器 `loadDrafts` **同一套口径**：
 *    · 野战 / 有 `location` 的条目 → 直接用 `location`
 *    · 攻城战没有 `location`（推罗就是靠 `defenderCityId` + 航点走的）→ 取**被攻据点**的坐标
 *   三处只要有一处口径不同，就会出现「编辑器配得上、玩家却到不了」的暗坑。
 */
export function battlefieldLocationOf(
    data: { location?: { lat: number; lng: number } | null; defenderCityId?: string } | null | undefined,
    cityLookup: (id: string) => { lat: number; lng: number } | undefined,
): { lat: number; lng: number } | null {
    if (!data) return null;
    if (data.location) return data.location;
    if (data.defenderCityId) {
        const c = cityLookup(data.defenderCityId);
        if (c) return { lat: c.lat, lng: c.lng };
    }
    return null;
}

/* ── 【战场 ↔ 剧本条目 的唯一配对判据】2026-09-17 ─────────────────────────────────
 * 修的是**两处判据不一致**：改之前运行时（HistoricalEventManager.findBattleForBattlefield）
 * 按「距离 ≤ 0.15 度」配、**完全不看年份**，取数组里第一个命中的；编辑器（battlefield-editor）
 * 按「同年 + 0.5 度」配。两条隐患：
 *   ① 运行时不看年份 → 同一地点将来有第二场战役（同地二战很常见），必定配错年份的那场；
 *   ② 容差差了三倍多（0.15≈16km / 0.5≈55km）→ 坐标写歪 0.3 度时，编辑器显示配对成功、
 *      运行时却找不到，表现为「点了战场没反应」，而编辑器一切正常，极难查。
 *
 * 实测（scratch/_probe_bf_match.mjs，14 条战役 × 14 个战场）：现有数据坐标**全部一字不差**
 *   （距离全是 0）、年份全对、两种容差下都唯一命中 —— 没有实际故障，所以收紧判据零风险。
 *
 * 判据取 BattlefieldData.lat/lng 头注那句已有的数据契约：「必须与该场剧本 location 一字不差」。
 * 容差留 0.15 度不是为了容错，是给攻城战用的 —— 攻城战条目没有 location，坐标取被攻据点的，
 * 与战场标记点本来就会差一点。真要一字不差就把它调成 0。
 */
export const BATTLEFIELD_MATCH_DEG = 0.15;

/** 这个战场是不是那条剧本条目打的。两处（运行时 / 编辑器）必须都走它，别再各写一套。 */
/**
 * 🔴 [2026-09-24 主人定「所有事件就两种：野战，战后显示战场；攻城战，攻城战必须有据点……攻城战，你搞什么战场呀」]
 * 攻城战没有战场记录。它在剧本流程里的编号（记「打没打过」、排先后）由「被攻据点 + 年份」生成，不画任何战场标牌。
 */
export function siegeSiteId(cityId: string, year: number): string {
    return `siege_${cityId}_${year}`;
}
export function isSiegeSiteId(id: string): boolean {
    return id.startsWith('siege_');
}

export function matchesBattlefield(
    bf: BattlefieldData,
    eventYear: number,
    loc: { lat: number; lng: number } | null | undefined,
): boolean {
    if (!loc) return false;
    if (bf.scriptYear !== eventYear) return false;
    return Math.abs(bf.lat - loc.lat) <= BATTLEFIELD_MATCH_DEG
        && Math.abs(bf.lng - loc.lng) <= BATTLEFIELD_MATCH_DEG;
}
