> # 🔴 铁律 · 兵种只认素材样貌，不认兵名
>
> 判断一个兵种能不能进某支军团，**只看 `public/SUCAI/` 里的素材长什么样**——甲胄形制、武器、马匹、旗号。
>
> 兵种 id 里的民族词（女真 / 波兰 / 越南 / 图皮 / 库曼…）、中文兵名、`age` 时代标签，**全都不算数**。
> 那是《帝国时代 2 DE》的出处标签，不是本作里这个兵属于谁。
>
> 反面教材（都被主人当场抓过）：
> - 「契丹军团用女真铁浮屠精锐 = 乱炖」→ **错**。契丹有铁林军重甲骑兵，那副具装甲骑的样貌本来就对。
> - 「库曼钦察弓骑不该给东北民族」→ **错**。那个素材的样貌和东北人本来就像。
> - 「捷克军团 4 档是波兰奥布奇战锤兵 = 乱炖」→ **错**。胡斯军本来就用连枷 / 战锤。
>
> **没裁图看过素材，就不许下「不符合历史 / 跨国乱炖 / 时代不对」的结论。**
> 裁图工具：`scratch/tools_sprite_sheet.py`。判据优先级：**样貌 → 兵种类型 → 时代 → 名字**。

# 武将技参数速查

> 旧 `tac_*` 参数表已退役。现行技能数量和参数持续变化，不在文档复制整张目录，避免与代码漂移。

## 现行单一真理

- 战术技定义与 `magnitude`：`src/data/TacticalSkillCatalog.ts`
- 战略技定义与 `magnitude`：`src/data/general-skills/catalogs.ts`
- 武将攻防六槽：`src/data/general-skills/profiles.ts`
- 三势系数：`src/combat/TacticalConstants.ts`
- 战术技执行：`src/combat/GeneralSkillCombat.ts`
- 战斗时长、胜负与战损：`src/combat/BattleField.ts`
- 文化、精锐、关隘等全局乘区：`src/config/GameConfig.ts`

## 装配口径

- 普将：攻方三槽 + 守方三槽
- 名将：攻方三槽 + 守方三槽 + 一个战略技
- 每场战斗按攻守侧和优/均/劣局势，从六槽中选一个战术技释放
- `aptitude` 与 `attackStyle` 均为武将标签，不计入技能数量
- `tacticalSkillId` 是招牌/回退索引，不计入六槽之外

## 检查命令

```bash
npm run tactical:assign-audit
npm run tactical:triclass
npm run expedition:triple-check
```

若检查结果与本文冲突，以代码和命令输出为准。
