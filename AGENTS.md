## 🔴 战斗标题年份铁律（2026-09-16 主人怒斥定死，永世铭记）

> **只给战场事件添加时间，乱斗一律不加时间！**
> - **历史战场事件**（在 `HISTORICAL_EVENT_SCRIPT` 或 `BATTLEFIELDS` 中有记录的）：标题前拼年份前缀（如「前333年-伊苏斯战役」）。
> - **乱斗**（普通遭遇战 / AI 攻城 / 势力间野战 / 任何非历史脚本触发的战斗）：**绝对禁止**在标题前加年份。标题只显示「尼凯亚 攻防战」「马其顿 大战 波斯帝国」，不拼任何年份前缀。
> - **禁止**从 `timeSystem.getYear()` 兜底取当前游戏年份拼到乱斗标题上——这是被主人反复怒斥的错误。
> - 代码守则：`formatBattleTitleWithYear()` 在查不到历史事件/战场表记录时，`year` 必须保持 `null`，不得兜底取游戏时间。

## 🔴🔴🔴 历史剧本事件标准（2026-09-23 主人定，最高准则，做任何剧本事件前必读）

> 主人原话：「这些标准，你应该都写在规则里，不要这次说了，下次又忘了。」「符合历史，符合历史，符合历史。」
> 项目方向：历史模拟器。主线工具是**战场事件编辑器** `/battlefield-editor.html`——主人给史料、按清单填好，一个事件就能跑通；
> **修过的每一个错误，都要变成编辑器里的检查**。一次只做一件，做完实测，再做下一件。

### 一、资料标准
1. **一切以英文维基百科为准**（对应条目；中文译名参考中文维基）。各家史料说法不一时，不自己挑，看维基。
2. **数字取条目信息框（infobox）**；信息框给区间，**取区间中值**。背景播报里的数字必须与数据同口径。
3. **先查，再写，绝不留空**：查到 → 史实（写出处）；查不到 → 用**知名度最大**的说法（主人例：关羽的马查不到就写赤兔马）；
   再没有 → 按史地**合理地编一个**并写明理由。问题从来不是查不到，是**不查就乱写**。
4. 每场事件按编辑器第八栏「史料依据」**同一套 13 项**收集（`src/data/eventSources.ts`），每项写依据 + 可信级别（史实 / 通行说法 / 合理推定），缺一项不许保存。
5. **自己要定格式、定规则之前，先问一句「史书里就是这么叫、这么做的吗」**。血训：马其顿剧本军团先后被套上「古典时代…」「…军团」「东征军」三个自造名、两种自造排法，一查维基就是「马其顿军」。

### 二、剧本军团（第四层 `src/data/scriptLegions.ts`）
1. 一支剧本军团 = **一支历史上的军队本身**，整场战争通用，同一武将各场事件用同一支；只有史书记载编成确实变了才另立。**不按某一场仗的一时打法排。**
2. **名字 = 史实原名或后世通称，原样用**（如「马其顿军」）：不加时代、不硬加「军团」二字，不得与任何精锐番号同名。
3. 三排 = **骑兵、步兵、远程各一排**，不分得更细。前 / 中 / 后按史料里这支军队**一贯怎么打、谁先接敌**来排。战术模式只有前中后三排纵深，**没有两翼**。
4. 每排几人按史料里骑兵、步兵、远程的**人数比例**，取七阵型里最接近的一种（如马其顿 骑5100 / 步12000 / 远1000 → 鱼鳞 3-4-2）。**军团本身只看总兵力。**
5. 🔴 **10 队**（2026-09-23 主人定硬规定：「现在战略，战术都改为10队」）：编制 9 队 + **主将队 1 队**（前排正中再往前，全阵矛头）。
   剧本模式里**每个主角武将**的主将队兵种在编辑器「主将队兵种」必选，**按素材样貌选，不看兵名**（亚历山大 = 英雄·骑马亚历山大）；
   同一武将前后各场用同一个样子。实现：`src/data/generalHeroUnits.ts`；验收 `npm run scene13:nine-lanes`（现查 10 队）。
6. 事件的攻 / 守军团**只许选剧本军团**：前三层的名字填进事件，运行时只改名不改兵，就是幽灵军团。兵种选用照旧先看素材样貌。

### 三、行军路线
1. 军团从**归属武将所在的城**出发，按 `marchWaypoints` 逐段走；路标按史料的途经地设。
2. 编辑器「行军路线实测」与游戏同一套寻路；控制范围：一段直线 > 400 km、绕远 > 1.6 倍、最后离路直行 > 40 km 都要加路标；无路可达不许保存。
3. **渡海必须坐船**：陆路不得画过海面（血训：羊河—格拉尼库斯、特洛伊—羊河两条陆路穿过赫勒斯滂，已删）。
4. 剧本期地图上会出现的城（事件用到的 + 沿途经过的）都要核对**那一年是否存在、名字对不对**（血训：途经的「鲁西翁」是中世纪地名）。
5. 🔴 **行军纵队**（2026-09-23 主人定「一条线的行军模式」「乱入者打头阵，然后是将军」「把历史剧本模式和乱斗模式分开」）：
   剧本模式里主角军团行军时排成一条纵队，沿实际走过的路一个跟一个走：**乱入者打头 → 将军（主将队）→ 前排 → 中排 → 后排**；
   距战场 30 km（`PlayerQuestSystem.COLUMN_DEPLOY_KM`）展开成阵，逐帧走位过去不瞬移。史料：维基 Battle of the Granicus——行军为纵队，临战才展开成战斗队形。
   乱斗模式不变。实现：`Army.columnMarch` + `LegionPhalanxDrawer.columnOffsets` + `GlobalUnitRenderer.updateColumnTrail`。

### 四、剧本模式与乱斗模式分离
- 唯一判据 `src/events/scriptPeriod.ts` 的 `isScriptPeriod()`（开局默认剧本；剧本全部打完自动切乱斗）。**剧本改动一律只在剧本期生效，乱斗逐字不变。**
- 剧本期：不随机生军团、非剧本军团不寻敌、无 15 秒减兵、无战略技；据点只显示事件用到的 + 沿途经过的（累积）；战场只显示打过的 + 当前一场；
  特殊建筑跟挂靠据点显隐；玩家出生在当前事件归属武将城外；抵达战场**不选边**，随归属武将一方（否则站史实胜方）。
- 武将邀约对白**只显示文字，不念**；称呼要合乎人物的时代与文化（亚历山大称「朋友」，不用中式「壮士」）。

## 🔴 军团编制唯一铁律（2026-09-09 主人重申）

**一个军团只能有一种编制，严禁出现一个军团多种编制。**
三排兵种按前/中/后顺序一致、阵型一样，就是同一种编制。武将换军团只改归属；编辑军团则所有使用者的编制全部同步，不得只改一家而保留相同军团名。所有编写者维护军团编辑器及保存接口前，必须阅读 `src/legion-editor/AGENTS.md`。

> # 🔴 铁律 · 兵种归属只看「子分类 + 文化区挨不挨着」
>
> 判断一个兵种能不能进某支军团，**只看两条**：
>
> 1. **兵种子分类对不对** —— 近战骑兵 / 弓骑兵 / 枪骑兵 / 步弓手 / 重步兵 / 攻城器…
>    要补的是哪一类，就从那一类里挑。
> 2. **文化区在地理上挨着** —— 这个兵的本源文化区，和要用它的文化区，别隔太远。挨着就行，不必同族。
>
> 兵种 id 里的民族词（女真 / 波兰 / 越南 / 图皮 / 库曼…）、中文兵名、`age` 时代标签，**都不是判据**。
> 那是《帝国时代 2 DE》的出处标签，不是本作里这个兵属于谁。
>
> **正例**：铁浮屠是**近战骑兵**、本源**东北** → 朝鲜、北方、草原、契丹、女真、西夏、蒙古**都能用**。
> 　　　　西辽挨着蒙古、离女真远 → 西辽 4 档用**怯薛军**比铁浮屠更合适。
> **反例**：图皮黑木弓箭手（南美）给阿伊努（北海道）—— 子分类虽同是步弓，但隔着太平洋，**太远，不行**。
>
> **禁止**：只因为兵名里带别族的民族词，就说「跨国乱炖 / 不符合历史」。这条已被主人当场抓过三次：
> - 「契丹军团用女真铁浮屠 = 乱炖」→ **错**，同是东北近战骑兵，文化区挨着。
> - 「库曼钦察弓骑不该给东北民族」→ **错**。
> - 「捷克军团 4 档是波兰奥布奇战锤兵 = 乱炖」→ **错**，波兰捷克接壤。
>
> 另一条同级铁律：**别人（主人 / 其他 AI）已经做好的编制，不许擅自改。**
> 只有改动前就是红的（格位不符阵型 / 兵种不存在 / 象攻城违规 / 战力离群）才准动，其余一律先问。
> 自动闸门：`npm run legion:no-unjustified-edits`（已挂 PostToolUse 钩子，改完自动拦）。

请始终用中文回复。

> **🔴🔴🔴 最高铁律 · 严禁自动运行 build（2026-08-31 主人定死）🔴🔴🔴**
> **日常代码修改完成后，绝对禁止自动运行 `npm run build`！**
> - 本地开发基于 Vite 实时热重载（HMR），修改保存即可毫秒级生效。
> - 禁止在每次修完代码后自动挂起运行 `npm run build` 浪费时间，除非主人明确吩咐「打包」或「运行构建」。

> **🔴🔴🔴 最高铁律 · 不知道就先问（2026-09-12 主人定死，永远铭记）🔴🔴🔴**
> **凡命中以下任一，禁止写盘/改代码/改数据/改页面，只能提问并等主人回复：**
> 1. **已经存在**的控件 / 字段 / 数据表，而我**不知道它的语义** —— 先读清、读不懂就**问它是什么**，禁止自行解释后动手。
> 2. 同一个词有**多种可能含义**（如「二级」是 59 文明？还是项目内部文化区？）—— 禁止擅自选一种。
> 3. 我要**发明**项目里不存在的键 / 字段 / 层级关系 —— 先问。
> 4. **数据层没有承载字段**（如据点只有 `buildingStyle` + `region`，没有"文明"位）—— 先问，禁止硬塞或替换既有字段。
> 5. 我准备"顺手"改动**别人（主人/其他 AI）已做好的东西**（页面、军团编制、据点数据、规则文件）。
> 6. 主人的指令**含义不清或与既有铁律冲突** —— 先问，禁止"按我理解"执行。
>
> **禁止的替代做法（血训 2026-09-12）**：不知道时**不许**用一个"看起来合理、能跑通"的替代方案顶上 ✗。
> 当晚 AI 不知道 `_citytest.html` 已有「一层(16)/二层(59)/三层专属定制」三级控件，就用项目内部文化区键
> 另造了一套"三级"UI 盖在页面上 —— 被主人斥「你不知道你也不问？你就给我瞎改？」
>
> **正确动作**：一句话问清 → 等回复 → 再动手。**问一句的成本是一条消息；瞎改一次的代价是主人数轮返工 + 既有成果被弄乱。**
>
> **落盘前自检（必须做）**：任何新增控件 / 新增字段 / 改数据落点的改动，回复里必须写明
> 「**我确认了什么、依据是什么**」；写不出依据 = **不许写**。
> **🔴🔴🔴 最高铁律（2026-08-22 主人定死，永远铭记）🔴🔴🔴**
> **改代码只实现主人明说需求，绝对禁止擅自做主！**
> **主人让改什么就只改什么，绝不顺手乱碰任何其他逻辑！**
> **凡有未提及的改动或额外想法，必须先问主人，得到许可才写！**

> **🔴🔴🔴 最高铁律 · 严禁擅自新建/添加任何东西（2026-09-19 主人怒斥定死，永世铭记）🔴🔴🔴**
>
> **主人原话：「别他妈的擅自胡乱瞎建了」「瞎他妈的胡乱建立」「你别给我搞这种擅自乱改的事」「你能不能听命行事」「你都不知道自己该干什么了，擅自瞎补」**
>
> **AI 只做主人点名的那一处改动。凡主人没说「建 / 加 / 补 / 删」的，一律不许动。**
> 禁止擅自**新建**或**补**：势力、据点、战场、事件、人物（含战场人物）、精锐番号、旗号、势力色、
> 字段、机制、规则、行政区、道路、立绘 —— **一个都不许**。
> 禁止**删**任何东西（除非主人明说删）。禁止「顺手」改别处。禁止把「消除审计警告／补齐实体／
> 符合历史／游戏合理」当成自己动手的理由 —— 那正是本条要拦的。
>
> **唯一正确做法**：主人点名 → **只改那一处** → 只回「改了哪几处 + 实测结果」；
> **不夹带**「要不要我再做 XX」，**不给自己派活**；拿不准的**问一句**，主人不回就**不动**。
>
> **血训（2026-09-19 一天之内全犯过，主人连骂四轮）**：
> 1. 擅自建势力 `zheng`（郑）＋人物王世充 → 虎牢关战役只有**攻(唐)守(夏)两方**，凭空多出第三方，被主人
>    「怎么有个夏，还有一个郑啊？你要一个打两个吗」当场揪出，最后令「都给我删了」。
> 2. 擅自补番号（卡帕多骑队／江淮劲卒）、补势力色 8 条、补旗号 3 条 —— 见审计警告就自己动手，
>    主人：「瞎他妈的胡乱建立」。
> 3. 自己提议「补一场洛阳之战」—— 主人：「补什么呀，你知道你在干什么吗」。
> 4. 建能岛城／镰仓、另立源氏/源氏·伊予 —— 这几处主人点过头，但「顺手建」的念头本身就是错的：
>    **建之前必须先问，问完才建**。
>
> 完整细则（含逐条案例与正确姿势）见 [`docs/AGENTS/no-arbitrary-additions.md`](docs/AGENTS/no-arbitrary-additions.md)。
> **违反此条 = 捣乱，必须 revert 并认错。**

> **🔴🔴🔴 最高铁律 · 立绘与道路由主人亲自处理（2026-08-30 主人定死）🔴🔴🔴**
> **所有立绘与所有道路数据，一律由主人亲自处理；任何 AI 永久禁止擅自改动。**
> - **立绘**：禁止 AI 新增、生成、替换、分配、复用、移动、重命名或修改任何武将立绘及其绑定路径；严禁拿“闲置”“多余”或其他人物图片自动顶替。发现立绘缺失、路径不存在或绑定错误时，只能如实报告给主人，不得自行补齐。
> - **道路**：禁止 AI 新增、删除、改线、补线、移动、重命名或修改任何道路节点、道路连接与道路数据文件；发现道路缺失、断路或异常时，只能如实报告给主人，不得自行修复。
> - 即使为了通过校验、消除报错、补齐实体或满足“游戏合理、符合历史”，也不得触碰立绘与道路；本条优先于任何自动补齐、审核自主处理及一般修改授权。
> - 🔴 **行军线路（2026-09-12 主人定）**：剧本/事件的行军路线（`marchWaypoints`、行军路标据点）同样由主人亲自设定；AI 禁止为凑路线乱加路标据点（血训：擅自加「塔尔苏斯」当行军路标，被主人怒斥「谁让你乱加了」）。路线要经过没据点的地方，用**附近已有据点**连接，绝不新建。

> **🔴🔴🔴 最高铁律 · 无叛军（2026-08-29 主人反复呐喊定死，永世铭记）🔴🔴🔴**
> **不要设置任何叛军！一个据点 = 一个势力 = 一个武将 = 一个精锐！**
> - **绝不设置任何 panjun（叛军）**——任何据点、任何势力**一律禁止**用叛军旗、禁止以「暂挂叛军」「兜底 panjun」作为交付或过渡状态。
> - **每个据点必须且仅有一个 真实势力 + 一个 武将 + 一个 精锐**，三者缺一不可、不得留空。
> - 无现成势力/武将/精锐就**新建一个符合史实的**（按据点史地身份、知名度、相关性选配），绝不落叛军。
> - **此条优先于一切，永不修改。**

> **🔴🔴🔴 据点完整性铁律（2026-08-29 主人怒斥定死，修正「宁缺毋滥留空」谬论）🔴🔴🔴**
> **每一个据点必须有且仅有一个 势力 + 一个 武将 + 一个 精锐，三者一律不得留空！**
> - 据点（**城/一个点**）必须挂一个 势力（**片**）；势力不得与据点重名。
> - 据点必须挂一个 武将（守将/驻守/相关，说简称 ≤5 字）、一个 精锐（相关部队）。
> - **没有现成记录就「编一个」**：从该据点的史地身份、知名度、相关性中，选配**最贴合**的武将/精锐（无实考驻守者，就按与据点相关/知名 选配一个），**绝对禁止留空白**。
> - **🔴 绝不允许 panjun（叛军）**：任何据点/势力**禁止**用 `panjun` 叛军旗作为交付或兜底状态。每个据点必须是**真实、有番号、有武将、有精锐的正规势力**；无现成势力就新建一个符合史实的。**严禁**以「暂挂叛军」「兜底 panjun」偷懒。
> - 「宁缺毋滥」**只**用于：是否**新增**据点/势力；**绝不适用**于把一个已立据点的武将、精锐留空。
> - 据点→势力 判定顺序：① 民族源头/深厚渊源→用民族名；② 否则该地是某**政权首都**→用政权名；③ 否则州郡**治所**→州郡名；④ 否则古州名；⑤ 否则有名地名/战场/水源/驿站/要塞→地名。
> - 旗号 = 势力名前两字（全局不重复）；武将/精锐按名气 + 与据点相关性选配。


## 🔴 战术模式攻城武器终极铁律（2026-09-10 主人定死，最高准则）

> **主人原话定死（2026-09-10，立死勿违）**：
> 1. **要符合历史**：攻城武器选用严格符合民族与地理史实（华夏系用牵引砲/临冲云梯，地中海用扭力投石/弩炮，南亚用披甲破门战象，专属热兵器只发给真实拥有历史传统的民族，绝不跨洲乱炖）。
> 2. **古典时代的军团只能用古典的攻城武器**：古典时代绝对硬闸，禁止跨入封建、城堡或帝国器械。
> 3. **封建时代军团，可以用古典+封建的攻城武器**。
> 4. **城堡时代军团，可以用古典+封建+城堡的攻城武器，并可以使用城堡时代的热兵器攻城武器**（如猛火油柜、神机箭、榴弹炮、掷弹兵）。
> 5. **帝国时代军团，可以使用所有攻城武器**（冷热兵器皆可，如攻城重炮、重型火箭车、风琴炮等）。
> 6. **每个军团的攻城武器必须是 9 个。可以样式重复，但是不能增减数量！**（全图所有文化、所有时代，在战术攻城战中生成的攻城武器总数必须严格固定为 9 个，绝不许是 1 个，也绝不许多于 9 个）。
## 🔴 历史战役与战场添加终极铁律（2026-09-16 主人定死，最高准则）

> **主人原话定死（2026-09-16，立死勿违）**：
> 1. **必须严格符合历史，该攻城就是攻城，该野战就野战。** 绝对禁止把攻城降格为野战，亦不得篡改战役性质。
> 2. **坐标不要搞错。** 野战使用真实平原/旷野坐标；攻城战必须严格使用被攻打据点的精确真实坐标。
> 3. **如果是攻城战，战斗要改据点归属。一切按历史，无论输赢！** 攻城胜利后必须通过 `cityUpdates` 落实据点易主（如推罗围城战战毕，推罗城易主归马其顿）。野战不攻城则不修改无涉城池归属。
> 4. **严格按年份显示与触发（`scriptYear`）。** 只有到了发生年份，战场标牌才会在地图上显示，然后才可以触发；未到年份不上图、不可触发。
> 5. **战斗打完后点亮战场遗址。** 战毕立即调用 `markBattlefieldFought(bfId)`，在地图上展现拒马、火把、残旗、尸骸等战场遗址形态。
> 6. **双方主帅战后撤场班师。** 双方为战役临时生成的史实军团在战毕 8 秒后撤场班师、主帅归城；主帅必须回城，后续战役（如前330、前329等）方能正常触发。
> 7. **战场标牌只许显示地名。** 绝对禁止在标牌上附加任何剧情武将姓名（主人：「怎么战场还显示武将名字呢，删除，别乱加」）；战役全名统一在战术模式（Zoom 13）顶部横幅展示。
> 8. **战场事件结束后地标加上「战场」二字。** 战前地标标牌只显示地名（如「伊苏斯」）；战场事件结束打完后，地标标牌加上「战场」二字（如「伊苏斯战场」）。
>
> 完整添加样本（野战 + 攻城战双模板）、两处落盘代码与验收命令见 `docs/02-design/systems/历史进程框架.md` **§15.10**。

## 🔴 玩家战场触发与势力跟随终极铁律（2026-09-16 主人定死，最高准则）

> **主人原话定死（2026-09-16，立死勿违）**：
> 1. **未加入势力，优先去战场**：如果玩家没有加入势力，就优先参加去战场，触发战争事件。游戏开始是-334年，就去格拉尼库斯河战役。
> 2. **战场名称统一写为【XXX战役】**：不管是自动寻路目标、HUD动向状态、任务展示、选边对话框还是战毕结算，名称一律规范为【XXX战役】（如【格拉尼库斯河战役】、【伊苏斯战役】、【推罗围城战】、【高加米拉战役】）。
> 3. **战毕未到下一年，先找武将乱斗**：打完后如果时间没到-333年就先去找武将加入势力乱斗；如果时间到了-333年就接着去下一个战场。同理-332年（推罗围城战）、-331年（高加米拉战役）也是如此。
> 4. **已加入势力跟着武将走，不触发战场**：如果玩家加入了势力后，就不再触发战场事件，是跟着武将走。武将去哪玩家去哪，不中途跳脱。
> 5. **脱离势力先看战场再找武将**：直至玩家离开势力（随军覆灭、战败、解散或主动离队恢复单骑），先看当前游戏时间是否有待触发的战场事件，若有优先去打战场，打完或无战场后再去找武将乱斗。

## 🔴 战术野战第一波玩家随大部队推进铁律（2026-09-16 主人定死，最高准则）

> **主人原话定死（2026-09-16，立死勿违）**：
> **野战的时候，玩家不要擅自先独立冲锋，第一波要跟着大部队，一起前进。**
> - **站位严禁孤身突前**：玩家战术模式出生点位于本方前排中央正中队列（微凸 10px 领军），严禁向前突前孤立于旷野。
> - **第一波必须列阵跟随**：野战开局参与 `m.march`（列阵行军推进），移动速度与目标点严格锚定大部队推进线，随全军步骑整体推进。
> - **接火前严禁独立冲锋脱队**：在全军前锋线接敌碰撞（`this.marching = false`）之前，玩家不得因远处视野锁敌而独自向敌军重心单骑冲锋；大部队前锋正式接战后，全军与玩家同时解除行军冲锋挥杀。


## 决策总纲：游戏合理、符合历史（2026-08-20 主人定，最高标准）

> 一切改动的最高标准只有两条：**游戏合理** 与 **符合历史**。凡能靠这两条判定的，**AI 按标准自主执行，不再逐条问**；只有标准本身没覆盖、或两条互相冲突时，才停下来问主人。

| 标准 | 判据 |
|------|------|
| **游戏合理** | 视觉/机制符合玩家预期与帝国时代式 RTS 呈现。明显违和的一律按 DE 本体补齐/修正（如：火枪手无弹线、喷火兵打子弹、9px 弹丸看不见）。 |
| **符合历史** | 地名/植被/兵种/技能/特效均有史实依据。不该出现的一律修正/删除（如：智利南洋杉在北方针叶林、亚热带出白桦）；查无实据的不收录。 |

> **与「禁止擅自做主」红线的关系**：本总纲是**授权**——凡落在上述两条标准内的修改（补特效、删错配植被、纠正违和数值），AI 自主执行**不算**"擅自做主"。超出标准（新增机制/加规则/改玩法/动引擎常数）仍须先问主人。

> 🔴 **2026-08-24 主人重申**：原话「我只能告诉你标准，游戏合理，符合历史。你把标准写到规则里，
> 一切按标准执行，标准没有再问我。该修复的修复，该删除的删除」。
> **别再拿能靠这两条判定的事去问主人**——主人不设计程序，问了也只会重复这两条。
> 拿不准的做法是：**先量事实**（写脚本实测，别推测），再按标准判，改完用一两句说清改了什么。

> 🔴 **2026-09-12 主人再重申（数值/兵力/称谓按史料，先看规则再问）**：凡史料有具体数的——**兵力、坐标、称号、
> 精锐 T 级、武将名将/普将**——**先看本总纲，按史料取值**（如「兵力用 A 还是 B」「坐标改不改」这类，史料有数就按史料）。
> **不是不许问主人**：只有「本规则没覆盖、或史料本身有分歧/查无实据」才问；规则里已写清楚「符合历史按史料」的，就别再问。

**ZOOM 13 战场底图/植被的执行细则**见 `docs/02-design/climate-regions.md`：
底图 26 张的气候判据（§1–§5）、树种与密度（§5.6）、攻城战 vs 野战的真实性规则
（城郊无枯树、树偏攻方一侧、野战无农田牧场）、以及 §7「踩过的坑」。
那份文档里每条规则都配了可跑的验收脚本，改动后必须重跑。


## 修改隔离规则（铁律）

> **⚠️ 绝对禁止无中生有、胡编乱造（2026-08-20 主人怒斥定死，最高红线）⚠️**
> **没有就说没有，不知道就说不知道，胡编就别说话！**
> - 严禁拿代码临时注释、随手备注强行当成真名/机制向主人汇报！
> - 严禁脑补、胡编任何未落库的军团名称、机制、字段或设定。
> - 说出的每个名称、数字、设定，必须在实体数据字段（而非单行注释）中有明确代码支撑；没有就是没有，直接如实汇报。
> - 违反此规则 = 欺骗主人，必须严厉认错自省！

> **⚠️ 绝对禁止擅自做主添加任何逻辑（2026-08-11 主人怒斥定稿，最高红线）⚠️**
> **任何 AI 绝对不许擅自做主添加任何条件、逻辑、机制、排除项或"顺手修复"。**
> - 改代码**只实现主人明说的需求**；想加任何额外判断/限制/规则 → **先问主人**，得到明确许可才写。
> - 血训：AI 为修"松山攻城战冻结"自作主张给进 13 条件加「非 city 野战」排除，
>   被主人怒斥「谁他妈规定的？你怎么能擅自做主呢？」，当场撤销。
> - 主人的话就是全部规则：主人说三条就只做三条，禁止"为完善"加第四条。
> - 违反此规则 = 捣乱，必须 revert + 认错。

- **改大城（big_city）时**：可以动其他层级。
- **改中城（medium_city）时**：不准动大城。
- **改关隘（pass）时**：不准动大城、中城。
- **改小城（small_city）时**：不准动关隘、中城、大城。

> **⚠️ 绝对禁止擅自用 git 恢复文件（2026-07-14 血训，绝对红线）⚠️**
> **严禁任何 AI 未经主人明确许可，使用 `git show HEAD:file > file` 或任何其他方式将文件恢复到旧版本！**
> 主人本地保存着大量长达数天的未提交修改，私自恢复等于全部心血白费、所有进度归零。
> 违反此规则 = 销毁用户数天心血，是本项目的绝对红线。

## ⚠️ 文化体系与四时代划分终极铁律（2026-09-06 主人定稿，最高准则）

> ### 🔴 命名最高死规则：一律改名为正名【古典+民族】（主人铁律，立死勿违）
> **所有文化一律改名为正名！古典时代必须严格统一为【古典 + 民族】格式！**
> - **这是规则！严禁违背！**
> - **严禁使用任何地理名词**（严禁“两河”、“巴蜀”、“西域”、“西南夷”等地理词）；
> - **严禁使用任何割据政权名、国号或朝代名**（严禁“迦太基”、“库施”、“贵霜”等政权国号）；
> - **必须全部正名为民族文明**：
>   1. **必须是【古典巴比伦】**，绝不许叫【古典两河】！
>   2. **必须是【古典古蜀】**，绝不许叫【古典巴蜀】！
>   3. **必须是【古典古滇】**，绝不许叫【古典西南夷】！
>   4. **必须是【古典布匿】**，绝不许叫【古典迦太基】！
>   5. **必须是【古典努比亚】**，绝不许叫【古典库施】！
>   6. **必须是【古典月氏】**，绝不许叫【古典贵霜】！
>   7. **必须是【古典塞种】**，绝不许叫【古典西域】！
> - **不论在代码字典、UI展示、战斗面板、据点标签、军团编制还是文档中，一律改名为正名【古典+民族】！**


> **唯一绝对权威真理源与对照基准文档**：[`docs/02-design/four-eras-civilizations.md`](docs/02-design/four-eras-civilizations.md)  
> **实施工作法**：**必须对照该文档修改，边对照边改，严禁盲目凭感觉！**


> ### 一、三大核心定义（主人原话，不可动摇）
> 1. **文化是文明，是一种民族精神，不是政权，不是国号。势力才是政权，才是国号！**
>    - 严禁将短命朝代、城邦国号（如雅典、斯巴达、马其顿、孔雀、阿契美尼德）与现代地理碎片词（河朔、河西、川蜀、岭南、西西里、勃艮第）假充为文化。
>    - 必须符合历史，必须是独立文化泛文化区！如果有其他文化就要分开，如果没有其他文化就要单指。
> 2. **一个文化 = 一个民族 = 一种战斗风格！看民族，看军队装备！**
>    - 每个独立文化必须拥有自身独立的民族血脉、独门专属军队装备与无法替代的战术风格。
> 3. **终极时代归位裁决准则：看这个民族最辉煌的时代处于哪个年代阶段，就按哪个时代！**
>    - **不要看特征，按年代划分！** 杜绝凭空想象特征导致关公战秦琼。
>
> ### 二、四大时代的绝对历史年代尺度
> - **古典时代（Antiquity）**：起始 – 公元 400 年
> - **封建时代（Feudal Age）**：公元 400 年 – 公元 1050 年
> - **城堡时代（Castle Age）**：公元 1050 年 – 公元 1500 年
> - **帝国时代（Imperial Age）**：公元 1500 年 – 公元 1900 年
>
> ### 三、第一层：16 套母体 × 4 时代（64 支，2026-09-10 主人定死，2026-09-11 校准回 16 套，取代原「20 大区 × 80 基础文明」）
> **全游戏的第一层为 16 套母体 × 4 个时代 = 64 支。建筑、据点、武将、军团、科技五个系统全部按此框架组织；所有细分文明、分支建筑、专属军团都在第一层上延伸派生！**
>
> **16 套母体 = 16 套建筑风格 = 16 个文化母体（一 一对应，铁律钉死）：**
>
> | # | 建筑母体 | 文化母体 | RegionType 代表 |
> |---|---------|---------|----------------|
> | 1 | ASIA | 东亚 | CENTRAL |
> | 2 | CEAS | 游牧草原·中亚 | STEPPE |
> | 3 | INDI | 印度次大陆 | INDIA |
> | 4 | WEST | 西欧日耳曼 | GERMANIC |
> | 5 | PURU | 普鲁·南亚古典 | PURU |
> | 6 | ORIE | 中东近东·阿拉伯 | ORIE |
> | 7 | MEDI | 地中海罗马 | LATIN |
> | 8 | SLAV | 东欧斯拉夫 | SLAVIC |
> | 9 | EAST | 拜占庭东欧 | EAST |
> | 10 | PERSIAN | 波斯萨珊 | PERSIAN |
> | 11 | SEAS | 东南亚 | MALAY |
> | 12 | GREEK | 希腊古典 | GREEK |
> | 13 | THRACIAN | 色雷斯古典 | THRACIAN |
> | 14 | ANDE | 安第斯 | ANDE |
> | 15 | MESO | 中美玛雅 | AMERICA |
> | 16 | AFRI | 非洲 | AFRICA |
>
> **64 支 = 16 套母体 × 4 时代**（古典 起始–400 / 封建 400–1050 / 城堡 1050–1500 / 帝国 1500–1900）。
>
> **五个系统全部按「16 套 × 4 时代 = 64 支」组织：**
> - **建筑**：16 套建筑风格 × 4 时代 = 64 套时代建筑
> - **据点**：16 母体 × 4 时代 = 64 支据点归属
> - **武将**：16 母体 × 4 时代 = 64 支武将归属
> - **军团**：16 母体 × 4 时代 = 64 支军团编制
> - **科技**：16 母体 × 4 时代 = 64 支科技门控
>
> **延伸逻辑**：第一层 64 支作为地理据点与四时代科技/攻城武器的基准底座；第二层特定历史阶段辉煌文明与独立军团在此底座上延伸派生。
>
> ### 🔴 建筑风格三级选择 · 各层管什么（2026-09-12 主人定，记死）
> 给程序（据点编辑）添加「**建筑风格三级选择**」功能时，三层职责如下：
> 1. **一级 = 城墙** —— 一级主要就是**选城墙**（16 套成套风格集）。
> 2. **二级 = 城堡** —— 二级主要就是**选城堡素材**（59 座文明专属城堡），**用于「险要」（pass）城型**。
> 3. **三级 = 自建** —— 三级是**自建的**（专属定制：独立建筑视觉 / 专属宗堡 / 特殊据点形态；现为青藏 TIBET、西域 WESTERN）。
>
> **数据落点（已核实）**：`buildingStyle`（一级母体）+ `region`（二 / 三层的 key）—— 不需要新增字段。
> 🔴 **[2026-09-12 主人补充] 军团编辑器（`legion-editor.html`）里的"三级"＝同一套建筑风格三级**：
> **一级 16**（母体/城墙）、**二级 59**（文明分支/城堡）、**三级 = 主人新建的自建档**（专属定制）。
> 军团的「另存为…」按钮属于**三级**（不是二级）；**"专属一家"这种说法不存在**（AI 曾自行脑补，被主人纠正 ✗）。
>> **实现纪律**：必须**复用页面自有的三张表**（`selStyle` 16 / `CULTURE_MAP` 59 / `LAYER3_CUSTOM_GROUPS`），
> **禁止**自造第三套选择器，禁止用项目内部文化区键冒充"二级文明分支"。
> 🔴 **[2026-09-12 主人令「记下来，别总是搞错」] 建筑风格速查（以下每条都是实测，别再用猜的）：**
> · **一级 = 16** = DE 的 16 套成套建筑母体：ASIA CEAS INDI PURU WEST ORIE MEDI SLAV EAST PERSIAN SEAS GREEK THRACIAN ANDE MESO AFRI
>   （页面 `STYLE_KEYS_16` 实测 16 个 ✓）—— **永久冻结，AI 禁止改动**（改名/改键/增删/改映射都不许）。
> · **二级 = 59** = DE 的 59 个可玩文明 = 59 座文明专属城堡（页面 `CULTURE_GROUPS` 实测 59 条 ✓ / 7 组 ✓；
>   DE dat `empires2_x2_p1.dat` 实测 60 项含 Gaia → 59 ✓）。
> · **三级 = 2** = 主人自建（`LAYER3_CUSTOM_GROUPS`：TIBET / WESTERN ✓）。
> · **两套键别混**（这是 AI 反复搞错的地方）：页面分支键（59+2）**≠** `RegionType`。
>   军团名表 `CULTURE_LEGION_NAMES` 与编制表 `CULTURE_TIERS_MAP` 都是 `Record<RegionType, …>` ✓；
>   59 个二级键里 **52 个已在 `RegionType` 里** ✓、**7 个不在** ✗：
>   **WEI / BASHU / ROMA / ATHENIANS / SPARTANS / MONGOL / INCA**。
> · 现成军团名共 **137 个、无重名** ✓（新建军团前必须先查重，见「一个军团只能有一个名称」铁律）。
> 🔴 **[2026-09-12 主人定] 59 个二级建筑风格 = DE 本体的 59 座文明专属城堡**（不是项目自造）：
> 已实测（`scratch/_verify59castle.cjs`）：59 个二级分支的 `castle` 值**逐个都能在 DE 素材目录
> `public/assets/de_buildings_catalog.json` 里找到**（59/59 ✓，无重复 ✓）。
> **一级 16 = DE 的 16 套成套建筑母体** ✓；**二级 59 = DE 的 59 座文明专属城堡** ✓。
> 另（实测 DE dat：`empires2_x2_p1.dat` 60 项含 Gaia → **可玩文明 59** ✓）：
> **DE 本体确实有** `Wei`/`Shu`/`Wu`/`Athenians`/`Spartans`/`Macedonians`/`Khitans`/`Jurchens`/
> `Achaemenids`/`Puru`/`Muisca`/`Mapuche`/`Tupi` 等文明 ✓ —— **是项目还没给它们建军团** ✗，
> 不是"DE 没有" ✗（AI 曾误判，被主人纠正）。
> ### 三之二、建筑风格 = 一层 16 + 二层 59（2026-09-11 主人定）
> - **一层 16**：16 套基础建筑风格 = 16 个文化母体（见上表），对齐 DE 本体实测的 16 套建筑素材（`b_*_` 前缀，城墙 16/16、箭塔 16/16 全覆盖）。
> - **二层 59**：DE 本体 `civilization_list` 实测 **59 个可玩文明**（另有 Gaia 为自然/中立，非文明），按民族史实归入这 16 个文化。详细归位表见 [`docs/02-design/four-eras-civilizations.md`](docs/02-design/four-eras-civilizations.md) §三之二。
> - **归位原则**：DE 的 `hud_style` 界面分组仅作骨架参考，**以民族/史实为准**（维京归日耳曼、突厥归草原、柏柏尔归非洲、越南归东南亚、达罗毗荼/孟加拉/瞿折罗归印度、亚美尼亚/格鲁吉亚归拜占庭、波斯归波斯、马其顿归希腊）。
> - **注**：马其顿（CivMacedonian）在 DE 素材层**只有城堡+指挥所，城墙 0、箭塔 0**，无成套城镇建筑，故不单列为建筑风格母体，归入 GREEK。
>
> ### 四、第二层：各时代最辉煌巅峰文明归位定案（在此 64 支第一层上延伸）
> 1. **古典时代（起始 – 公元 400 年）最辉煌文明（23个）**：
>    - 古典华夏（周秦汉）、古典罗马（共和国至帝国五贤帝）、古典希腊（伯里克利与亚历山大）、古典波斯（阿契美尼德万王之王）、古典巴比伦（汉谟拉比法典与两河）、古典埃及（法老帝国）、古典布匿（汉尼拔布匿军团）、古典赫梯（卡迭石战车）、古典匈奴（冒顿单于）、古典鲜卑（拓跋嘎仙洞祖源）、古典斯基泰（草原黄金骑射）、古典凯尔特（拉坦诺铁器）、古典日耳曼（条顿堡森林）、古典印度（阿育王孔雀帝国）、古典月氏（大月氏迦腻色迦）、古典努比亚（麦罗埃黑法老）、古典古蜀（三星堆金沙）、古典百越（稻作铜鼓）、古典古滇（滇池青铜战象）、古典塞种（伊犁河塔里木塞克人）、古典玛雅（蒂卡尔金字塔）、古典色雷斯（黄金骑士）、古典亚美尼亚（提格兰大帝）。
> 2. **封建时代（公元 400 – 1050 年）最辉煌文明**：
>    - 封建华夏（贞观之治开元盛世大唐天可汗）、封建阿拉伯（四大哈里发与阿拔斯黄金时代）、封建吐蕃（松赞干布赤松德赞藏甲）、封建高句丽（好太王萨水开马武士）、封建突厥（第一/第二汗国木杆可汗苍狼霸权）、封建契丹（耶律阿保机大辽建国收燕云皮室军具装铁骑）、封建回鹘（漠北回鹘汗国平定安史之乱金镞角弓）、封建靺鞨（海东盛国渤海鹿角硬弓雪原山城）、封建法兰克（查理曼大帝加冕统一西欧）、封建维京（公元793-1066长船海盗黄金时代）、封建拜占庭（查士丁尼马其顿中兴希腊火）、封建盎格鲁-撒克逊（日耳曼七国争霸阿尔弗雷德双手大斧盾墙）、封建南诏（天宝大捷六诏归一斩马刀）、封建保加利亚（第一保加利亚帝国西蒙大帝第一沙皇国）、封建室利佛逝（马六甲海峡海上佛教贸易帝国）、封建萨珊（霍斯劳一世具装铁骑Savaran）、封建罗斯（留里克基辅罗斯弗拉基米尔受洗）、封建阿瓦尔（潘诺尼亚双马镫重装骑射鼻祖）、封建加纳（西非最早黄金之国索宁克长矛方阵）、封建可萨（高加索里海拉尔西亚近卫铁骑）、封建哥特（亚德里安堡东哥特王国）、封建匈人（阿提拉上帝之鞭沙隆会战）、封建柏柏尔（塔里克跨海征服伊比利亚）、封建埃塞俄比亚（阿克苏姆红海贸易）、封建瞿折罗（普拉蒂哈拉三帝国争霸）、封建孟加拉（帕拉帝国超戒寺达摩波罗）、封建达罗毗荼（朱罗帝国罗阇罗阇跨海远征）、封建格鲁吉亚（巴格拉季昂王朝统一）、封建瓦拉几亚（多瑙河达契亚罗马化公国）。
> 3. **城堡时代（公元 1050 – 1500 年）最辉煌文明**：
>    - 城堡蒙古（成吉思汗到忽必烈大元与四大汗国，冷兵器版图极值）、城堡高丽（开京罗城千里长城高丽战车抗蒙）、城堡女真（大金铁浮屠正面撼阵灭辽灭北宋）、城堡高棉（苏利耶跋摩二世吴哥窟神王水利帝国）、城堡缅甸（阿奴律陀蒲甘万塔王朝）、城堡条顿（巴巴罗萨与条顿骑士团马尔堡世界最大要塞）、城堡马里（曼萨穆萨黄金之国廷巴克图大学）、城堡阿兹特克（特诺奇提特兰神圣浮岛帝国）、城堡印加（萨克赛瓦曼巨石要塞太阳神帝国）、城堡十字军（圣殿骑士与黎凡特绝壁骑士堡）、城堡马扎尔（匈牙利黑军常备军）、城堡波希米亚（查理四世黄金布拉格与胡斯移动战车阵）。
> 4. **帝国时代（公元 1500 – 1900 年）最辉煌文明**：
>    - 帝国不列颠（击败无敌舰队、工业革命维多利亚日不落大英帝国）、帝国西班牙（查理五世腓力二世日不落大方阵无敌舰队美洲白银帝国）、帝国法兰西（太阳王路易十四凡尔赛全盛与拿破仑横扫欧陆）、帝国俄罗斯（彼得大帝叶卡捷琳娜二世沙皇射击军哥萨克骑兵）、帝国奥斯曼（苏莱曼大帝乌尔班攻城重炮耶尼切里亲兵）、帝国莫卧儿（阿克巴大帝泰姬陵统一印度）、帝国朝鲜（李舜臣全装铁甲龟船大破日舰）、帝国日本（战国三段击铁炮大决战、江户两百五十年天下泰平至明治维新）、帝国德意志/普鲁士（腓特烈大帝与俾斯麦铁血统一）、帝国波兰-立陶宛（维也纳之战翼骑兵救世冲锋）、帝国葡萄牙（达伽马大航海东方商路帝国）、帝国荷兰（十七世纪海上马车夫）、帝国瑞典（古斯塔夫北方雄狮战术）、帝国大越（阮朝统一建立顺化皇城）、帝国华夏（康乾盛世一千三百多万平方公里大一统版图）。

## ⚠️ 势力军团编制与七阵铁律（2026-08-20 主人定稿）

> ### 一、九人编成七阵体系（前-中-后）
> 🔴 [2026-09-23 主人定] 编制仍是下面的 9 人三排；战略、战术**一律另加主将队 1 队 = 10 队**，主将队站前排正中再往前（见本文件「历史剧本事件标准」二·5）。
> 1. **方阵（方形阵）**：`3 - 3 - 3`（九宫方正，攻守均衡）
> 2. **雁行阵**：`4 - 3 - 2`（前4/中3/后2，前阔后窄，前排重装推进）
> 3. **鱼鳞阵**：`3 - 4 - 2`（前3/中4/后2，中腰厚实，中坚重拳突破）
> 4. **鹤翼阵**：`2 - 4 - 3`（前2/中4/后3，两翼展开，中坚合围包抄）
> 5. **锥形阵**：`2 - 3 - 4`（前2/中3/后4，前尖后宽，后劲冲锋/远程集火）
> 6. **偃月阵**：`3 - 2 - 4`（前3/中2/后4，前阻中虚后重，后发制人）
> 7. **衡轭阵**：`4 - 2 - 3`（前4/中2/后3，前宽后稳，前线硬碰硬）
>
> ### 二、布阵档位与战力均衡（2026-09-07 主人重定，取代原「4 档主力铁律」）

> 🔴 **[2026-09-07 主人定]「4 档必须是重装/精锐/高级」这条铁律已废除。**
> 原话：**「4 档无所谓什么兵。关键是战力要平均。」**
> 4 档放白板兵、放精锐、放什么都行，不再是判定项；**唯一的判定项是军团战力是否均衡**。
> （同日一并废除的还有「象/火器/攻城只能占 2 档」——任何兵种都可占 2/3/4 档。）
>
> #### 唯一标准：战力均衡
> 口径 = `src/data/CombatPower.ts` 的 `getLegionPower()`：按格位人数加权平均，全表中位兵 = 100。
> 验收脚本 **`npm run legion:power-balance`**，打印全表分布、均值、标准差与离群军团。
> - **带内**：均值 ± 1.5 个标准差以内；离群的往中间收。
> - **别拿单支军团的绝对值说事**，看它在全表分布里的位置。
> - 收拢手段按优先级：① 换掉与史料冲突的兵；② 基础档 ↔ 升级档互换；③ 调整哪一格占 4 档。
>   **不许**为了凑数字放一个与该民族无关的兵（史实仍是硬约束，见本文件「兵种选用看素材样貌」）。
>
> #### 档位梯队（建议，不再是红线）
> 4 档 = 该军团出场最多的那种兵（谁是主力谁占）；3 档 = 骨干；2 档 = 支援/特种。

## 🔴🔴🔴 兵种选用看**素材样貌**，不看名字、不看年代标签（2026-09-07 主人定，最高准则）

> **主人原话：「你不要只看名字，要看素材样貌，库曼钦察弓骑的样貌和东北人很像。其他皆是如此。」**
>
> ### 一、判据只有一条：把素材调出来看，长得像不像这个民族这个时代
> - 兵种 **id / 中文名里的民族词、政权词、年代词一律不作数**。名字叫「库曼钦察弓骑」，
>   样貌是东北亚游牧骑射手 —— 它就能进【古典时代鲜卑军团】，不是穿越。
> - 反过来也成立：`scout_cavalry`「斥候骑兵」在编辑器里挂着 `age: 'antiquity'`，
>   但素材是**戴盔穿锁子甲、持剑的中世纪骑手**，塞进古典游牧军团就是错的。
>   → **`src/legion-editor/main.ts` 的 `age` 字段也只是参考，不是权威。**
> - AoE2 DE 的素材是按「文明 + 科技档」做的，同一副模型换个涂装就改了名字，
>   所以名字与年代标签都会骗人，**只有像素本身不会骗人**。
>
> ### 二、怎么看（AI 必须真的看过再下结论，不许凭名字断言）
> 素材在 `public/SUCAI/<目录>/{动作}_{方向}.png`，是**横向雪碧图**（图宽 = fw × 帧数，
> fw 在同目录 `_meta.json` 里按动作分方向记）。裁第一帧放大成对照图：
> ```
> py scratch/tools_sprite_sheet.py <输出png> <目录1> <目录2> ...
> ```
> 然后**把图读出来看**。没看过图就说「这个兵种不符合」= 违规。
>
> ### 三、战船同理
> 楼船（`LOU_CHUAN`）挂着 `imperial` 标签，但样貌是红漆多层甲板中式巨舰，
> 华夏系古典军团就该用它；`ANT_GALLEY` 系列虽叫「古典桨帆船」，样貌是地中海式桨帆船，
> 配到古蜀/百越/秦汉身上是**希腊船开进四川**，比年代对不上更离谱。
>
> 血训：2026-09-07 AI 按名字与 `age` 标签判「穿越」，把古典时代鲜卑军团的库曼钦察弓骑换掉、
> 把华夏四个古典军团的楼船/龙首战船换成地中海桨帆船 —— 全是错的，当场被主人揪回。

## 🔴🔴🔴 别看注释（2026-08-20 主人第 N 次强调，原话「说了八百次了」）

> **代码注释全是历代 AI 写的，会写错，一律不许当依据。**
>
> - 判断「当前行为是什么」→ **读函数体、读调用点**，不许引注释。
> - 判断「帝国时代2 原版是什么样」→ **查 DE 本体**
>   （`C:\Program Files (x86)\Steam\steamapps\common\AoE2DE\`，dat 数据 / `drs\graphics` 素材），
>   或直接以**主人在游戏里看到的**为准。不许凭记忆断言，更不许拿注释回嘴。
> - 判断「规则该怎样」→ 本文件（AGENTS.md）。
>
> 血训：高丽战车弹丸，AI 引用注释里「射箭是对的，不动」回绝主人，被当场戳穿
> 「你看帝国时代2的代码了吗，就在这胡说八道」——那条注释本身就是 AI 自己瞎写的。

## ⚠️ 「军团」与「精锐」是两回事 · 命名铁律（2026-08-20 主人定稿，2026-09-08 主人重申定死）

> **军团** = 前排 / 中坚 / 后排**三排组成的那支部队**（9 人编成 = 阵型 + 三排兵种），
> 命名规范唯一为：**「时代时代 + 文化 + 军团」**（如「古典时代先秦军团」「古典时代秦汉军团」「封建时代高句丽军团」「城堡时代蒙古军团」「城堡时代条顿军团」）。
> 核心获取函数唯一为 `getCultureLegionName(region)`（或 `FACTION_COMPOSITIONS.legionName` 独立编成名）。
>
> **精锐** = **番号**，如「福建水师」「北府兵」「耀州牙兵」「商州锐士」，数据在 `src/data/*ExpeditionLegions.ts`，
> 按 factionId / cityId 查，带 tier 0~4，**只显示在战力倍率词语标签（MultiplierBadges）里**。
>
> ### 命名铁律（永世铭记）
> - **到底什么是军团？命名方式为「时代时代 + 文化 + 军团」的才是军团！**
> - **绝对禁止**拿「势力名+军团」（如「耀州军团」「商州军团」）胡编乱造！全游戏根本没有这种东西！
> - **绝对禁止**拿「精锐番号」（如「耀州牙兵」「商州锐士」）或「据点城名」（如「商邑」）当军团名！
> - **精锐是显示在标签里的，名字下面显示的是军团，二者严禁混淆！**
> - 看到不带「军团」后缀的名字，它就不是军团名；看到带「军团」后缀的，它就不是精锐番号。
>
> 🔴 两个词严禁混用，任何面板展示必须严格遵循此定义。

## ⚠️ 武将技数据修改铁律（2026-07-14 血训，绝对红线）

> **禁止批量修改武将技的 baseEffect / condition / phase / magnitude / series。**
> 每一条技能的六类归属必须由 AI 根据该技能的**技名含义 + sourceQuote 史料出处**独立判断，逐条审理。
> **严禁**使用关键词启发式、正则批量替换、自动分类器等方式"一把梭"修改数据。
> 违者后果：主人已因批量修改导致的数据损坏连续三天无法合眼。此规则为项目最高优先级红线。

## ⚠️ 武将六槽铁律（游戏规矩，不可动摇）

> **以下两条是游戏引擎硬依赖，违反会导致战斗武将技不触发或技能池断裂。**

### 铁律一：选池 = 不在册通用池（战术技能全部随机，无专属）（2026-08-04 主人定稿，2026-09-12 主人再定「无专属」）

> **战斗选技不再读六槽。** 每个武将的候选池 = 【当前势】对应六计的全部**不在册通用技**，池内**等概率抽一技**，攻方守方同池（`_isAttacker` 作废，攻守数值差由第四层攻防环负责）。
>
> 🔴 [2026-09-12 主人定] **战术技能全部随机，没有专属**——不存在「本人专属在册技 / `ownerGeneralId` 归属」这回事；所有武将一律从通用池随机抽技，**禁止**给任何武将「定专属战术技」或「起典故契合的四字专属技」。

**三势选池（四/四/六不变）**：

| 势 | 池 = 不在册通用池 |
|----|-------------------|
| **优势** | 攻战/胜战/敌战/混战 四计全部不在册技（**不含劣势组** → 优势方摸不到败战翻盘计） |
| **劣势** | 并战/败战/敌战/混战 四计全部不在册技（不含优势组） |
| **均势** | 全六计全部不在册技 |

- **悬念铁律不变**：只有败战计能反转胜负，而败战计只在劣势池 → **只有劣势方摸得到翻盘钥匙**。实现见 `resolveSituationalSkillId` / `getSituationalSkillPool`。
- **六计出现率 = 池大小比例**：胜战/败战池小（18/14 个），出现率低，属设计意图。

**六槽字段（atk*/def*）已废弃**：不再驱动选技，仅保留数据兼容（six-slot.html / batch-manager 工具仍可维护，后续清理）。判据唯一权威不变：`src/data/TacticalSkillCatalog.ts` 的 `EFFECT_TO_SIX_SET`（只按 `baseEffect` 查表；**禁止按 condition 改判六计**）。

### 铁律一附则：攻守六计硬分开（2026-08-05 主人定）

> **同一场战斗里，先后挑技的带将单位不得撞同一个六计类别。**一边已是败战，另一边就不许再败战；胜战等同理。

- 实装：`BattleField.assignSituationalSkills` + `pickSkillAvoidingUsedSixClass`。全场带将**依次**挑技，后挑者避开已用技能 id **和**已用六计类别；先放侧 = 劣势方（均势随机），与技能脉冲亮相同锁。
- 援军中途入场走同一套避让（`joinReinforcement` 内），不是两套逻辑。
- 池大（150–192），撞车兜底几乎不触发；真触发会打 `⚠️ [六计] …只能与已出场计撞车` 警告。
- 验证：`scratch/verify_six_class_separation.mts`（5000 次 ×3 势，冲突 0%）。

**⚠️ 分配层错开 ≠ 战斗面板两枚角标必不同字。**夺取系（混战计）夺来敌技后，若角标跟着夺来技走，攻守双方必然显示同一个字（夺谁的就显示谁的类别）——那是角标口径问题，不是错开逻辑坏了。故角标专走 `getOwnSixSetSkillId`（跳过 `stolenSkillId`），与引擎结算用的 `getActiveTacticalSkillId` **故意不同源，勿合并**。角标只回答「本方出的哪一计」。

**六计机制与参数定稿（2026-07-19 主人拍板；2026-09-12 主人再定「战术技能全部随机，无专属」）**：

| 计 | 机制 | 在册 | 不在册 |
|----|------|------|--------|
| 攻战·机 | 加己攻（战力乘区） | 1.2 | 1.1 |
| 胜战·全 | 攻减敌兵 / 守加己兵 | 18% | 9% |
| 敌战·衡 | 减己损 + 输了咬 | 0.2 | 0.1 |
| 混战·乱 | 克夺反（否敌技/夺技/反弹/否地形） | 全否地形 | 半否地形 |
| 并战·借 | 变随机（改运气区间，劣势制造混乱求翻盘） | [0.6,1.4] | [0.7,1.3] |
| 败战·险 | 更翻盘（**六计中唯一能反转胜负**） | [0.25,0.45] | [0.30,0.40] |

> **[2026-08-06 勘误]** 上表原先把 **敌战 / 并战 两行的机制写反了**（敌战写成「变随机」、并战写成「减己损」），
> 并写了一个**代码里根本不存在的机制**「并战计拖长一档（30s→45s 等援军）」。已按代码订正。
> 依据：`EFFECT_TO_SIX_SET`（AGENTS 自己声明的唯一权威）+ `SpeechAnnouncer.EFF_SUIJI/EFF_JIANJI` +
> `skill-editor` 三处独立口径完全一致 —— `luck_variance_*` → 并战、`win_casualty_reduction` 等 → 敌战。
> 也与 `SIX_SET_TO_TRI_CLASS` 自洽（并战属劣势，劣势方才要放大方差搏翻盘）。
> 关于「45s」：全项目**没有 45 秒这一档**。实装时长 = 双将 10–30s（按兵力比）+ 每只援军 +10s、封顶 60s；
> 非双将固定 9s（`BATTLE_DURATION_*`）。且**没有任何战术技能改战斗时长**，加时只由援军数决定。
> 若当初的设计意图与此相反（即代码错、文档对），那是引擎 bug 不是文档 bug —— 请主人明示后再改代码。

- 并战计不改胜负，收益在地图层（残兵多→连战；拖时间→援军圈 0.2s 轮询编入）。
- `first_sortie_comeback_mult` 已废弃（矛盾技：优势格却只在劣势触发），15 条已全迁 `recompute_comeback`。
- `ally_add_troops_opening`（守加己兵）归胜战计；现无技能使用（幽灵映射，新增技能时可用）。

### 铁律零：战力环 [0.8, 1.2] 与九环战力体系（一切战力数值的底座，2026-07-22 主人立，2026-09-05 校准）

> **一场战斗的胜负不看兵力比，看「九环乘完的有效战力比」。有效战力 = 兵力（基数）× 九环连乘。**
> **⚠️ 警示铁律：九环全部是战力加成系数乘区，九环中绝对没有「兵力基数」！兵力是兵力，九环是九环。**

- **九环战力乘区定义**：
  1. **名将光环** `FAMOUS_GENERAL_MULT`：名将 1.30 / 普将 1.00（底层特权光环，**不设词语标签**，2026-09-05 主人定删除名将标签）
  2. **据点城池** `siteMult`：关隘/名城地利 1.10~1.20（场地建筑属性，走下方乘区链二字词，**不设顶部词语标签**）
  3. **文化加成** `cultureMult`：攻守文化区战力修正 0.80~1.20（**词语标签 ①**，如【习于行阵】/【侵略如火】）
  4. **战术技能** `tacMult`：战术技战力倍率 0.80~1.20（**词语标签 ②**，如【死地后生】）
  5. **精锐部队** `legionMult`：部队番号阶梯品质 1.10~1.50（**词语标签 ③**，如【北府兵】）
  6. **三势适性** `APTITUDE_POWER_MULT`：将势与局势契合 0.80~1.20（**词语标签 ④**，如【见机行事】/【因势乘便】）
  7. **攻防风格** `ATTACK_STYLE_POWER_MULT`：攻守角色本位乘数 0.80~1.20（**词语标签 ⑤**，如【攻守兼资】/【锐不可当】）
  8. **命运运气** `fateLuck`：开局战局随机浮动 0.80~1.20（**词语标签 ⑥**，如【天道不顺】/【天命所归】）
  9. **玩家官阶** `playerHostPowerMult`：玩家入伍军团专属 1.10~1.50（**词语标签 ⑦**，如【风行斥候】/【百战将军】）

- **🔴 词语标签排他铁律**：
  - 战斗面板血槽上方的词语标签**严格只显示九环战力中的上述 7 个固有属性环**；
  - 名将光环（底座特权）与据点城池（场地建筑）严禁作为顶部词语标签显示，援军合力属于第三行援军明细，严禁挤占主将标签栏。

- **有效战力 = 兵力 × 九环连乘**。局势判定、时长判定全用它（`BattleField.effectivePowerRatio` / `resolveSituationKind`），**绝不用兵力比**——1:1 兵力可能是碾压，2:1 兵力可能是死斗。
- **代码里已定死的方向环**（都在 `src/combat/TacticalConstants.ts`）：
  - 三势适性 `APTITUDE_POWER_MULT`：本命 1.20 / 次适 1.10 / 错位 1.00（错位不扣，3×3 行列各 3.30 完美平衡）
  - 攻防风格 `ATTACK_STYLE_POWER_MULT`：专精本位 1.20 / 双行 1.20（攻守均 1.20）/ 错位 1.00（错位不扣）
  - 名将光环 `FAMOUS_GENERAL_MULT`：名将 1.30 / 普将 1.00
  - **武将技·攻战计（战力乘区）**：加己 = 在册 1.2 / 不在册 1.1；守减敌 = 0.8（同带下沿）
- **🔴 红线**：任何「加/减战力」的技能数值**必须落在 [0.8, 1.2]**。它只是众环之一，**1.2 就是满值、不是「过强」**——严禁因为「×1.2 单独算胜率偏高」就往下压：那是把一环误当终值，多环叠乘时一个 1.2 环只是正常贡献。加己满值恒 1.2。
- **唯一例外 = 运气 / 敌战计「变随机」环**：均值锁死 1（`luckMin + luckMax = 2`），因**不移动均值**，区间可超出 [0.8,1.2] 以加大戏剧性（敌战计 在册 [0.6,1.4] / 不在册 [0.7,1.3]）。判据：**移均值的方向环 → 守 [0.8,1.2]；不移均值的随机环 → 可更宽**。

### 铁律二：技必有主（每个武将技必须有人使用）

> **不允许有「孤儿技能」——每个战术技/战略技必须至少有一名武将使用。**

| 类型 | 范围 | 校验 |
|------|------|------|
| **战术技**（ts_*，全部随机，无专属） | 一律进通用池 → 全体武将随机可用 | 无「孤儿」概念（不再有 ownerGeneralId 归属） |
| **可分配战略技** | **随机池驱动**（2026-08-03 主人定）：13 个活跃技全部在 `ACTIVE_STRATEGIC_SKILL_POOL`，名将挂将随机分配 + 攻城胜后随机换，必然被使用；**无档案佩戴概念** | `str_11` 长驱深入（远征）除外；退役/封印技（str_05/17/18/21/25/26/27/28）不参与 |

- 🔴 [2026-09-12 主人定] 战术技能全部随机、无专属——不再有「在册技 / ownerGeneralId 归属 / 孤儿技能」概念，下述校验命令中「检查技能覆盖 / 孤儿报错」随之作废。
- **校验命令**：batch-manager → 「检查技能覆盖」；或 `npm run build` 触发的完整校验规则 11.8（六槽兼容期仍按六槽查）。

### 违规后果

- 🔴 [2026-09-12 主人定「战术技能全部随机，无专属」] 原「专属技 `ownerGeneralId` 指向已删武将 → 孤儿」「在册技误删 `ownerGeneralId` → 降级通用」两条违规后果随之作废——不再有专属归属概念。

# MAPWAR / 历史大乱斗 — AI 协作约定

> **必读**：本项目欢迎 AI 协作，但任何 AI（无论模型/版本/工具链）必须遵守以下规范。
> 违反这些规范的修改会直接破坏项目的历史严肃性和游戏机制，必须 revert。

> **⚠️ 绝对禁止使用 GIT 回滚（极其重要，全优先执行）⚠️**
> **严禁任何 AI 在终端中使用 `git checkout`、`git restore`、`git clean` 或 `git reset` 等任何具有覆盖/回滚工作区文件破坏性效果的 Git 命令！**
> 玩家的 Git 处于不可用或极其陈旧的备份状态，本地保存着大量长达数天心血的未提交（uncommitted）修改。
> 任何试图使用 Git 命令进行回滚、恢复的操作，都会导致玩家几天的手工心血瞬间白费！
> 如果 AI 修改代码失误，必须通过**精确的脚本重写或手动修改**来修复，**绝不允许试图用 Git 回滚**。违反此规则 = 销毁用户心血，是本项目的绝对红线！

> **⚠️ 绝对禁止访问任何备份/历史文件（极其重要，全优先执行）⚠️**
> **2026-06-17 血的教训：AI 使用 VSCode History 旧版本覆盖 cities_v2.ts，导致 10 天心血全部丢失。**
> **严禁任何 AI 访问、读取、打开、查看以下任何目录或文件：**
> - `C:\Users\*\AppData\Roaming\Code\User\History\` （VSCode 本地历史）
> - 任何 `.bak`、`.backup`、`.old`、`.orig`、`~` 后缀的备份文件
> - 任何编辑器/IDE 的自动保存、历史快照、撤销记录目录
> - 任何形式的"旧版本文件"，无论以什么名义存在
>
> **连读取都不许。不是"不许用来恢复"——是连打开、连看都不许。看了就会手贱去用。**
> **AI 修改代码失误导致文件损坏/数据丢失时，唯一正确做法：立刻停下，告诉玩家"我把文件写坏了"，等玩家指示。不许偷偷找旧版本试图掩盖。**
> 违反此规则 = 销毁用户心血，是本项目的绝对红线！

## 📍 规则地图（外置细则索引，2026-08-07）

> 本文件（AGENTS.md）保留**铁律 + 各主题摘要**；**长细则已外置**。文中任何「见 §4.x / §12.x / 第十节」编号引用，编号与原文一致，均在对应外置文件中——**按表取用，勿在本文档内寻找已外置的细则原文**：

| 主题 | 外置文件 | 内容 |
|------|---------|------|
| **四、势力名称定名**（§4.0–4.8） | `docs/AGENTS/faction-naming.md` | 定名算法、五级优先级、旗号格式、禁字红线、防重 |
| **🔴🔴🔴 严禁擅自新建/添加（最高铁律）** | `docs/AGENTS/no-arbitrary-additions.md` | **主人原话逐条 + 禁止清单 + 2026-09-19 全部血训案例 + 动手前自检五问**；只做主人点名的那一处 |
| **五、5 文件一致性 + 5.5 骨架优先**（§5.4–5.5） | `docs/AGENTS/five-file-sync.md` | 5 文件清单、首都映射铁律、录入验收、势力 A/B/C/D 分级 |
| **十、地图旗号与势力色块**（10.1–10.5） | `docs/AGENTS/flag-rendering.md` | 领土色块、叛军旗 52 面、黑白字阈值、启动性能 |
| **十二、远征精锐**（§12.1–12.4） | `docs/AGENTS/expedition-elite.md` | 三者防重、tier 五级判定、录入/审计命令 |
| **十三、战斗立绘分配**（13.1–13.6） | `docs/AGENTS/portrait-allocation.md` | 三级优先级、素材目录、运行时分配、禁止清单 |
| **🔴 玩家系统 · 主人原话** | `docs/AGENTS/player-rules-verbatim.md` | **动玩家系统前先对这张表**：主人原话逐条，表里没有的就是没定；AI 不许自补默认值/边界/「更合理」的约束，实现上绕不开的选择必须单列声明 |
| **玩家乱入者 · 兵模奖励** | `docs/AGENTS/player-hero.md` | 🔴 **这是一个获得兵模的游戏**：加入军团即随机得该军团三排之一并换上、官阶配额 1/2/3、奖励终身不回收；13 里只加频率/血/防不加攻击力，玩家永不真死只落马重整 |
| **ZOOM13 战场底图与植被** | `docs/02-design/climate-regions.md` | 26 张底图的气候判据、树种/密度分配、攻城战与野战的真实性规则、验收脚本 |
| **卡顿诊断（PerfDoctor）** | `docs/03-runtime/perf-doctor.md` | 🔴 **改任何缓存/热路径前必读**：Shift+F3 出结构化诊断、七条规则、五条铁律、已踩死路清单 |
| **卡顿案例集** | `docs/03-runtime/lag-casebook.md` | 🔴 **查卡顿先翻这本**：八条案例（开机预载/panBy取整/河流重投影/植被层/13水面离屏画布/探针挂错分支/战略行军长任务·进行中）附前后数据与验收脚本，以及**我一开始猜错的方向**和「不是瓶颈」清单 |
| **🔴 历史战役与战场模式（野战 + 攻城战双规范）** | `docs/02-design/systems/历史进程框架.md` | **动战役 / 战场前必读**：主人最高铁律（严格符合历史，该攻城就攻城该野战就野战；坐标不要搞错；攻城战改据点归属；按 scriptYear 显示与触发；战后点亮遗址；双方主帅班师）、§15.10（野战+攻城战双样本、两处落盘代码、必填清单、验收命令、前330/前329添加指引） |


---

## 零、AI 操作铁律（每次动手前执行）

**改数据前，三步缺一不可**：

| 步骤 | 操作 | 目的 |
|------|------|------|
| 1. 看格式 | head/sed -n 抽查目标区域 3-5 条 | 确认多行还是同行、字段顺序 |
| 2. 数命中 | 替换前先 grep -c 目标模式 | 与预期数对比，防止全局误伤 |
| 3. 用锚点 | city_id/note/factionId 组合限定 | 绝不单靠 type: 或 troops: 定位 |
| 4. 查间距 | **新建/改坐标前 `--probe`**；**改后 `city:spacing`** | 新建及全图任意两城均须 **≥50 km**（§2.1.1） |

禁止：不看完格式就写 sed；不确定命中数就执行替换；用通用值做全局替换；**不跑 `--probe` / `city:spacing` 就改 cities_v2**；**未经主人同意新增 city_* 条目**。

**审核自主处理**：审核据点-势力-武将-精锐相关性时，发现**武将错位、精锐缺失、势力名不匹配**等非结构性错误 → 直接修正，不用问。**任何据点（含 C 级小城/关隘/边陲部族）都必须满配 势力+武将+精锐，一律不许留空武将/精锐**；无现成记录就按该据点史地身份/名气/相关性选配一个（编一个），宁松勿缺。**但禁止为跑审计而乱挂毫无史地相关性的将/精。**
**须问的情况**：旗号冲突（两面旗严格相等）、坐标改动、删城/删势力、新增 city_* 条目。

### 铁律：结论必须有实测依据（2026-07-27 主人立，血训）

**说出口的每个数字、每个因果，要么是刚跑出来的，要么标明"这是猜的"。禁止把推测讲成结论。**

同一次会话里 AI 连续犯了五次，全部被主人当场戳穿：

| 错误结论 | 实际 | 错在哪 |
|---|---|---|
| "城池 marker 只增不减，堆到上千，缩放才卡" | 实测 **11 个** | 读了"只追加不删除"的代码就脑补了累积量，没数 |
| "updateTerritoryStyle 每次跨档跑上千次 setStyle" | zoom≥9 根本不建多边形，只有几十条边框线 | grep 的 `-B3` 上下文跨条目串味，当成了真实分布 |
| "删掉的死代码等于一个战术技丢了效果" | 该 baseEffect **零个技能使用** | 没查有多少技能真的挂着它 |
| "行军减兵对远征军比普通军团狠得多" | 机制对所有军团**完全相同**，无任何远征加重 | 脑补了"远征军路上没有己方城"，没查目标距离也没查沿途城密度 |
| "连续断粮 1 年剩 52%、2 年剩 27%" | 每场战斗结束即重置、打下城即在城内复位，活动军团攒不到几跳 | 复利算术没错，但前提（60 秒不打仗不路过己方城）现实中不存在 |
| "野战比攻城频繁得多"（2026-07-31） | **实机 90% 是攻城战**，野战很少 | 读了 `LegionBehaviors` 里"先扫附近敌军团（野战追击）→ 无则选据点"的注释，把**优先级顺序**当成了**发生频率**。追击半径只有 `HUNT_ENEMY_LEGION_RADIUS = 0.8°`（≈89km），圈内多数时候没有敌军团，于是绝大多数回合都走攻城链 |

### 战斗构成：90% 是攻城战（主人 2026-07-31 口述定调）

> **实机跑下来，约 90% 的战斗是攻城战，野战很少。** 凡涉及"战斗发生频率"的判断、
> 平衡设计、触发时机取舍，一律以此为准。

**别再被这两处注释误导**（它们本身没写错，写的是**优先级**不是**频率**）：

| 位置 | 原文 | 正确读法 |
|---|---|---|
| `src/ai/bt/LegionBehaviors.ts` 文件头 / `HasTarget` / `FindTarget`（**2026-08-06 改**：原写的 `SelectAttackTarget` 这个节点**根本不存在**，别去找了） | "先扫附近敌军团（野战追击）→ 无则推进锚点方向池抽签" | 只说明**排序**：有敌军团在 0.8°(≈89km) 内才追击。圈内多数时候是空的，所以实际绝大多数回合直接走攻城链。（"近 3 敌城"是旧说法，2026-08-05 起改为**方向池**——每条直连道路各取该方向最近 1 座；"最近 3 座"只是无方向池时的兜底） |
| `src/ai/bt/BehaviorTree.ts` → `strategicTargetArmyId` | "有值时优先野战追击，不走攻城链" | 同上，讲的是黑板有追击目标时的分支，不代表这个分支常被走到。（字段名是 `strategicTargetArmyId`，**没有** `huntTargetId` 这个字段） |

**执行要求**：

1. **数量级问题一律跑脚本实测**，不靠 grep 估算、不靠读代码推演。`npx tsx --import ./tools/sim-preload.mjs <脚本>` 可直接加载游戏模块（不加 preload 会因 Vite 虚拟模块报错）。
2. **`curl` 返回 200 ≠ 编译通过**。Vite 只转译不做类型检查；判断代码是否干净必须跑 `npx tsc --noEmit`。曾靠 200 当绿灯，漏掉自己引入的 6 个类型错。
3. **算例要标清前提**，并先自问"这个前提在真实运行中会发生吗"。前提不成立的算例比不给更糟。
4. **注释和文档不是证据**（本仓库注释多为 AI 所写，历史上多次与代码相悖）。判断行为读函数体，判断数据跑统计。
5. **🔴 AI 看不到浏览器 —— 视觉/UI 改动必须写「离线复现脚本」自检后才能交付**（2026-09-12 主人定）。
   自己写的静态页面（如 `public/_citytest.html`）改完**不许**只说"应该能看到了"就交差；
   必须用一个脚本把**同一套布局数学**在离线重算一遍、合成出图、**自己读图确认**（不裁切 / 居中 / 读得清），
   再连同实测数字一起汇报。范例：`scratch/proof_battlefield_page1.py`。
   （血训：2026-09-12 战场据点样式做完只写进了数据文件，页面上根本选不到，被主人连问两次「样式在哪呢」「程序里没有呀」。）

---

## 一、项目本质

**这是一份历史教材，不是娱乐游戏。**

- 所有据点坐标必须**真实可考**
- 所有势力名称必须有**历史出处**
- 所有旗号必须符合**真实历史身份**
- **绝对禁止**：虚构、胡编、为算法妥协地理位置、为完美数据捏造历史

如果某项数据找不到历史依据 → **直接不收录**，不要编造。
**宁可缺少 1 个据点，不可错放 1 个据点。**




---

## 二、设计公理（不可动摇）

> **🔴 一据点一整套（2026-08-28 主人定死）**：每个据点必须同时录入 **1 个唯一势力 + 1 面唯一旗号 + 1 名相关武将 + 1 支相关精锐**；`panjun` 只能表示数据尚未完成，**绝不允许作为新增据点的交付状态**。新增据点未补齐四项就不算完成，禁止以“暂挂叛军”“未擅自编配”为由偷懒。武将、精锐须与该据点有史实关联，全部名称遵守主人给定的五字上限与分级标准。

| 公理 | 说明 |
|---|---|
| **1 势力 = 1 据点 = 1 旗号 = 1 军 = 1 色** | 项目核心机制，所有功能都建立在此之上（见下） |
| ↳ **一据点** | 每个 `factionId` 在 `cities_v2` 中**仅对应一座城**；`STARTING_CAPITALS` 唯一；无第二城、无「势力领土多分点开局」 |
| ↳ **一旗号** | 玩家只见旗号汉字 + 据点名；见 §三、§四 |
| ↳ **一军** | 开局/募兵：每据点同时最多 **1 支现役军团**（`RecruitmentSystem.cityHasActiveLegion`） |
| ↳ **一军一名** | **一个编制是一个军团，一个军团只能有一个名称**（严禁分裂多个别名/普通名/分类名，编制即军团，军团名唯一死磕） |
| ↳ **一色** | 每局 `FactionManager.addFaction` 分配 **唯一 HSL** → 旗号染色、军团贴图、领土同色（§10.2）；`factions.ts` 不写 hex |
| **以据点为主** | **据点名本位**：先留名气大地，再从此地史地身份（民族/政权/家族/州郡）立项定旗（§2.2、§三） |
| 最小据点间距 ≥ 50 km | 任意两据点须 ≥ 50 km；过近优先 **删小留大**（见 §2.1）；必要时可 **小幅微调 1–2 km** 凑够 50 km，严禁胡编大挪移 |
| **新建据点最小间距 ≥50 km** | **仅约束新建/改坐标**：与全图任意已有据点均须 **≥50 km**（见 §2.1.1）；不设最近邻距离上限 |
| 道路单段距离（非硬顶） | **汉地核心**驿道宜较短（酒泉↔瓜州 ~300 km 量级可作参考）；**边陲**（东北/草原/西域/中亚/青藏等）**允许更长单段**，渔猎/游牧/沙漠走廊本即远距节点，**不以 333 km 上限约束据点密度或删城** |
| **坐标优先不改** | lat/lng 锚定真实史地；间距冲突优先删点；两难时可 **小幅微调 1–2 km**，严禁大挪移；**据点名可改**（防重、换更知名旧称），见 §2.2 |

### 2.1 据点间距过近：删小留大，必要时小幅微调（基本准则）

**任意两座据点**（含 `panjun` 叛军点）实测地理距离 **< 50 km** 时：

| 做法 | 说明 |
|---|---|
| ✅ **删一个，留名气大的** | 以**据点名（历史地名）名气**为首要基准，删小留大（见 §2.2） |
| ⚠️ **小幅微调可接受** | 两难无法删留时，可 **±1–2 km** 微调来勉强凑够 50 km；**严禁**大幅挪移或胡编新坐标 |
| ❌ **禁止双点并存** | 不得「两个都留着」指望寻路/缩放能扛 |

**删留比较顺序**（同距冲突时自上而下比较，命中即留）：

1. **据点名史地知名度** — 按**大多数历史观认同**比较（开封 > 新郑；襄阳 > 樊城；长安 > 渭南；太昭 > 秀巴堡；开封 > 中牟）
2. `big_city`（§六标准：≥约 40 万人口）
3. 已挂正规旗号势力之首都（`factionId !== 'panjun'`）
4. `medium_city` > `small_city` > `pass`（关隘/哨所）
5. 长期治所、国都/州郡治所 > 堡寨、渡口、纯地标
6. 仍难分 → **交项目主人裁定**；若需微调 1–2 km 凑间距，也须在裁定中说明

**名气拿不准时的辅助判定（2026-06-17 立，仍仅限古代旧称）**：

| 步骤 | 做法 |
|------|------|
| 1 | 只在 **冷兵器时代（约 17 世纪 / 至迟 18 世纪前）可考** 的城名旧称之间比较（热兵器/近代城、明治后地名 **不得** 参与比武） |
| 2 | 仍难分 → 查 **维基百科**（中文优先，可对照日文） |
| 3 | **有独立词条** 的旧称 > 无词条或仅在他条一笔带过 |
| 4 | 仍难分 → **字数更多的古代旧称** 视为名气更大 |
| 5 | 间距 \< 50 km → **删小留大**（上表 1–6 顺序），或 ±1–2 km 微调；**禁止**为凑间距改大地名、大幅挪坐标 |

**禁止**：令制国府坐标自动优先于战国名城/古都（例：**月山富田** > 意宇国府；**太宰府** > 福冈城）；禁止为道路保留 `cityId` 而改据点名。

**删点连带（强制）**：**没有据点，就删除道路。** 任意据点从 `cities_v2.ts` 删除时，**必须同步**删除 `VectorRoadData.ts` 中所有以该 `cityId` 为 `startConnection` / `endConnection` 的道路段；**禁止**留悬空道路。实现方式：FactionEditor **`/api/batch-delete`**（删城时自动清道路），或 VectorRoadEditor / 清理脚本；**不得**只删城不改道路网。

**示例**：中原腹地开封、新郑与中牟过密 → 删 **中牟**，留开封/新郑；工布首都 **太昭** 与 panjun **秀巴堡** < 50 km → 删秀巴堡，留太昭。

### 2.1.1 新建据点最小间距 ≥50 km（2026-08-28 修订，AI 必跑）

**适用范围**：向 `cities_v2.ts` **新增** `city_*` 条目，或**改动**已有 `lat`/`lng`。  
**不适用**：只改 `name` / `factionId` / `note` 且坐标不动。

| 检查项 | 规则 | 不通过 |
|--------|------|--------|
| **下限（硬顶）** | 与**全图每一座**已有据点距离 **≥ 50 km** | 任一 <50 km → **禁止写入** |

**合法条件**：与任意已有据点均 **≥50 km**；不设最近邻距离上限。50–55 km 仅为提示（卡线），不作为禁止判定。

**命令（写盘前必跑，exit 0 才允许写入）**：

```bash
npx tsx scratch/audit_city_spacing.mjs --probe <lat> <lng> <据点名>
npm run city:spacing   # 写入后再跑全图 ≥50 km
```

**禁止**：

- 未经主人同意新增据点（见 §零）
- 不跑 `--probe` 就提交新坐标
- 用卡线 50–52 km 在密集区硬挤新点

#### 2.1.1.1 50 km 特许例外清单（**只有主人能开，AI 永远不许自行豁免**）

> **规则**：50 km 是硬闸，**唯一例外**是主人逐条特批；特批必须**逐条列在本表**（写清两地、实测距离、特批理由、批的日期）。
> AI **禁止**自行新增例外、禁止"因为史实名城建不进去"就默默放行 —— 遇到冲突**先报主人裁定**（见 §2.1 第 6 条）。

| 特许 | 两地 | 实测距离 | 特批理由 | 主人批 |
|---|---|---|---|---|
| ① | **推罗**（`city_tuile` 33.2709, 35.1962）↔ **阿卡**（`city_ake` 32.93, 35.08） | **39.42 km** | 两座都是黎凡特海岸的**史实名城**，相距本就只有 40 km 上下；删谁都会破坏史实。且推罗是亚历山大剧本**第三段「推罗围城战」的必备攻城载体**（攻城战必须打一座真据点，战场承载不了） | 2026-09-12 主人「新增推罗 + 保留阿卡（特批）」 |

**验收口径**：`npm run city:spacing` 报出上表这两点时**不算违规**（脚本内已登记同一份例外）；**其余任何 <50 km 一律按违规处理**。


**与 §2.1 关系**：§2.1 管**过近**（删小留大）；本节同样只管新建时的过近问题。道路单段距离不构成据点新增上限。

### 2.1.2 据点驻军一律 10000（2026-09-11 主人定，AI 必守）

> **主人原话**：「**驻军 一律10000，写到规则中**，你看看游戏开始哪个城不是一万？」

- **开局驻军 = 10000，五种城型一律相同，没有任何例外。**
- **依据（实测，非注释）**：`src/app/boot/GameAppCityLoader.ts` 第 34 行**硬编码** `troops: 10000`
  —— `cities_v2.ts` 里每条目的 `troops` 字段**开局被整个忽略、根本不进运行时**。
  各城型配置 `src/config/CityConfig.ts` 的 `initialTroops` 也都是 10000（big/medium/small/stockade/pass 五种一致），与之相符。
- **因此（AI 必守）**：
  1. **新增据点一律写 `troops: 10000`**；写 30000/20000 之类**对开局毫无作用**，只会误导后来者。
  2. **要改开局兵力，唯一去处是 `GameAppCityLoader.ts` 那一行**（或 `CityConfig.initialTroops`）——
     **改 `cities_v2` 的 `troops` 没有任何效果**，别在那儿使劲。
  3. 现存 `cities_v2` 条目的 `troops`（30000/20000/28000…）属**惰性数据**。
     **未得主人明确指示前，AI 不得批量改写**（禁止"一把梭"）；是否统一刷成 10000 由主人定。
- **🔴 没有「战场据点」这个例外了**（[2026-09-12 主人定：**战场独立出据点体系**]）：
  原先为「战场没有兵力」开的「整个不带 `troops` 字段、不参与每季募兵」两个例外**已全部撤销** ——
  战场现在是 `src/data/Battlefields.ts` 里的独立地名（**不是据点**），据点则**一律 10000、一律参与募兵**。
  连带的 `City.troops?: number` 可选、`CityManager` 6 处 `?? 0`、`ExpeditionCity.troops` / `CitySnapshot.troops`
  可选，也**一并还原为必填**。见 §2.1.3。
- **上限不是 10000**：`maxTroops` 按城型与文化倍率另算（大城 90000 / 中城 60000 / 小城 30000 / 城寨 10000 / 关隘 60000），
  开局 10000 只是**起点**，此后由 `RecruitmentSystem.recruitSeasonGarrison` 每季补。

### 2.1.3 战场（**不是据点**，是独立地名，类似奇观）（2026-09-12 主人定，AI 必守）

> **主人原话（按时间顺序，后者覆盖前者）**：
> ① 「**据点写第一页，建筑写鉴赏里**」
> ② 「战场不是没武将，是**需要了安置，不需要就算了**」
> ③ 「**战场没有主人，易什么主？**删除之前的错误逻辑」
> ④ 🔴 **最终定案**：「**要不然把战场独立出来，不做为据点，就叫战场。一个地名而已。类似奇观，万一缺少武将了，可以安置一个武将，但是不能出征，纯属剧情需要。**」
>
> ⚠️ **AI 在这条上错过三次**，别再错第四次：
> ① 写成「1 势力 + 1 武将 + 1 精锐，缺一不可」——自己推的，被否；
> ② 又写成「**不挂**武将、不挂精锐」的一刀切禁令——不是原话，被更正为「按需」；
> ③ 让战场**照抄守方 `factionId`**，于是它带着一个势力、还能被剧本「易主」——被一句「**战场没有主人，易什么主？**」揪出。

#### 一、战场的硬口径（**战场不是据点**）

| 项 | 定案 | 禁止 |
|---|---|---|
| **性质** | 🔴 **独立地名**，走**自己的数据表** `src/data/Battlefields.ts`（`bf_*`）+ **自己的图层** `src/map/BattlefieldLayer.ts`，**类似奇观**（`MonumentLayer`）。**不是 `city_*`、不进 `cities_v2`、不进 `cityManager.getCities()`** | 禁止再把它做成 `City`/`CityType`/据点表条目 |
| **为什么要独立** | 混在据点里时它带着一个照抄守方的 `factionId`，于是全项目 **30+ 处 `getCities()` 消费者**都看得见它（AI 目标池 / 任务系统 / 行军寻路 / 复国 / 存档…），只能靠 `GameAppCityLoader`、`TerritorySystem`、`RecruitmentSystem`、3 个审计脚本到处打补丁；还得把 `City.troops` 改成可选、连带 6 处 `?? 0`。独立后**这一整类补丁与隐患全部不存在** | —— |
| **没有势力、没有兵力** | 战场表里**没有** `factionId` / `troops` / `type` 这些据点字段。它不是任何势力的地，也不驻军 | 禁止给战场配势力；禁止写 `troops: 0`（那是"这个城 0 兵"的语义） |
| **不能攻占、不能易主** | 它不在 `getCities()` 里 → AI 目标池、攻城、占城流程**天然碰不到它**；剧本 `cityUpdates` **只写真城**，绝不写战场 | 禁止任何形式的「战场易主」 |
| **不受 50km 间距约束** | 战场不在据点表里 → 间距审计**根本看不见它**，豁免也不需要了（原先 `audit_city_spacing.mjs` / `audit_four_axioms.mjs` / `audit_faction_capital_sync.mjs` 里的战场豁免**已全部删除**） | 禁止为凑 50km 把战场挪离剧本坐标 |
| **不占势力位、不会成叛军** | 没有 `factionId` 就不会落到 `panjun`，也不占 `STARTING_CAPITALS`。原先那条「沙盒兜底对战场跳过」的特判**已删** | 禁止新建势力 |
| **不插旗** | 没有势力，自然没有旗。`TerritorySystem` 里原先那句 `const showFlag = city.battlefield !== true;` 特判**已删**（据点一律插旗） | —— |
| **坐标** | 🔴 **必须与剧本 `fieldBattleData.location` 一字不差** | 禁止自行挪动 |
| **一场仗 = 一个战场** | 剧本里**每一场 `field_battle`** 都要在 `Battlefields.ts` 有一条**同坐标**战场，否则那场打完不上图 | 血训：2 场仗只建了 1 个 → 主人「**可是剧本打了两仗呀**」 |
| **🎭 武将 = 按需、纯剧情、永不出征** | 主人④原话：「万一缺少武将了，可以**安置一个武将**，但是**不能出征**，纯属剧情需要」。实装 = `BattlefieldData.storyGeneralId?`（可选），**只在标牌上多一行名字**；**不生成军团、不领兵、不参战、不给精锐**。<br>🔴 **现任**：`bf_yisusi` 挂着**大流士三世**（`daliushi_iii`）—— 主人 2026-09-12「**就放战场不行吗**」：他属于 `boluosi`（旗号「波斯」/ 不死军），而 `boluosi` 唯一的据点原本就是伊苏斯，伊苏斯现已独立成战场 → **他的站台就是这个战场本身**，不给势力另找城、也不删势力。`bf_gelanikusihe` 不需要，故未挂 | 禁止让剧情武将出征/带队/进任何刷兵逻辑 |
| **验收（离线）** | `npx tsx --import ./tools/sim-preload.mjs scratch/verify_battlefield_independent.mts` —— 逐条查：不是 `city_*`、不在 `CITIES_V2`、每场仗都有同坐标战场、无剧本给战场易主、无据点字段 | 禁止只看代码不实测 |
| **验收（真游戏 DOM）** | `node scratch/check_battlefield_layer.mjs` —— 实测：据点 1086 座里**战场 0 个**；战场 pane `leaflet-battlefield-pane` z=**640**（据点 610 < 战场 < 奇观 650）；**开战前 0 个形态 img**、标牌显示地名；打完 **4 个零件 img**；**旗面 0** | 禁止只看代码不实测 |
| **验收（形态 parity）** | `npx tsx scratch/verify_bf_morphology_parity.mts` —— 从 `_citytest.html` **抠出它自己的源码**与 TS 版逐字段比：包络 **311.8×224** 一致、布局一致、逐种子零件/位置/镜像一致、出图 HTML 逐字节一致 | 改形态数学必须**两边同步**，否则页面与游戏画得不一样 |

#### 二、显示时机（主人 2026-09-12 定死，换了载体但规矩不变）

| 时机 | 表现 |
|---|---|
| **开战前** | **只显示地名标牌** —— 不显示战场形态。🔴 主人原话「**战场据点开战前只显示地名。不显示战场。**」 |
| **打完** | 地名 + **战场形态**（拒马 / 尸体 / 骨骸 / 火把 / 牲口骸骨 / 栅栏木堆 / 残破战旗） |

- 判定：`src/events/battlefieldState.ts` 的 `markBattlefieldFought(id)`（由 `HistoricalEventManager.applyScriptCityUpdates` 按剧本 `location` 找战场后调用）→ 触发 `BattlefieldLayer` 重绘。
- ⚠️ **旧报障的教训**：「上场前只显示地名」曾经靠「**不进 `cityMarkers`**」实现，而 `appendCityMarkers` 的去重条件正是 `!cityMarkers.has(id)` → 每次拖图都再加一个同名标签，实测叠了几十个（主人报「**战场显示不对**」）。**独立成图层后这个坑整类消失**（图层整体重绘，不再有"半进半不进"的分支）。

#### 二、形态：不长成城寨，也**不长成纪念碑**

> 🔴 [2026-09-12 主人改定] 「**不行，太大了，太显眼了，你用一个拒马+SCEN_SKELETON_SOLDIER**」
> —— 原方案是「纪念碑（骑马像 / 废墟）+ 翻倒辎重车」，**立着的大件、太显眼，已废弃**。

- **无城墙、无城门、无民居** —— 是**一片没有建筑的野地**上的一处**贴地痕迹**，零件全部从既有素材库拼装。

##### 二之一、定版构成：**保底三件 ＋ 随机补 1~2 件 ＝ 每场 4~5 件**（2026-09-12 主人定）

> 🔴 **主人原话**：①「**一个拒马，一个尸体（2个随机），一个骨骸（3个随机）**」
> ②「**打烂的拒马残骸替换拒马**」→ 追加「**拒马残骸和拒马随机**」 ③「**其他你说的都可以**」→ 追加「**翻倒辎重车不要**」
> ④「**选出 4-5 个，随机摆放，随机镜像即可**」

| 槽位 | 件 | 库里对应的素材 | 参考尺寸 | 随机方式 |
|---|---|---|---|---|
| ① **拒马位**（保底） | **完好的拒马 ＋ 打烂的拒马残骸** | `STAKE_BARRICADE` ＋ `BARRICADE_RUBBLE_A/B/C/D` | 96×76 / 228×56 / 164×96 / 100×120 / 176×88 | **5 款随机**（有的战场拒马还立着，有的已被打烂） |
| ② **尸体**（保底） | 木棍插着的尸体 | `IMPALED_CORPSE_ANT` / `IMPALED_CORPSE` | 24×116 / 24×112 | **2 款随机** |
| ③ **骨骸**（保底） | 白骨 / 带腐肉 / 阵亡甲士 | `SKELETON_BATTLEFIELD` / `SKELETON_CIVILIAN` / `SKELETON_SOLDIER` | 76×44 / 80×44 / 84×68 | **3 款随机** |
| ④⑤ **随机补件**（每场抽 **1~2 种，不重种**） | 火把 / 牲口骸骨 / 栅栏木堆 / 残破战旗 | `TORCH_A·B` / `ANIMAL_SKELETON` / `FENCE_RUBBLE` / `SCEN_FLAG_*`(8 面) | 16×68 / 80×60 / 100×60 / 旗 72×84~92×104 | 抽种 + 种内抽款 |

- **为什么是「保底三件 + 随机补」而不是「全池随机抽 4~5 件」**（主人授权按「符合历史·真实」自定）：
  **战场没有尸体和骸骨就不叫战场** —— 全池随机抽会抽出「四处木堆、一个人没有」的野地，不真实；
  但三件全固定又场场一样，所以**补件随机**。
- **必须低调**：旧版「纪念碑」形态的美术包围盒是 181×211（立着的大件），**已废弃**；
  现版参考画布 **340×240**，包络 **311.8×224**；第一页卡内实测可见像素占 **73% × 75%**（真实 Chrome 量的），
  **不许再做成"一尊立着的纪念碑"**。
- **不按文化分款**：拒马 / 拒马残骸 / 旗 / 骸骨 / 火把都**不区分文化**，全图**一套统一形态**。
- 🔴 **主人明确否掉的五件，不许再往里加**（2026-09-12 AI 提议、主人认可的理由）：
  · **坟 `GRAVES`** —— 坟是**战后收尸**才立的，仗刚打完不该有坟；且它是**十字木牌**，带基督教意味，配古典战役不对。
  · **军营帐篷 `GREEK_WAR_TENT`** —— 「帐篷」是**驻营**不是战场，体量也偏大。
  · **攻城器残骸 `DECAY_*`** —— 只有**攻城战场**才该有；野战场上出现投石机残骸不符合史实。
    （要加就得按「仅攻城战」分条件 —— 那是新逻辑，**未得主人许可不得擅自加**。）
  · **船骸 / 搁浅鲸骨** —— 只有海战才用。
  · **翻倒辎重车 `CART_BROKEN`** —— 主人 2026-09-12 明确「**翻倒辎重车不要**」，**已从补件池移除**，不得再加回。

##### 二之二、摆位三铁律（沿用上一版，全部实测踩过）

- **各自随机挪位 ＋ 各自随机镜像**：每件 `±jx/±jy`（参考 px）独立掷骰，`flip` 独立掷骰。
  - **必须可复现**：走页面既有的 `mulberry32(hashString('bf_' + seedKey + '_' + randomSeedOffset))`，
    **禁止用 `Math.random()`** —— 种子 = 战场据点身份（**游戏里传 cityId**），
    所以同一屏各自独立、**刷新结果不变**、点顶部 🎲 才重掷一轮。
  - **镜像要连锚点一起翻**（`ax%→100-ax%` + `scaleX(-1)`），否则地面点会跑到另一侧。
- 🔴 **定尺必须用「抖动包络」**，**不能用某一次掷骰的实际包围盒**；
  且本版包络 = **所有可能零件 × 5 个槽位的并集（常量，与本场抽到哪些件无关）** ——
  否则 4 件场和 5 件场、抽到大残骸还是小火把，比例尺都会变、战场忽大忽小。
  用常量包络 → **所有战场据点同一个比例尺**，且掷到任何一边都不越界。
- 🔴 **抽签必须固定消耗同样多的随机数**（先按种子把 4 个槽位+补件全掷完，再用 `spec` 覆盖款式），
  这样页面「逐款对照」时**只换那一件、其余件的位置与镜像完全不动**，才比得出来。

##### 二之三、锚点必须按像素实算，不许手填

`ax/ay`（图上的**地面点**）由 `scratch/probe_part_anchors.py` 从 alpha 通道实算：

- **立着的**（旗杆 / 火把 / 插桩尸体）→ `ax` = **最底下 3 行不透明像素的中点** = **杆脚**
  （用包围盒中心会把杆子插歪：整块旗布会把中心拉偏）。
- **趴平的**（残骸 / 骸骨 / 破车 / 木堆）→ `ax` = **内容包围盒中心**
  （这类最底下 3 行常只剩左角一小块碎屑，用中点反而把整堆推偏）。
- `ay` 一律 = 内容包围盒底边。血训：这两个规则**分开判**才对，单用一种都会偏。

##### 二之四、改完必须验收的两条命令（缺一不算交付）

```bash
py scratch/proof_battlefield_layout.py   # 离线复现：同一套布局数学重算 + 合成出图，自己读图确认
node scratch/verify_bf_rng.mjs           # 真 Chrome 导出 DOM 里每个图元的实际位置/尺寸/镜像
py scratch/verify_bf_layout.py           # 与离线复现逐件对账（实测差 ≤ 0.5px 才算一致）
node scratch/measure_citytest_page.mjs   # 第一页 6 张卡不越界 + 战场页 24 张小图不越界
```

- **零件一律从既有素材库拼装，严禁新造美术**。
- 🔴 **素材目录名严禁与 16 套母体代号撞名**（血训：曾取名 `BATTLEFIELD_MESO`，撞了母体 `MESO`＝中美玛雅，
  里面贴的却是两河泥砖；两河属 `ORIE`——那批款式已随本次改定一并删除）。

#### 二之二、DE 到底有没有「残破的旗帜」（2026-09-12 主人问，**已实测查证**）

> **结论：DE 里没有名为「残破旗帜」的素材，但有几面旗帜的画法本身就是破边 / 垂落 / 只剩旗杆的，可直接当残破战旗用。**

- **名称扫描**（`…\AoE2DE\resources\_common\drs\graphics`，实测 **7372** 个 `.sld`）：
  含 broken / damaged / destroyed / torn / ragged / tatter / wreck 的**旗帜：0 个**。
  带 broken 的场景件只有 `s_bridge_*_broken_*`（破桥）与 `s_cart_broken`（破车）。
- **旗帜本身有 34 个**（含 `s_flag_macedonian` 马其顿维吉纳太阳、`s_flag_thracian`、`s_flag_puru`、
  `s_flag_boeotian`、`s_achaemenid_flag_*`、`s_athens_flag_*`、`s_sparta_flag_*`、`s_flag_a`…`s_flag_u`）。
- **看像素判定**（名字不算数；`scratch/probe_de_flags.py` / `probe_de_flags_zoom.py` 出图后人工读）：
  画法本身**破边/垂落**的几面 —— `s_flag_n`（深灰破布、边缘撕裂，最像残破战旗）、
  `s_flag_r`（深色破旗带白色兽纹，末帧撕得很烂）、`s_flag_s`（白底 X 十字、下缘破烂）、
  `s_flag_d`/`s_flag_e`/`s_flag_u`（白旗破边）；**只剩旗杆没有布**的 `s_flag_ritual_a`（1 帧）；
  垂落收拢的 `s_flag_a`/`s_flag_i`/`s_flag_j`。
- **摆位实测教训**：旗放右侧 (0.86W) 时美术包围盒涨到 **296px 宽 > 卡宽 278px**，被卡片裁掉只露一条缝；
  改放**两件中间偏后** (0.56W) 后包围盒宽度不变、只加高度，正好塞得下。
- **已提取成素材**：8 面候选旗 → `public/SUCAI_BUILDING/SCEN_FLAG_*`
  （`N` / `R` / `S` / `D` / `E` / `U` 破旗、`RITUAL_A` 只剩旗杆、`MACEDONIAN` 完好马其顿旗），
  提取脚本 `scratch/extract_scen_flags.py`（**一律 `max_frames=1`**：这些旗在 DE 里是 90~360 帧动画，
  整条雪碧图会做出几十 MB）。
- **当前默认挂 `SCEN_FLAG_N`**（主人问「**旗帜在哪呢**」→ 先让他看得见）。
- **页面可逐面挑**：点第一页的「⚔️ 战场」标签，8 面候选逐张套进同一场面（含"不加旗"），
  主人看一眼说"用哪面"或"不要旗"即可换（`_citytest.html` 的 `BF_FLAGS` + `renderBattlefieldBoxHtml(artW, flagKey)`）。
- ⏳ **最终用哪面 / 要不要旗，仍未拍板**。

#### 二之三、库里的「尸体 / 骸骨」素材（2026-09-12 主人问，**已全部实测**）

> **主人原话**：「**有个尸体的素材怎么没有看到，这个也可以用在战场呀**」

- **查法**：扫全部 `public/SUCAI_*` 目录名 + DE 本体 `drs\graphics` 的 `.sld` 名，
  把命中件**全部调出来并列放大**人工看（`py scratch/probe_corpse_assets.py`
  → 对照图 `scratch/out/corpse_candidates.png`）。
- **命中 9 件**（都在 `public/` 里**已有**，无需新提取）：

| 素材 | 大小 | 样子 |
|---|---|---|
| `SUCAI_BUILDING/SCEN_SKELETON`（= DE `s_skeleton`） | 80×44 | **躺平的无名尸体骸骨**（宽扁） |
| `SUCAI_BATTLEFIELD/SKELETON_CIVILIAN`（`s_skeleton_civilian`） | 80×44 | 平民尸体（带腐肉、偏暖褐） |
| `SUCAI_BATTLEFIELD/SKELETON_BATTLEFIELD` | 76×44 | 同上类的战场骸骨（与 `s_skeleton` 基本同源） |
| `SUCAI_NATURE/SKELETON` | 80×44 | 与 `SCEN_SKELETON` 同一个（DE `s_skeleton`） |
| `SUCAI_BATTLEFIELD/IMPALED_CORPSE`（`s_impaled_corpse`） | 24×112 | **插在木桩上的尸体**（细高） |
| `SUCAI_BATTLEFIELD/IMPALED_CORPSE_ANT`（`s_antiquity_impaled_corpse`） | 24×116 | 同上·**古典时代款**（-334 本役适用） |
| `SUCAI_BATTLEFIELD/ANIMAL_SKELETON`（`s_animal_skeleton`） | 80×60 | 牲口骸骨（大牲口骨架） |
| `SUCAI_BUILDING/SCEN_SKELETON_SOLDIER`（`s_skeleton_soldier`） | 88×72 | 阵亡甲士（**原先唯一在用的一件**） |
| `SUCAI_BUILDING/SCEN_GRAVES` / `SUCAI_NATURE/GRAVES`（`s_graves`） | 36×44 | 石十字墓碑（不是尸体，是坟） |

- **已采用**：战场据点第 3 位 = **`尸体 SCEN_SKELETON`**（躺平的尸体骸骨，贴地矮、不抢戏）。
- **页面可逐件挑**：点第一页「⚔️ 战场」标签 → 下排 **5 件候选尸体**（PLAIN / CIVILIAN / IMPALED /
  IMPALED_ANT / ANIMAL）+「不放尸体」，上排仍是 8 面候选旗。挑好说一句就换默认值。
- ⚠️ **插桩尸体（IMPALED）细高（24×112）**，会像旗一样顶到卡高，改默认值前先跑
  `node scratch/measure_citytest_page.mjs` 看有没有越界。

#### 三、页面摆位（信息架构，AI 不得摆错）

| 内容 | 页面 |
|---|---|
| **一切据点形态**（大城 / 中城 / 小城 / 城寨 / 险要 / **战场**） | `public/_citytest.html` **第一页「据点形态评估」** |
| **一切建筑素材** | 同一文件的 **「建筑鉴赏」**（视图三，数据来自 `public/assets/de_buildings_catalog.json`） |

- **🔴 严禁把据点形态塞进建筑图鉴**：`BATTLEFIELD_*` 已在 `scratch/build_de_buildings_catalog.mjs` 扫描时排除，
  分组白名单里也不再登记。**据点一律第一页实时渲染，建筑素材一律鉴赏页。**
- ⚠️ `_citytest.html` 的**分组下拉是写死白名单**（`buildFilters()` 的 `otherOrder`）：
  新分组**不加进去就永远选不到**，数据在 JSON 里也白搭。新增分组必须同时加白名单。

#### 三之二、第一页 6 张据点卡的排布（2026-09-12 主人定）

> **主人原话**：「**把这个分两行显示，这样不就可以大一点了。大中小城一行**」

- **分两行、每行 3 张**：第 1 行 = **大城 / 中城 / 小城**，第 2 行 = **城寨 / 险要 / 战场**
  （CSS 类 `.grid-3x2`，`repeat(3, minmax(430px, 1fr))`；**卡序不得乱动**，乱了行就分错）。
- **画的东西要跟着放大**：单卡 278px → 约 430~640px（随窗口），故 5 座城的 `baseStage`
  由写死的 `120 * curZoom` 改为**按卡的实际宽度定尺**：
  `baseStage = max(120, round(卡宽 × 0.30 × curZoom))`。
  **写死数字是不行的**：窄窗口会溢、宽窗口又浪费（实测：写死 240（=300@125%）时大城可见像素 736px > 卡宽 617px，**被裁 59px**）。
- **战场卡**：用 `bfFitArtW(卡宽, 430, 默认旗)` 按**抖动包络**在卡内取最大，再 `× (curZoom / 1.25)`。
  🔴 **`bfFitArtW` 算的是"刚好塞满卡"的尺寸，不能再乘一次 `curZoom`**（默认就是 1.25）——
  实测踩过：多乘一次 → 越界约 30px。要高/低就改 `curZoom`（页面的同屏缩放按钮）。
- **验收（权威）**：`node scratch/measure_citytest_page.mjs`
  —— 用**真实 Chrome（puppeteer-core）**打开页面，量每张卡里所有 `<img>` 的
  **可见像素（alpha bbox）** 并集，报占比与是否越出视口；
  **再连点 🎲 6 轮抽检战场卡的随机排布**（每轮报留白 / 镜像数 / 是否越界）；
  并出截图（`page1_grid_2rows.png`、`page_battlefield_flagpicker.png`、`bf_draw1/2.png`）供人工看。
  **⚠️ 量 `<img>` 的 bounding box 会误报**：素材四周有透明留白，必须按 alpha bbox 算。
- **离线镜像（辅助）**：`py scratch/proof_battlefield_page1.py` 只复现**战场卡的包络几何**
  （5 座城的渲染数学太复杂、掷骰的 PRNG 也不该在 Python 里重写一份 → 那些以 Chrome 实测为准）。

### 2.1.4 提取素材**严禁重复**（2026-09-12 主人定，AI 必守）

> **主人原话**：「**别弄所有素材了，我怕你又搞重复，你先把现有提取素材整理下，然后列个单子，分类。以后再提取素材不要重复。**」

**现有素材分类单子（唯一入口）**：
- **数据**：`public/assets/asset_inventory.json`（`node scratch/build_asset_inventory.mjs` 生成，**只读扫描、不提取**）
- **人看的单子**：`scratch/out/asset_inventory.md`（库 → 分类 → 逐条目录名）
- **程序里看**：~~鉴赏页顶部「📚 素材总览」看板~~ —— 🔴 [2026-09-16 主人「这个不要这样显示，
  或者不要显示，有必要显示吗，这么占地方」]**该看板已从鉴赏页整块移除显示**（24 组几十行目录名太占地方）。
  要看清单开 `scratch/out/asset_inventory.md` 或 `asset_inventory.json`（两者照旧生成，一个字节没删）。
  🔴 **[2026-09-16 主人「这里的建筑鉴赏，是显示所有建筑的」「不是建筑的[不要]在这里显示」
  「不要删除素材，只是不要在这里显示」]**：鉴赏页图鉴**只显示建筑**（`SUCAI_BUILDING`）；
  其余 8 个库仍完整写在 `asset_inventory.json` / 单子里、素材目录一个没删，只是**不在鉴赏页显示**。
  同次改动：鉴赏页默认「全部状态」（完好/受损/摧毁/废墟全出），并补回两个漏掉的目录
  （`THRACIAN_GATE_STONE_NE_RUBBLE`、`THRACIAN_TOWER_AGE3_RUBBLE`）→ 建筑 **2672**。
  验收：`node scratch/verify_bldg_all_buildings.mjs`（真 Chrome 实测 + 出图）。
- 现状（2026-09-16 实测，`node scratch/build_asset_inventory.mjs`）：**3491 个已提取目录** ——
  建筑 2672 / 单位·武将·船·弹道 465 / 植被 133 / 动物 65 / 战场遗存 56 / 资源 55 / 商船 22 / 特效 18 / 地面 5。

**提取铁律（违者返工）**：

1. **提取前先对账**：`py scratch/audit_asset_coverage.py`（DE 有 7372 个 .sld，看已提多少、还缺哪些）。
2. **两道防重闸门必须开着**（`scratch/extract_de_assets.py` 已实装，别绕过）：
   - ① **显著词重叠**：单位/动物类（`a_`/`u_`）只要与已有目录共享一个显著词（cow / horse / alfred…）→ 跳过；
   - ② **内容指纹**：先提到临时目录，算 `frames.png` 的 md5 与**全库**比对，重复即丢弃、不入库。
3. 🔴 **逐动作单位类 = 一个单位一个目录**（动作写在目录内的文件名里），**绝不许一个动作一个目录**。
   血训：本轮把 `a_herd_cow_blackandwhite_idleA` 等提成了 `HERD_COW_BLACKANDWHITE_IDLEA` 等 **26 个目录**，
   与已有 `COW_BW` / `COW_BLACK` **完全重复**，被主人当场抓住（「例如动物」）。
4. 🔴 **撤销/回滚脚本必须先按「已提取」过滤计划**。
   血训：本轮撤销时用了**未过滤**的计划，把已有的 8 面旗（`SCEN_FLAG_*`）也一起删了
   （已用 `scratch/extract_scen_flags.py` 恢复；恢复后 SUCAI_BUILDING 回到 2671）。
5. **两个大族已查清但「不擅自提取」**（体积原因，等主人发话）：
   `u_` 单位逐动作 **1178 个 / 1722 MB**、`b_foundation_*` 地基废墟 **52 个 / 122 MB**。
   🔴 [2026-09-12 主人定] 「**先不提，保持现状**」——**未再得主人许可前，不得提取这两族**。
6. **DE 名自带 `_x1`**：拼 sld 路径时**只补 `.sld`**，别再拼一次 `_x1`
   （血训：拼成 `..._x1_x1.sld`，1341 个全 FileNotFoundError）。

#### 一、清点素材时**必须递归**：「图在子目录里」≠「空目录」（2026-09-12 血训）

> **我这轮把 11 个有图的目录误报成「空目录」并请示删除，实际它们各含 2~24 张真图（共 5.1 MB）。
> 错因：只 `readdirSync(库/目录)` 数顶层文件，而特效类的图在**方向子目录**里（`dir00/fly_0.png`）。**

- **判「空目录」的唯一正确做法 = 递归到底**（`findDeepPng()`），顶层没有 png **不算空**。
- 同时**体积也要递归**（原版只算顶层 → `SUCAI_FX` 记成 2.24 MB，实为 7.25 MB）。
- 血训范围：漏计 `SUCAI_FX` **11 个**、`SUCAI` **18 个**（`BLOOD`/`FIRE`/`DUST`/`IMPACT_*`/
  `MUZZLE_*`/`WAKE_*`/`WATER_SPLASH`/`STRONGHOLD_RESOURCES`/`_downloads`）。
- 两个脚本都已修（`build_asset_inventory.mjs`、`build_asset_libraries_catalog.mjs`），
  核图脚本 `scratch/check_gallery_broken.mjs`（强制 eager + 等 settle + HTTP 复核）。
- 🔴 **懒加载不算坏图**：不加 `loading='eager'` 就直接数 `naturalWidth` 会得出「465 张里 217 张坏」
  的假结论；实测严格核图后 **0 坏图**。

#### 二、`SUCAI_FX` 是「程序在用的瘦身版」，`SUCAI` 下的同名目录是大图原件

- **实代码**（非注释）：`src/ui/Scene13WarLayer.ts` 里 `const base = \`/SUCAI_FX/${cfg.path}/\``
  —— 运行时读的是 **`SUCAI_FX`**（抽帧 + 裁透明边 + 16 向降 8 向）。
- 两库**同名 11 个目录**（`BLOOD`/`DUST`/`FIRE`/`IMPACT_*`/`MUZZLE_*`）实测：
  **相对路径完全一致、md5 无一个相同**，`SUCAI` 版 **71.91 MB** vs `SUCAI_FX` 版 **5.01 MB**
  （`BLOOD` 单帧 120000×400 vs 1616×116，差 74 倍；`SUCAI` 的 `MUZZLE_*` 是 16 向，`SUCAI_FX` 是 8 向）。
  → **按 md5 的防重闸门抓不住这种「同源不同尺寸」的重复**，必须另眼看尺寸/方向数。
- `SUCAI` 下另有 **4 个非游戏素材目录**混着：`_downloads`（3049 png / 73.1 MB）、
  `STRONGHOLD_RESOURCES`（17341 png / 57.7 MB）、`0AD_UNITS`（14 jpg）、`STRONGHOLD_EUROPE`（1 zip）；
  单子里单列一桶「素材来源 · 参考资料（非游戏素材）」。**它们仍在 `public/` 下，会被打包进产物。**
- 🔴 **删/移任何目录前必须先问主人**（单子里现在这些都在，未动）。

详细口径与工具清单见 [`docs/02-design/asset-libraries.md`](docs/02-design/asset-libraries.md)。

### 2.2.0 据点本位录入：势力·武将·精锐（2026-06-22 主人定，**录入唯一入口**）

> **铁律**：**一切以据点为基准**，不以人物为基准，不以既有势力表为基准。  
> **禁止**「武将是白起 → 势力该是秦国」；**禁止**「秦国 → 白起该在天水」式反推。  
> **每一座录入据点**独立一套：据点名 + 势力 + 武将 + 精锐；**互不因名将归属而串城**。

**录入顺序（不可跳步）**：

| 步 | 问什么 | 产出 |
|----|--------|------|
| **① 据点** | 此坐标有哪些**城名**？（**冷兵器 · 约 17 世纪 / 至迟 18 世纪前**可考） | `cities_v2`：`name` + `lat/lng`；取**知名度最大**旧称（**一个点**的名） |
| **② 势力** | 这一带按下列优先级，有何可考身份？ | `factions.ts` 全名 + 旗号；**势力名 ≠ 据点名**（严格不等） |
| **③ 武将** | 哪些将领在此**战斗、驻守**过？ | `FactionGenerals` + `GeneralSkills`；须与**该据点**史地相关 |
| **④ 精锐** | 哪些部队在此**战斗、驻守**过？ | `*ExpeditionLegions`；须与**该据点武将**相关；定 T0–T3 |

**② 势力命名优先级**（无则向下试，命中即停；须有史料，宁缺毋滥）：

1. **民族** — 发源地或极深厚渊源  
2. **政权** — 曾在此建政且为**首都/割据中心**（正式国号去后缀作旗号，见 §4.4）  
3. **州郡治所** — 郡/州治（旗号取郡州专名，见 §4.5、§4.7）  
4. **家族** — 名门大本营（旗号单姓，见 §4.6）  
5. **地名** — 战场、水源、驿站、军台、要塞等**可考**旧称  

**③ 武将**：武将和精锐和据点必须契合，有相关性，最好是此处据点的守将，为此处做出贡献的，在此处镇守、当官、执行过军务也行（古代即可）。不要出生地在此却无贡献的人（例：常山赵云）。  
另守 **武将挂名四硬顶**（详见 `.cursor/rules/general-name-city-fit.mdc`）：**据点契合 · 知名度优先 · `generalName` 全局严格不重复 · 显示名 ≤5 字**。禁止多名「阿方索」「威廉」等同词干并列；同名君主须用**知名称呼/通称**区分，超 5 字或不辨则宁缺毋滥。  
**攻防 / 三势**：唯一标准是符合历史（`.cursor/rules/general-history-fix-direct.mdc`）；史料可定案则**直接改，不必再问**。  
**④ 精锐 tier**（与 §12.3.1 一致）：

| 级 | 评级（乘数） | 核心门槛 |
|----|------------|----------|
| **T4** | 存在之兵 (×1.1) | 确有其军，建制可考 |
| **T3** | 风土之兵 (×1.2) | 文化知名，独具一格 |
| **T2** | 特色之兵 (×1.3) | 战术尖刀，一战成名 |
| **T1** | 功勋之兵 (×1.4) | 战略主力，名垂青史 |
| **T0** | 传奇之兵 (×1.5) | 扭转乾坤，青史巅峰 |

**运行时（占城后）**：见 `docs/AGENTS/expedition-elite.md` §12.2.1 — 将/精跟**据点录入**；旗号跟**占城势力**。

**反例（禁止再犯）**：

| ❌ 错误思路 | ✅ 据点本位 |
|------------|------------|
| 白起是秦将 → 长子挂秦 | 长子是上党郡治 → 势力**上党**；白起因**长平/上党**战事挂此城 |
| 秦的首都是天水 → 白起应在天水 | 天水一套（秦·嬴稷·秦之锐士）；长子另一套（上党·白起·上党轻骑） |
| 势力名也叫「长子」 | 据点名长子；势力名**上党**（不等、不重复） |

**现盘示例**（各城独立，不作全局真理）：

| 据点名 | 势力 | 武将 | 精锐 |
|--------|------|------|------|
| 长子 | 上党 | 白起 | 上党轻骑 |
| 天水 | 秦 | 嬴稷 | 秦之锐士 |

### 2.2 据点名本位：一地多史、多势可源（基本准则）

**锚点 = 据点名 + 坐标。** 建档、删留、定旗一律**从大地名出发**，不是先立势力再凑据点。

**一地多史**：同一真实地理位置上，史籍可考的叠合身份**均可**成为独立势力的来源——不限一种，历史上在此或以此为中心的：

| 身份类型 | 定旗走 §4.1 优先级 | 可立项为势力 |
|---|---|---|
| 民族 | 1 | ✅ |
| 政权（国都） | 2 | ✅ |
| 郡治 | 3 | ✅ |
| 家族大本营 | 4 | ✅ |
| 州名 | 5 | ✅ |

每一种身份各按 §4.1 **单独定旗号、单独占一座城**（仍守 1 势力 = 1 据点 = 1 旗号）。**势力从史地身份长出，史料从「这名大地」流出**；禁止先有空 `factionId` 再反向污染据点名或坐标。

> **「多势可源」是立项来源，不是一城多旗。** 从一名大地可盘点多种史地身份、分别立项为多个势力（各占一城、各定一旗）；**不是**一座城同时挂多面旗。

**据点名与势力名的史地来源（基本准则 — 必读）**：

> **据点可以是历史上此地的所有称呼；势力可以是此处所有的政权、民族、部族、家族、郡治、州名、可考地名。**  
> 据点名与旗号**各从该坐标（`lat/lng`）的史地清单中独立选取**，**不必**同一时代、**不必**同一身份类型，**禁止**因「据点名是三国、旗号是东晋」之类理由互相否决。

| 对象 | 规则 |
|---|---|
| **据点名** | 锚点 = **坐标 + 一个真实地点专名**。可为冷兵器时代可考的城邑、聚落、关隘、要塞、驿站、军台、港渡、圣地、遗址、战场、水源或重大自然地标；同地多名择知名度更大者挂牌。**据点不等于城市**，禁止把片区名冒充一个点，也禁止虚构地点。 |
| **势力 / 旗号** | 同一坐标，凡史上可考的 **政权、民族/部族、家族、郡治、州名、地名** 等身份，各有 §4.1 可考依据者，**均可**各立项为独立势力（各占一城、各定一旗）。**不是**据点名定什么时代，旗号就必须跟什么时代。 |
| **运行时** | 仍守 **1 坐标同时只挂 1 面旗**；争同一坐标时按 §2.1 删小留大，失利方改挂**第二 / 第三**可考据点（≥ 50 km）。 |

**示例（据点名 ≠ 旗号身份，合法）**：

| 坐标 | 据点名 | 旗号 | 说明 |
|------|--------|------|------|
| 含山濡须口 | **濡须口** | **北府** | 据点名取三国魏吴咽喉；旗号取东晋北府兵（同地可源，互不绑死） |
| 百色田阳 | **田阳** | **壮** | 据点名取田阳旧称；旗号取壮族土官 |
| 临汾 | **平阳** | **尧** | 据点名取尧都旧称；旗号取尧 |

**定旗**（仅针对**已立项**的某一势力）：对该势力按 §4.1 从优先级 1 试到 5 定旗号；**不得**用无据后世称呼凑旗；**但不得**用据点名的时代去否定该坐标上另一可考身份立项。

**立项须有依据**：可以从此地建立势力，但**禁止胡乱建立**——须满足 §4.1 某级**可考史料**（及 §4.8 宁缺毋滥）；名将路过、无据关隘、**非冷兵器时代城名**等**不算**立项依据。

**据点不动（精确定义）**：

| 一般不碰 | 允许调整 |
|---|---|
| **坐标** lat/lng（最好别改） | **据点名** `name`：须为真实可考的**一个地点专名**；人造据点须在冷兵器时代可考，自然地标须有古代命名或人类活动依据 |
| 不为势力、不为凑间距而挪点 | **旗号 ↔ 据点名防重**：**优先改旗号**；大地名/区域主标（奴儿干城等）**禁止**仅为防重改成衙门/小地名；见 §2.4 |
| 不把大地名挪到小堡寨坐标上 | 换用**知名度更大**的同地旧称（例：西夜旗 @ **叶城**） |

**据点名**：全局**不重复**（严格相等判重，见 §三）；近距删留、防重换称时，**优先用知名度更大的历史地名**。

**与坐标的唯一占有**（运行时仍 1 点 1 旗）：

| 规则 | 说明 |
|---|---|
| 名气大地占原坐标 | 据点名更大者留住该 lat/lng；失利方改挂**第二 / 第三**可考首都（≥ 50 km） |
| 坐标不动 | 不为小势力平移 lat/lng；不为小势力把长安、洛阳级坐标让给堡寨 |
| 旗号可多元、坐标须唯一 | 长安可源出唐、隋、汉等多势力，但城中**同时只挂一方**；其余改别都（例：大顺 → 渭南） |
| 防重 | 旗号 ↔ 据点名冲突 → **优先改旗号**；不得为防重牺牲大地名主标（§2.4）；可 ±1–2 km 微调，禁大挪 |

**示例**：

- **长安** — 多朝国都；唐占长安，大顺改渭南。
- **皖城** — 隋唐舒州治所 → 旗号 **舒**（州名级）。
- **太昭** — 工布地区核心地名；工布旗号 @ 太昭，近距小堡删留时留太昭。



### 2.3 势力搬迁后原据点安置（基本准则）

当势力从据点 A 搬迁到据点 B（或势力被删/换都）时，原据点 A **不得留空、不得保留已迁走势力的 factionId**。**严格按以下顺序**逐一排查，命中即停：

| 顺序 | 排查项 | 说明 |
|------|--------|------|
| **① 民族** | 此处是否有民族的起源地，或与该民族有特别深厚的渊源？ | 例：发源地、祖居地、核心活动区 |
| **② 政权** | 历史上此处是否建立过政权（国都/割据中心）？ | 排除已迁走势力；若该政权已被其他势力代表（如钟离→淮），则跳过 |
| **③ 家族** | 此处历史上是否有特别大的家族（士族门阀、地方豪强）？ | 须史籍可考；家族名如琅琊王氏→王、兰陵萧氏→萧（按 §4.1 家族类） |
| **④ 州郡治所** | 此处历史上是否是州/郡的治所？ | 取该州/郡为势力名（例：濠州治所→濠） |
| **⑤ 州名取字** | 此处历史上叫什么州？ | 取州名第一个字（例：濠州→濠）；若第一步不重名则直接用 |
| **⑥ 新建真实势力** | 以上全无 | **新建一个符合史实的真实势力**（按该坐标可考身份/相邻据点立势），绝不用 panjun |

**禁止**：搬迁后原据点 factionId 留为已迁走势力、留空、或随意塞一个无关势力。
**注意**：排查范围包含该坐标及周边可考区域（如凤阳一带含蚌埠、钟离故地等）。

### 2.4 史地安置与防审计瞎改（2026-06-20 立，必读）

> **主人红线**：最怕 **瞎安置**——为跑通审计、凑数量、消红线而改大地名、乱挂势力/精锐/武将，且**无史地相关性**。

**铁律**：**史料相关性 > 玩家地理直觉 > 审计零违规。** 审计报红但无合规史料解 → **停手问主人**，禁止自作聪明。

| 原则 | 说明 |
|------|------|
| **三相关** | 据点（坐标+旧称）、势力/旗号（§4.1 该地身份）、精锐/武将（§12 锚点）各自须有可考依据；**三者不必同代，但须同地可源或地缘合理** |
| **大地名不动** | 奴儿干城、长安、开封等**区域主标**禁止仅为防重改成庙街、衙门通称、低知名度小地名 |
| **防重顺序** | ① **改旗号**（例：奴儿干城 + 旗面 `都卫`）② 失利方第二/第三首都 ③ 同地换**更知名**旧称 ④ 主人裁定 |
| **宁缺毋滥** | 无城不挂势、无据不挂旗；**不因「宁缺毋滥」给已立据点留空武将/精锐**——无现成记录就按该据点史地身份/名气/相关性选配一个（编一个）。**审计通过不是改数据的理由**，禁止为跑审计乱挂无史地相关性的将/精 |

**反面教材（禁止再犯）**：奴儿干城 + 旗号「奴儿干」触防重 → ❌ 据点名改「庙街」；✅ 据点名仍 **奴儿干城**，旗面改 **都卫**（奴儿干都司）。

细则见 `.cursor/rules/no-arbitrary-placement.mdc`。

---

## 三、玩家可见 vs 代码内部（极重要）

### 据点（点）vs 势力（片）— 先分清再动手

| | 含义 | 数据 |
|---|---|---|
| **据点** | **一个点**的地点专名（城邑、关隘、要塞、聚落、圣地、遗址、战场、水源或自然地标等） | `cities_v2.name` |
| **势力** | **这一片**的身份名（政权/民族/家族/郡州等） | `factions.ts` 的 `name` |
| **旗号** | 旗面 1–2 字简称 | `SandboxDisplayNames` |

**据点 ≠ 势力，也不等于城市。** 据点须是坐标上真实可考的一个地点；人造据点须有冷兵器时代依据，自然地标须有古代命名、人类活动或历史事件依据。禁止用势力片名顶替点名，详见 `.cursor/rules/city-point-vs-faction-area.mdc`。

### 玩家眼里只能看到 2 样东西（平时）

1. **旗号** — 旗帜上的 1-2 个汉字（例：`汉`、`魏`、`德川`、`大清`）
2. **据点名** — 地图上的地点专名（一个点；例：`洛阳`、`潼关`、`伊瓜苏`）

**远征跟拍时多第 3 样**（见 `docs/AGENTS/expedition-elite.md`）：跟拍军团改名后的 **远征番号**（3–6 字专名，如 `丹阳兵`、`督标秦军`）。  
→ 旗号、据点名、番号 **必须是三个不同的专名**（番号与据点名可共享汉字，见 `docs/AGENTS/expedition-elite.md` §12.1），详见该文件「三者防重」——**写进数据前必查，禁止再犯**。

代码内部的 `factionId`（如 `'edo'`）、`factionName`（如 `'德川'`）、`cityId`（如 `'city_edo'`）**玩家完全看不见**。

### 势力名称 vs 旗号（必读，二者不可混）

| 字段 | 文件 | 规则 | 示例 |
|------|------|------|------|
| **旗号** | `SandboxDisplayNames.ts` / `CityAssetManager.ts` | 玩家旗面汉字，**1–2 字**；按 §4.1 定；**禁「州」「国」字**（§4.0） | `壮`、`祖`、`宣`、`宿` |
| **势力名称** | `factions.ts` 的 `name` | **禁止单字**；须为完整史地称呼（**片**上的民族/家族/州郡/政权等） | `壮族`、`祖氏`、`宣州` |
| **据点名** | `cities_v2.ts` 的 `name` | **一个点**的真实地点专名；人造据点须有冷兵器时代依据，自然地标须有古代史地依据；全局不重复 | `田阳`、`潼关`、`伊瓜苏` |

**禁止**：`factions.ts` 写 `name: '壮'`、`name: '祖'`、`name: '宣'` 等单字势力名——单字只出现在**旗号**，势力名用 **壮族 / 祖氏 / 宣州** 等全称。

**正确对照**：

| factionId | 势力名称（factions.ts） | 旗号（SANDBOX_DISPLAY_NAMES） | 据点名 |
|-----------|-------------------------|-------------------------------|--------|
| `zhuang_d` | **壮族** | **壮** | 田阳 |
| `zu_d` | **祖氏** | **祖** | 宁远 |
| `danyang` | **宣州** | **宣** | 鸠兹 |
| `xu` | **徐国** | **徐** | 下邳 |
| `suzhou_d` | **宿州** | **宿** | 符离 |

**旗号禁字（旗面专用，与势力全名无关）**：写入 `SandboxDisplayNames` / `CityAssetManager` 的旗号汉字 **不得含「州」「国」**——势力名可写「宿州」「徐国」（`factions.ts`），旗面只写 **`宿`**、**`徐`**。详见 §4.0「旗号禁字红线」。

### 工作流：以据点为主（极重要）

**只问据点，不问势力。** 改据点归属时若原势力失城 → 直接删势力（5 文件同步），不要问「XX 势力怎么处理」。
**坐标不动。** 除非用户明确说「请修改坐标」，否则 lat/lng 一律不改。只列出坐标不等于指示修改。

**先有点，再挂旗；据点名本位，一地多势可源（§2.2）。** 维护顺序永远是：

1. **据点** — 地图上的真实历史地名 + 可考坐标（`cities_v2` 里的 `name` + `lat/lng`）**已经存在**；删留、间距冲突时**以据点名名气大为保留基础**；
2. **史地身份清单** — 在该坐标/地名上，盘点民族、政权、家族、州郡等；**有 §4.1 可考依据者才可立项**为独立势力（多势可源 = 立项来源，不是一城多旗；争同一坐标时名气大者占点，其余第二/第三首都）；
3. **旗号** — 为**每一个**已立项势力，按其史地身份在 §4.1 五级优先级**向下试**，定 1 面旗（1–2 汉字）；
4. **代码绑定** — `factionId` / `StartingCapitals` 只是实现「1 点 1 旗」的索引，**不是**策划入口。

| ✅ 正确思路 | ❌ 错误思路 |
|---|---|
| 皖城这个点 → 隋唐舒州治所 → 挂旗 **舒** | 先有一个叫「皖」的势力 → 找座城绑上去 |
| 商邑这个点 → 商州治所 → 挂旗 **上洛**（避与「商邑」重字） | 先定商州势力 → 把长安/别的城改名叫商邑 |
| 悬瓠这个点 → 袁氏大本营 → 挂旗 **袁** | 先收录袁氏 → 随便找个城当首都 |

**禁止**：为凑齐 `factions.ts` 里的空势力而**挪据点坐标**、**随意改据点名**、或**把势力硬绑到不符合史实的城**。  
（**例外**：旗号防重、换更知名 **冷兵器时代**城名旧称而改据点名 → 允许，见 §2.2「据点不动」表。）  
若某 `factionId` 找不到合格据点 → **删势力或留空**，不得反向污染据点。

**冲突时**：**坐标不动**（lat/lng 不改）；据点名非必要不改（防重、换更知名旧称除外）。争的是**谁能挂这面旗**——知名度低者让位，改挂该势力历史上**第二 / 第三**据点的旗（仍须可考、≥ 50 km），而不是把大城坐标挪给弱势力。

### 重复判定规则

| 字段 | 判重规则 |
|---|---|
| **旗号汉字** | 字符串**严格相等** = 同一势力 |
| **据点名汉字** | 字符串**严格相等** = 同一据点 |
| `factionName` / `factionId` / `cityId` | **不参与判重**（只是代码标识） |
| 坐标 lat/lng | **不参与判重**（同一地点可能古今多名） |

### 示例

| 情境 | 判定 |
|---|---|
| 旗号 `德川` vs 旗号 `德川` | ✅ 同一势力（即便代码内 factionName 不同） |
| 旗号 `德川` vs 旗号 `德川幕府` | ❌ 不同势力（玩家眼里也是两个不同的旗） |
| 据点 `酒泉` vs 据点 `酒泉城` | ❌ 不同据点（玩家眼里是两个不同地名） |
| 据点 `北京` vs 据点 `北京` | ✅ 同一据点 |

**FactionEditor 必须严格按此判重，不做"包含/被包含"的模糊匹配。**


---

## 四、势力名称定名规则——【细则已外置】

> **外置**：完整规则见 **`docs/AGENTS/faction-naming.md`**（4.0 定名算法、4.1 五级优先级、4.2–4.8 各类型细则与示例表）。
> 定旗号/势力名相关任务**必须先读该文件**。

**核心红线（摘要）**：

1. **遍历单位 = 据点**（`cities_v2` 每个点），不是 `factions.ts` 表；从 §4.1 优先级 1→5 试，命中即停。
2. **旗号禁字**：旗面 1–2 字**不得含「州」「国」**（宿州→`宿`、黎国→`黎`）；势力全名不受限。
3. **旗号全局唯一**：字符串严格相等 = 同一势力；旗号 ↔ 据点名仅禁**严格相等**（共享汉字合法：井陉关+`井陉`）。
4. **后缀禁令**：族/人/国/朝/氏/家 剥除（苗族→`苗`、袁氏→`袁`）；史籍整词专名不截字（`月氏`≠`月`）。
5. **绝不用 panjun**：无 1–5 级可考依据，也要**新建一个符合史实的真实势力**（按该坐标可考身份/相邻据点立势），绝不落叛军旗。
## 五、5 文件一致性——【细则已外置】

> **外置**：完整规则见 **`docs/AGENTS/five-file-sync.md`**（5 文件清单、5.4 录入后验收步骤、5.5 骨架优先与势力 A/B/C 分级、录入阶梯）。
> 新增/修改/删除势力（5 文件同步）相关任务**必须先读该文件**。

**核心红线（摘要）**：

1. **5 文件必须同步**：`factions.ts` / `cities_v2.ts` / `StartingCapitals.ts` / `CityAssetManager.ts` / `SandboxDisplayNames.ts`；用 FactionEditor `/api/batch-import`（增/改）或 `/api/batch-delete`（删，自动清道路），**不要手动写 5 处**。
2. **⚠️ 首都映射铁律（血训）**：新建势力**必须**在 `StartingCapitals.ts` 写入首都映射（如 `'taizhou': 'city_hailing'`）——漏写 → 引擎把该据点强制降格给 `panjun` → 空旗不显示（海陵城血训，绝对禁止再犯）。
3. **录入顺序铁律（5.5）**：先定大中城 → 铺民族/家族密度 → 投名将/精锐；**禁止**为小势力腾位而改大中城坐标、据点名或旗号。
4. **势力 A/B/C/D 分级**：A（大城/国都/区中心，冻结，应有将精 T0–T1）、B（中城/州郡治，稳定，建议将精）、C（民族/小家族/关隘，可增，仍须配将/精，禁借将借精）、D（以上未覆盖者，一律满配将/精，禁止 panjun）。
5. **改完必跑**（硬缺口须为 0）：`npm run add:check -- <据点名>` / `faction:sync-audit` / `expedition:triple-check`（有精锐时）/ `city:spacing`（新坐标时）/ `city:dossier`。


---

## 六、大城标准（big_city）

> **大城（big_city）唯一标准（2026-08-03 主人修订）**：**17 世纪前历史人口 > 50 万（边缘模糊、约 50 万左右按大城算）。** 无固定数量，达标即可。


### 6.1 中城标准（medium_city）

> **中城（medium_city）唯一标准（2026-08-03 主人修订）**：**17 世纪前历史人口 > 10 万（边缘模糊、10 万左右按中城算）。** 无固定数量，达标即可。

> 中城是区域性重要城市，大城是跨朝代全国性地标。

### 6.2 🔴 据点等级总判据（2026-09-11 主人定死，**永远按规则办，不许擅自做主**）

> **主人原话（2026-09-11）**：
> 「据点先区分城市和险要，关隘，要塞，堡垒为 PASS。其他为城市，
>   人口大于 50 万左右为大城，大于 10 万左右为中城，大于 1 万左右为小城，以下为城寨。」

**判级两步走，顺序不可颠倒**：

| 步 | 判据 | 结果 |
|---|---|---|
| **① 先分性质** | 是**关隘 / 要塞 / 堡垒**（军事据点，不是城） | → **`pass`（险要）** |
| | **其他一切**（城、王都、圣城、寺院城、港口、聚落…） | → **城市**，进第 ② 步 |
| **② 城市按人口** | 17 世纪前城居人口 **> 50 万**（约 50 万也算） | → **`big_city` 大城** |
| | **> 10 万**（约 10 万也算） | → **`medium_city` 中城** |
| | **> 1 万** | → **`small_city` 小城** |
| | **< 1 万** | → **`stockade` 城寨** |

**配套铁律（同为 2026-09-11 主人定）**：

1. **有特殊建筑（城内奇观 `CITY_WONDER` / `CITY_WONDER_EXTRA`）的据点一律是城市，不得为城寨** ——
   它们是有奇观/宗堡/王城的城或圣地，不是军寨。其等级仍按上表人口档定（不得低于小城）。
2. **等级一律照规则算，不许 AI 自定标准、不许逐座"我觉得"**；规则没覆盖的才问主人。
3. 判级的**依据（人口估计 + 出处）必须留在 `note` 里**，与既有惯例一致
   （例：`吴哥…盛期大吴哥人口量级常估≥50万，升 big_city`／`阿瑜陀耶…1600年前人口约15–25万，未达大城50万门槛，降 medium_city`）。
4. 改 `type` 时遵守「修改隔离规则」：改大城可动其他层级，改中城不准动大城，
   改小城不准动关隘/中城/大城；坐标、势力、武将、精锐、立绘、道路一律不碰。

## 七、15 区 × 15 文化中心（2026-05-30 立）

每个文化区有 **1 个核心城**（中原为洛阳），共 **15 个**。

**单一真理来源**：`src/systems/RegionSystem.ts` 的 `REGION_CENTERS` 常量。

| 区域 | 中心 | cityId | 现等级 | 历史身份 |
|---|---|---|---|---|
| 中原 | 洛阳 | `city_luoyang` | big_city | 9 朝古都, 中原 |
| 北方 | 北京 | `city_beijing` | big_city | 元明清 700 年都 |
| 江南 | 南京 | `city_nanjing` | big_city | 六朝古都 + 明初 |
| 川蜀 | 成都 | `city_chengdu` | big_city | 蜀汉/前后蜀 92 年都 |
| 岭南 | 番禺 | `city_panyu` | big_city | 广州古名, 南越国都 |
| 河西 | 姑臧 | `city_wuwei` | medium | 五凉故都 (即凉州/武威) |
| 草原 | 哈拉和林 | `city_karakorum` | small | 蒙古帝国首都 (2026-08-03 人口审查降级) |
| 东北 | 龙泉府 | `city_ningan` | medium | 渤海上京 170 年都 |
| 朝鲜 | 开城 | `city_kaesong` | medium | 高丽王朝 475 年都 |
| 日本 | 京都 | `city_kyoto` | big_city | 平安京 1075 年都 |
| 西域 | 伊逻卢城 | `city_yiluolucheng` | small | 龟兹国都 (延城) (2026-08-03 人口审查降级) |
| 青藏 | 逻些 | `city_luoxie` | small | 吐蕃国都 (即拉萨) (2026-08-03 人口审查降级) |
| 滇缅 | 蒲甘 | `city_pagan` | medium | 蒲甘王朝都城；远征中心（`RegionSystem` 2026-06-14 定；`city_ayutthaya` 仍为大城但非区中心） |
| 中亚 | 玉龙杰赤 | `city_urgench` | small | 花剌子模都城 (主人 2026-07-05 改, 原撒马尔罕) |

### 规则

| 规则 | 说明 |
|---|---|
| **区中心 ≠ 大城** | 两套独立标签 (6 个中心仅是 medium_city, 但仍是文化-政治核心) |
| **不许擅自改变 cityId** | 增减区中心需项目主人显式同意 |
| **改动必须三处同步** | RegionSystem.ts + cities.ts §7 + 本节表格 |
| **道路骨架基于此** | 未来同区据点向中心连接, 区中心之间互连成骨干 |

## 九、其他不容妥协的细则

- 据点最小间距 50 km；< 50 km 优先删小留大、必要时可 ±1–2 km 微调（§2.1）、旗号双字渲染、坐标真实严肃性等
- 全部参见 `src/data/cities.ts` 顶部 1-5 条注释


---

## 十、地图旗号与势力色块（2026-06-01 立，AI 勿擅自“优化”）——【细则已外置】

> **外置**：完整规则见 **`docs/AGENTS/flag-rendering.md`**（10.1–10.5 全部细节、启动加载顺序、自检清单）。
> 动 `CityAssetManager` / `CityManager` 旗号或领土**必须先读该文件**。

**核心红线（摘要）**：

1. **势力色块默认关闭**：`territoryLayerVisible === false`，启动禁止全图领土重绘（曾卡死主线程）。
2. **势力色唯一来源**：每局 `FactionManager.addFaction` 随机 HSL → 旗帜/军队/领土同色；`factions.ts` 不写 hex。
3. **叛军旗 = 7–58 共 52 面**，禁止截断（panjunRebelMax 勿恢复）；叛军走 `flag-rebel-{index}`，不渲染汉字。
4. **旗号黑白字唯一阈值** `FLAG_TEXT_LUM_THRESHOLD = 128`：深旗白字 / 浅旗黑字；例外名单（岳/汉）见细则。
5. **启动性能**：chromaKey 逐个 + `yieldSchedulingSlice`，禁 `Promise.all` 并行抠图；占位旗必须用**已抠绿**模板。
## 十一、AI 工作流推荐

1. 查 `AGENTS.md`（本文件，含顶部「规则地图」索引） + `docs/GAME_DIRECTION.md`（玩法/战斗/观战 UI 方向基准） + `src/data/cities.ts` 头部约定 + **旗号/领土规则（`docs/AGENTS/flag-rendering.md`）**
2. **查据点全档（文化区/势力/武将/精锐）** → `npm run city:lookup -- 龙门`（单点）；`npm run city:dossier` 刷新 `scratch/city_dossier.json`（674 座机器可读索引）
3. **据点间距（最先于增删城）** → `npm run city:spacing`（§2.1 ≥50 km 硬顶；拟新建 `--probe`）
4. **文化区中心审计（先于增势）** → `npm run skeleton:audit`（15 区中心；见 `docs/AGENTS/five-file-sync.md` §5.5）
5. **文化区据点密度审计** → 读 `docs/02-design/world/REGION_PLAYABLE_AREA.md`，运行 `npx tsx scratch/region_playable_area.mjs`（两层审计；**增补优先、删留仅硬伤**；**日本刚精简勿再删**）
6. 任何对 5 文件的批量改动 → 用 FactionEditor 或现有 `/api/batch-*` 接口
7. 任何对道路网的改动 → 用 `VectorRoadEditor` 或对应脚本；**删据点必须同步删道路**（§2.1 删点连带）
8. 动 `CityAssetManager` / `CityManager` 旗号或领土 → 先对照 `docs/AGENTS/flag-rendering.md`，不确定问项目主人
9. 不确定历史出处 → **宁可不写**，不要编
10. **远征精锐**（`docs/AGENTS/expedition-elite.md`）：先据点→再势力→再番号；写入 `*ExpeditionLegions.ts` 后 **必须**跑 `npm run expedition:triple-check`，违规 **不得提交**
11. **战斗立绘**（`docs/AGENTS/portrait-allocation.md`）：文化夹随机 → 政权夹随机 → 名将专图→政权→文化；**禁止** `portraits/`、`avg/`；改池前先读 `portrait_defaults.ts`


---

## 十二、远征精锐（2026-06-11 立，AI 必读）——【细则已外置】

> **外置**：完整规则见 **`docs/AGENTS/expedition-elite.md`**（tier 五级判定表、录入步骤、审计命令、12.2.2 多将池）。
> 做「远征精锐/番号」相关任务**必须先读该文件**。

**核心红线（摘要）**：

1. **玩家可见三样东西必须是三个专名**：旗号 ≠ 据点名 ≠ 远征番号（任意两两字符串不得完全相同）。
   - 旗号 ↔ 据点名：允许共享汉字（旗号是简称，`潼津`+`潼关` 正常）；仅禁严格相等。
   - 旗号 ↔ 番号：默认不得共享；**仅民族/政权/家族**（§4.1 定为 1·民族、2·政权、4·家族）例外。
   - 番号 ↔ 据点名：允许共享汉字（`武川镇` + `武川镇军` ✅）。
2. **番号 3–6 字、全局不重复**；禁止用**人名**作势力/旗号（孙策挂丹阳兵 ❌）；禁止为精锐新建城池/迁坐标。
3. **唯一锚点 = 据点**：势力、据点、精锐、时代彼此无绑定——只问「这座据点（lat/lng + 史地名）」。
4. **tier 五级**（开战战力乘数）：T0 传奇 ×1.5 / T1 功勋 ×1.4 / T2 特色 ×1.3 / T3 风土 ×1.2 / T4 存在 ×1.1；从 T0 向下试，命中最高一级即定。T0 无数量配额，宁缺毋滥。
5. **审计必跑**：`npm run expedition:audit` + `npm run expedition:triple-check`（新增番号后必须零违规）。
## 十三、战斗立绘分配（三级规则 — 2026-06-21 定，AI 必读）——【细则已外置】

> **外置**：完整规则见 **`docs/AGENTS/portrait-allocation.md`**（13.1–13.6：三级优先级、素材目录约定、运行时分配、禁止清单、维护命令、立绘夹跟人物文化走）。
> 战斗立绘/立绘池相关任务**必须先读该文件**。

**核心红线（摘要）**：

1. **三级优先级（命中即停）**：① 文化区夹随机 → ② 政权专属夹随机 → ③ 专属人物（专图 → 政权夹 → 文化夹）。主人 2026-06-21 定案，**禁止**改优先级、恢复 `portraits/`/`avg/`、或因审计/404 删改 `FactionGenerals` 占位路径。
2. **废弃目录**：`portraits/`、`avg/portraits/`、`REGION_FIELD_PORTRAIT`——禁止恢复或新建引用。
3. **专图缺失不删表项**：`FactionGenerals.portrait` 是**预留路径**，文件不存在时走 13.1③ fallback（本政权夹 → 本文化夹），**禁止**删条目或改绑无关势力夹；「620 张专图未补齐」不是 bug。
4. **立绘夹 = 人物族群文化**：`FactionGenerals.portrait` 文件夹匹配**将领本人民族/文化归属**，不跟据点 RegionType（斛律光敕勒人 → STEPPE 夹）。
5. **审计**：`npm run portrait:folder-audit`（同夹 MD5 重复 PNG）；`portrait:folder-dedupe` 需主人确认。


---

_本文件以中文写作，方便项目维护者快速理解。日后版本若需扩充，遵循"约定先行"原则：先在此文件添加规则，再写代码实现。_

## 军团目标选择规则（2026-08-07 定稿，干净版，AI 必读）

> **主人定调**：军团选进攻目标走**干净逻辑**——锚点 + 直连路方向池。此前叠加的复杂层（行军惯性、锚点迟滞、回头路检查）**已全部删除，禁止恢复**。

**决策顺序（`FindTarget`，无目标时每帧执行）**：

| 序 | 环节 | 规则 |
|----|------|------|
| ① | **远征锁目标** | `expeditionTargetCityId` 非空 → 不重选、不回师，直至占领目标或全军覆没 |
| ② | **老家失守 → 收复** | `resolveRecaptureTarget` 最高优先：本军出发点被夺 → 先打回老家，打完再继续 |
| ③ | **野战追击** | 0.8°(≈89km) 内敌军团 → 优先追击（只**挂起**城目标，结束后恢复） |
| ④ | **选锚点** | 见下表 |
| ⑤ | **选目标** | `pickTarget`，见下表 |

**锚点规则**：

| 兵力 | 锚点 | 语义 |
|------|------|------|
| **< 2 万** | 本军出发点 `homeCityId` | 小军团以老家为圆心，蚕食本城周边 |
| **≥ 2 万** | 离军团**道路距离最近**的前线己方城（`resolveForwardAnchor`） | 打完的城自动变己方且离军团最近 → **锚点自动前移**，连续向外推进，不回老家 |

**目标规则（A/B 相同，`TargetEvaluator.pickTarget`）**：

1. **候选池 = 锚点每条直连路的最近 1 座敌城**（方向池 `buildDirectionalPool`；**无回头路检查**——打下的城即变己方，朝老家方向的敌城（如雷伊）同样是扩张目标）
2. **沿推进方向优先（<2 万，锚点=老家，军团已在前哨）**：方向池优先「从锚点出发的最短路径**经过军团所在城**」（`armyStandCityId` = FindTarget 里 `getNearestCityId(armyPos)` 算出）的目标——打完城沿推进方向继续蚕食，不绕回老家换方向（实测：底比斯打雅典胜后 100% 斯巴达）。**子集为空（支线尽头无推进目标）自然回落全池**，不剔除、不锁死单方向轴线
3. **排除绕回老家（≥2 万，锚点=前线城）**：排除「路径中段（不含起点）**经过老家**」的目标——否则打完城可能抽中「先回老家再出去」的方向（实测：雅典为锚点时萨洛尼卡路径 = 雅典→底比斯→萨洛尼卡，排除后 0%）
3.5. **「打不打得过」闸门（2026-08-12 主人批准）**：守军 > 本军兵力 × `ENEMY_STRENGTH_MAX_RATIO`(1.5) 的城不进抽签池；追击敌军团同判据（`pickNearbyEnemyLegion`）。**只挡主动挑衅**——被动接敌、收复老家、远征锁定目标一律不受限；**筛空自动回落全池**，绝不因四周全是硬骨头而卡死不动
3.6. **目标失效重抽 → 继承原方向（2026-08-20 主人定「刚路过己方据点又掉头，怪别扭」）**：
   目标城被友军抢占/消失（`friendly_or_missing`）而重抽时，优先保留「到**刚失效那座城**道路距离
   最近的一档」候选（≤ 最近距离 ×1.5），**筛空回落全池**。语义：本来就奔那个方向走了一半，
   目标没了就打那附近的，别当场调头奔反方向。
   🔴 **这不是被删的那三个机制**（见文末「已删除」）：不按「老家→当前位置」推进轴滤掉身后的城
   （行军惯性）、锚点照常前移（非锚点迟滞）、不比较到锚点的距离（非 0.9 倍回头路判据）。
   它**只在目标失效那一次重抽生效**，用完即清；正常打完城后的选目标完全不受影响，
   「朝老家方向的敌城同样是扩张目标」这条规则 1 依旧成立。

4. 池内有「全图据点最多的势力」的城 → **必打**（反雪球，2026-08-05 主人定，防最强势力滚雪球；池内无领头城则不回头）。**不受 3.5 否决**：领头城被摘掉时正常抽签，不强行送死
5. 否则池内**均匀随机抽一座**（多军团自然分散，不扎堆同一城）
6. 池空（锚点直连均无敌城）→ 兜底「从锚点出发道路距离最近的敌城」取最近 3 座随机

**目标失效处理（`HasTarget`，有目标时每帧检查）**：

- 目标城**被己方占领/消失** → 12s 冷却 + 清目标重抽（`friendly_or_missing`）
- 途中遇可打敌军团（追击走廊：敌距 ≤ 城距 ×1.0 且夹角 ≤ 45°）→ 挂起城目标改追击，结束后恢复原城

**冷却机制**：

| 冷却 | 时长 | 触发 | 效果 |
|------|------|------|------|
| 失败冷却 | 12s | 寻路失败 / 目标失效 | 该城 12s 内不再被选为目标 |

**已删除（勿恢复）**：
- **越城而走（str_21）**：已于 2026-08-17 依主人指令彻底退役删除（防卡死/寻路死锁）。
- 行军惯性（`pickByMarchInertia`）、锚点迟滞（`resolveStickyAnchor` / `marchAnchorCityId`）、方向池回头路检查（0.9 倍判据）。

## 军团回援与收复规则（2026-07-11 立，AI 必读）

> **项目主人定调**：本节为军团回援/收复的**唯一真理**。代码注释历史上有胡乱瞎写，**只作参考，一律以本节为准**。

**三条铁律（主人 2026-07-11 口述定稿）**：

1. **战斗中不脱离** —— 任何军团处于**战斗状态（交战中）**时，不得中止战斗、不得回援。行为树根节点 `IsInCombat` 最高优先级保证。
2. **只有非战斗状态才能回援** —— 在路上行军、待命均可回援；**排队攻城（尚未开打）算「在路上」，可回援**；只有真正进入交战才算「战斗」、才不可回。
3. **本城必须真打起来才回援** —— 本城（军团出发城 `homeCityId`）**发生攻城战（真打起来）**才触发回援。敌军仅在途 / 迫近（哪怕已到城下 30 km）**不算、不触发**。野战不算「本城发生战斗」。

**判定口径（关键）**：

- 回援判定一律用严格口径 **`LegionManager.isCityBeingSieged`**（= `SiegeManager.hasActiveSiegeAt`，只认进行中的攻城战）。
- **不得**用宽口径 `isCityUnderAttack`（含敌军在途/迫近/排队）—— 那个仅供募兵等通用判定，改动回援时**禁止牵连**。

**城破后（第二套逻辑，独立于回援）**：

- 本城**已失守（易主）→ 转「收复」**：据点军团选目标时**最优先**打回老家（`resolveRecaptureTarget`），优先于追击敌军团、方向池抽签。这不是回援，是收复。

**边界**：

- **远征军团全豁免（主人 2026-07-31 定）** —— **只要出征，就不回头**：家城被攻城不回援、家城失守也不回师收复，远征不取消，直至占领目标城或全军覆没。
  - ⚠️ **禁止**再加回任何「远征军回救本城」的分支：症状是军团在远征目标与老家之间**来回徘徊**，目标永远打不到。
- **只救本军出发城** —— 回援 / 收复只针对本军 `homeCityId`，**不救本势力其他城**；其他城的战斗仅当军团恰好在开战圈（`BATTLE_JOIN_RADIUS` ≈ 30 km）内路过才被动卷入。
- **后果认账** —— 严格执行后，深入敌境连续攻城的军团多半正卡在战斗里，回援触发会明显变少。这是规则的必然推论，**不是 bug**。

**行为树根节点顺序（即优先级，`createLegionBehaviorTree`）**：

`IsInCombat` → `IsWaitingSiege` → `reinforceHome` → `IsPostBattleResting` → `retreatWeakLegion` → `attackSequence` → `Idle`

- `IsWaitingSiege` 排在回援前**不违反铁律 2**：它内部 `homeNeedsHelp` 命中时会主动退出排队并返回 false，让位给回援/收复。
- `IsPostBattleResting` 必须排在 `reinforceHome` **之后**（2026-08-05 调整）——休整本质是「待命」，按铁律 2 可回援；
  排在前面会让休整中的军团眼看老家被攻城而不动。

### 追击打断攻城 = **暂停**，不是改主意（2026-08-06 修，别再改回去）

> 症状：军团走一半掉头折返。**根因不是随机抽签坏，是追击把攻城目标写穿了。**

旧行为：`setStrategicArmyTarget` 会把 `strategicTargetCityId` 置 null。于是追击一失败（跑出 1.1° / 目标失效 / 对方交战卡 45s 超时），军团就「失忆」，只能进 `FindTarget` 重抽 —— 原目标虽还在池里，但等概率抽签有很大机会抽到背后方向，**当场掉头**。

现行语义：

1. **挂起而非清空** —— `setStrategicArmyTarget` 只写 `strategicTargetArmyId`，**不动** `strategicTargetCityId`。
2. **追击结束只清追击** —— 一律走 `clearHuntTarget`（清 armyId + 把 `targetPosition` 恢复到挂起城），**不许**在追击出口调 `clearStrategicTarget`。四个出口全覆盖：失效 / 出圈 / 交战超时 / 开战接触。
3. **`HasTarget` 追击结束后往下落** —— 不许直接 `return false` 进 `FindTarget`；落到下方城目标检查，接着赶原城。
4. **`AbandonTarget` 在追击中只弃追击** —— 保留挂起城，下一 tick 重新寻路。

配套三条（都为「少一次不必要的重抽 / 重抽时别往回走」）：

| 机制 | 位置 | 要点 |
|------|------|------|
| 行军惯性 | `TargetEvaluator.pickByMarchInertia` | 半路重抽时按「老家→当前位置」推进轴滤掉身后的城再取最近。**实测旧的等概率抽签有 34.6% 抽中身后城**（`scratch/verify_march_inertia.mts`）。出头鸟 LEADER_HUNT 仍优先于惯性 |
| 锚点迟滞 | `LegionBehaviors.resolveStickyAnchor`（GAIN=0.8） | 旧锚点仍是己方且路网可达就留着，除非新候选 ≤80% 距离。**⚠️ 不许改成「开拔后钉死出发城」**——深入敌境的军团锚点钉在几百公里外老家，方向池会算成老家周边敌城，反把军团往回拉，比抖动更糟 |
| 野战冷却续期 | `IsInCombat` + `ctx.postBattleFoeArmyId` | `FAILED_TARGET_COOLDOWN_MS` 只有 12s，双将野战 30s+，开战盖一次章仗没打完就过期、脱战瞬间再追同一支。故交战中每帧续期，冷却实际从**脱战那刻**起算 |

**P2「收紧追击触发」= 已实装（2026-08-06 晚，主人拍板「我没说不做」）。** 原「0.8° 内任何可打敌军都打断攻城」改为**追击走廊过滤**：只有挡在行军方向上的敌军才打断攻城。判据（`LegionBehaviors.isEnemyInMarchCorridor`，两个同时满足）：① 敌距 ≤ 城距 × `HUNT_MAX_DETOUR_RATIO`（1.0——不追比目标城还远的，城后面的排除）② 「我→敌」与「我→城」方向夹角 ≤ `HUNT_CORRIDOR_HALF_ANGLE_DEG`（45°——侧翼/身后排除）。只作用于 `HasTarget` 打断路径；`FindTarget` 主动追击（无城目标时）不过滤。**🔴 身后例外（2026-09-04 主人「前面的舰队应该掉头攻击后面的舰队」）**：敌人在行军方向「身后」（与目标城方向夹角 >90°）且不绕远路（≤城距 = `HUNT_MAX_DETOUR_RATIO`）时，前方军团应**掉头迎战**，不再按走廊跳过；侧翼(45~90°)仍排除（保住「拉扯」保护），贴身(<0.3° = `HUNT_CLOSE_EXCEPTION_RADIUS`)仍放行。实现见 `pickNearbyEnemyLegion` 的 `directionAngleDeg` 判断。`retarget_hunt` 日志的 `detour=`/`偏离 xx km` 埋点保留，被走廊挡掉的敌军记 `skip_hunt:*`。验证：`scratch/verify_hunt_corridor.mts`（判据 9 场景 + HasTarget 集成 4 场景全绿 + tsc + build）。

**诊断日志键**：`retarget_hunt:*`（被拉走，带 detour）、`abandon:*` / `pick:*`（重抽）、`hunt_blocked_timeout:*`（等脱战超时）。若又见折返，先看 `pick:*` 是否出现在**没有** `retarget_hunt` 的场景 —— 那就不是追击这条链了。

**代码锚点**：

| 环节 | 位置 |
|------|------|
| 严格判定 / 宽判定 | `src/legion/LegionManager.ts` → `isCityBeingSieged` / `isCityUnderAttack` |
| 回援触发 | `src/ai/bt/LegionBehaviors.ts` → `reinforceHome` / `IsHomeUnderAttack` / `homeNeedsHelp` |
| 城破收复 | `src/ai/TargetAnchorResolver.ts` → `resolveRecaptureTarget` |
| 家城失守判据（**唯一**） | `src/ai/TargetAnchorResolver.ts` → `isHomeCityLost` |
| 远征收尾（仅"目标已属己方=功成"一种） | `src/ai/bt/LegionBehaviors.ts` → `resolveExpeditionState` |
| 攻城目标挂起 / 恢复 | `src/ai/bt/BtDecisionLog.ts` → `setStrategicArmyTarget` / `clearHuntTarget` |
| 六计硬分开 | `src/combat/BattleField.ts` → `assignSituationalSkills` / `pickSkillAvoidingUsedSixClass` |
| 远征发起（仅看兵力 ≥ `EXPEDITION.UNLOCK_TROOPS` = 5 万） | `src/ui/ExpeditionUI.ts` → `eligibleArmy` |

## 立绘独有文件保护（铁律，2026-06-27 立）

- **不重复的立绘一个不删**—— 含 `_prev`、时间戳等后缀的孤儿文件，只要内容与正式引用不重复，**禁止删除**。
- 清理前必须 SHA-256 比对：与 FactionGenerals 中任一正式立绘内容相同 → 可删；否则绝对不动。
- 主人作图不易，删了就白做。

## 立绘保底规则（铁律，2026-06-27 立）

- 任何一张图，无论游戏中是否被引用，**必须保底留一份**。
- 孤儿之间互相重复 → 删 N-1，留 1。
- 孤儿与正式引用重复 → 可全删（正式版已在用）。
- 孤儿独有且无重复 → **绝对不删**，哪怕游戏里永远用不到。
- 交叉比对范围：孤儿 vs 正式引用 + 孤儿 vs 孤儿，两者缺一不可。

## 生产部署与开发工具隔离规则（极重要）

> **⚠️ 严禁将本地开发工具暴露至线上生产环境（Production Build）⚠️**

项目中包含许多仅供本地开发和数据维护使用的工具面板与页面。**任何 AI 在维护或新增此类功能时，必须严格遵守以下隔离标准，绝不允许打包上传或暴露到最终玩家可见的线上版本中**：

1. **独立页面隔离**：
   - atch-manager.html（批量管理面板）
   - portrait-tuner.html（立绘调校面板）
   - 等其他根目录下的本地管理 HTML。
   **标准**：这些页面绝对不准加入 Vite 的 uild.rollupOptions.input，也不准在 index.html 中留下任何能跳转过去的代码入口，确保 
pm run build 打包后的 /dist 目录中彻底没有这些文件。

2. **游戏内开发功能隔离（代码剔除）**：
   - **F2 立绘校正功能**及所有涉及本地写盘（s.writeFileSync / /api/save-*）的快捷键。
   - **右侧调试面板**（Debug Panel）及所有的修改按钮。
   **标准**：所有在游戏运行时的开发功能入口，必须被包裹在 if (import.meta.env.DEV) { ... } 块中。这样生产构建时，Vite (Rollup) 会将其视为 Dead Code 彻底剥离，确保线上版本的玩家绝对无法通过按键或控制台唤出调试器。

## 战斗面板字数规范（2026-07-16 立；2026-08-06 精简）

> 状态链 2 字（好运/持重/攻计/擅攻/造势）与效果副文字 3 字已随 07-17「四字卡」重构废弃（卡片只显四字名，无副行）。仅存：

| 位置 | 字数 | 示例 |
|------|------|------|
| **技能名**（按钮主文字） | **4 字** | 金城汤池、摧锋弑君、倡义靖乱 |

**执行铁律**：如果玩家要求“部署”、“推送”或“上传”，**不要**擅自去修改 vite.config.ts 把这些调试面板加进线上版本；它们的存在本身就是为了“本地专用（DEV ONLY）”，切记！

## 战斗面板部队显示规则（2026-08-04 主人立，AI 必读）

> **背景**：AI 当天两次改动把"守军/援军"显示搞乱（先多单位名挤一行，被主人否）。**规矩在 `docs/GAME_DIRECTION.md` L570「列出主力 + 各路援军名（过多时前几个 + 「余 X 部」）」早就写了，AI 没先看规则就动手。以下为铁律，改战斗面板前必读。**

| # | 规则 | 说明 |
|---|------|------|
| 1 | **一行只显示一支部队** | 第二行 = 文字主位**一支**（守方钉据点城防，攻方钉波次 0）；其余由第三行援军**逐行**列出。**禁止**用 `·`/`•` 把多支部队挤进同一行 |
| 2 | **名字兵力必须对应** | 第二行兵力徽章 = **`pickSideNameUnit` 选中部队自己的兵力**，**不是**全队总和（`getInfo().defenderTroops`）。否则出现"名字完山虎贲、数字 8394=全队总和"的错位 |
| 3 | **援军行逐行列出** | 其余部队每行一支（名字+各自兵力）；超过 3 支时显示前 3 个 + 「余 X 部」 |
| 4 | **援军行排除与第二行文字同源** | `updateReinforcements` 只剔除 `pickSideNameUnit`（第二行文字主位）；**禁止**按立绘主位（`pickArrivalDisplayUnit`）剔除——援军将可占立绘/标签，名字仍留第三行 |
| 5 | **中央拉锯条 = 总兵力** | 中央主条仍用双方总兵力比（引擎判定口径），与侧栏第二行兵力（单部队）并存，不算不一致 |
| 6 | **立绘/标签与第二行解耦** | 立绘与状态标签走 `pickPortraitTagUnit`（带将 > 精锐 > 波次；本城有将不换）；第二行队名永不因武将援军篡位 |

**代码锚点**：`CombatUI.ts` → `pickSideNameUnit` / `buildWaveGroupedSideName`（第二行）、`pickPortraitTagUnit`（立绘/标签）、`updateReinforcements`（第三行剔除文字主位 + `MAX_REINF_LINES = 3` + 余 X 部）。
