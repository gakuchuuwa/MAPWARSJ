---
description: 玩家可见文案：势力 vs 旗号，禁止混用
alwaysApply: true
---

# 势力 ≠ 旗号（玩家可见层）

对话、军情/事件面板文案、注释说明玩家所见内容时，**必须区分**：

| 术语 | 含义 | 数据来源 | 示例 |
|------|------|----------|------|
| **势力** | 势力全名（军情/叙事里说的「谁」） | `FactionManager.getFactionName()` → `factions.ts` 的 `name` | 占城**国**、大**辽**、难兜 |
| **旗号** | 旗面上渲染的 1–2 汉字 | `SANDBOX_DISPLAY_NAMES` / `CityAssetManager.getProcessedFlagText` | 占城、辽 |
| **据点** | 地图城名 | `cities_v2.name` | 阇槃、建业 |

## 军情 feed 格式（复国 / 覆没 / 灭亡）

```
{年季}  {势力}  于  {据点}  复国
```

- 中间字段写 **势力**，不写旗号。
- 示例：`秦始34年 春  占城国  于  阇槃  复国`（不是「占城 于 阇槃」）。

## 禁止

- 把军情/事件里的 `getFactionName()` 结果称为「旗号」。
- 用 `SANDBOX_DISPLAY_NAMES` 短名充当军情里的「势力」——除非产品明确要求改代码。
