---
description: 据点间距≥50km硬顶；禁止 AI 擅自新建 cities_v2 条目
alwaysApply: true
---

# 据点间距 ≥50 km — 最最基础铁律

## 做什么

1. **改 `cities_v2.ts` 之前**：`npm run city:spacing`（必须 exit 0）
2. **改完之后**：再跑一遍；仍须 exit 0
3. **拟新增/改坐标**：先探测  
   `npx tsx scratch/audit_city_spacing.mjs --probe <lat> <lng> <据点名>`  
   输出含 ❌ → **不写**；仅 ⚠️ 卡线（50–55 km）→ **须问项目主人**，AI 不得自行新建

## 禁止

- **未经项目主人明确同意，新增 `cities_v2` 条目**（新 `city_*`、新 lat/lng）
- 为腾势力、挂将领/精锐而**发明新锚点**
- 在密集区**卡 50–52 km 凑数**建城
- 用链式改 `factionId` 波及多势力却不跑间距审计
- 用 ±1–2 km 微调规避审计（除非 AGENTS §2.1 主人已裁定）

## 势力搬迁（只许已有城）

- 1 势力 = 1 据点；搬迁**只能**改绑**已有** `cityId`
- 找不到 ≥50 km 的已有城 → **停手问主人**，不得新建

## 失败时

- 审计不通过 → **不写入**；告诉主人哪两城 <50 km
- 不得 git 回滚；不得读 VSCode History / `.bak`

## 报告格式（改据点后必附）

```
npm run city:spacing → 违规 N 对 / ✅0
（若动 cities_v2）改前改后各跑一次
```
