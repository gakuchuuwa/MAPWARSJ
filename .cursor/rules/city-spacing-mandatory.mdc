---
description: 新建据点须 50–500 km 间距窗口；禁止 AI 擅自新建 cities_v2 条目
alwaysApply: true
---

# 新建据点间距 50–500 km — 最最基础铁律

## 硬顶（新建 / 改坐标前必查）

对**拟新增**或**拟改 lat/lng** 的坐标，与全图已有据点（Haversine）须同时满足：

| 条件 | 阈值 | 说明 |
|------|------|------|
| **下限** | **≥ 50 km** | 与**每一座**已有据点均须 ≥50 km；任一 <50 → **禁止写入** |
| **上限** | **≤ 500 km** | 与**最近**已有据点须 ≤500 km；>500 → **孤悬过远，禁止新建** |

合法窗口：**最近邻距离 ∈ [50 km, 500 km]**，且不得与任意城 <50 km。

> 全图存量城对之间可以 >500 km（边陲正常）；**500 km 上限仅约束新建点与最近邻**，不是要求任意两城都 <500。

## 做什么（顺序不可跳）

1. **未经项目主人明确同意，禁止新增 `cities_v2` 条目**（新 `city_*`）
2. **拟建 / 改坐标前**（写盘前）：
   ```bash
   npx tsx scratch/audit_city_spacing.mjs --probe <lat> <lng> <据点名>
   ```
   必须 **exit 0**；含 ❌ 或 ⚠️ 卡线（50–55 km）→ **不写**，须问主人
3. **改 `cities_v2.ts` 之后**：`npm run city:spacing`（全图 ≥50 km 硬顶，exit 0）

## 禁止

- 为腾势力、挂将领/精锐而**发明新锚点**（优先改绑**已有**城）
- 在密集区**卡 50–52 km 凑数**建城
- 在空白海域/荒漠**距最近城 >500 km** 硬塞孤点
- 不跑 `--probe` 就写入新坐标
- 用 ±1–2 km 微调规避审计（除非 AGENTS §2.1 主人已裁定）

## 势力搬迁（只许已有城）

- 1 势力 = 1 据点；搬迁**只能**改绑**已有** `cityId`
- 找不到 [50, 500] km 窗口内的已有城 → **停手问主人**，不得新建

## 失败时

- 审计不通过 → **不写入**；报告过近城名、最近邻距离是否 >500 km
- 不得 git 回滚；不得读 VSCode History / `.bak`

## 报告格式（动 cities_v2 后必附）

```
--probe <lat> <lng> <名> → ✅ / ❌（最近 XX km）
npm run city:spacing → 违规 N 对 / ✅0
```
