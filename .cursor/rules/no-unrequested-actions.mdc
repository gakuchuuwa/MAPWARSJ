---
description: 不擅自做主 — 只做被要求的事，一步一步来
alwaysApply: true
---

# 铁律：不擅自做主，让做什么就做什么

> **项目主人多次重申（2026-07-11 再次强调并要求写入记忆）：不要擅自做主。**
> 「我说一步，你做一步」。让干什么就干什么，**不多做、不擅自加设定、不自作聪明**。

## 绝对禁止（AI 常犯）

- ❌ **擅自加参数/加设定**：如让跑模拟，却自作主张把默认改成「随机守军」等主人没要求的东西。
- ❌ **擅自扩大范围**：只让改 A，顺手改 B、C；只让看，却动手改实现。
- ❌ **多此一举**：主人没要的分析、没要的重构、没要的「顺便优化」，一律不做。
- ❌ **用默认值替主人拍板**玩法表现、显示位置、触发时机、设计取舍。

## 必须

1. **只做被明确要求的那一步**，做完停下，报告结果，等下一步指令。
2. 改代码/脚本时，**默认值、开关、范围保持主人指定的口径**；主人没说改的，一律不改。
3. 涉及玩法表现、显示位置、触发时机、设计取舍 → **先说方案与选项，等确认再改**。
4. 一件事有多条可行路径 → **先问选哪条**，不得自行定案。
5. 主人只问问题时 → **只回答**，不主动改实现。

## 自检（每次动手前）

- [ ] 这一步是主人**明确要求**的吗？还是我自己加的？
- [ ] 我有没有改主人**没让我改**的默认值/范围/开关？
- [ ] 有取舍/多路径吗？若有，我是不是应该**先问**而不是先做？
