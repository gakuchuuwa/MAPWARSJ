---
description: 据点本位录入 — 势力/武将/精锐均以据点为唯一入口（禁止以人物或势力反推）
globs: src/data/cities_v2.ts,src/data/factions.ts,src/data/FactionGenerals.ts,src/data/*ExpeditionLegions.ts,src/data/StartingCapitals.ts,src/data/GeneralSkills.ts,src/combat/SiegeGarrisonTier.ts,src/legion/LegionSpawnTier.ts
alwaysApply: false
---

# 据点本位：武将/精锐唯一出场规则

**真理源**：`AGENTS.md` §2.2.0（录入）、§12.2.1（运行时）。

---

## 最核心的一句话（重中之重，反复犯错）

> **以据点为基准，绝对不以势力为基准。**

武将和精锐锚定在**据点**上，和哪个势力占着这座城无关。
不管是蒙古、室町还是任何势力占着不破关，攻打不破关时守军都能出大谷吉继和大谷部众。

---

## 铁律 A：录入本位

录入顺序只能是：**据点 → 势力 → 武将 → 精锐**，不可倒推。

---

## 铁律 B：武将/精锐只能同时出现在一个地方

每座据点有且只有一个武将、一个精锐。它们在任意时刻只能出现在以下两个地方之一：

| 场景 | 说明 |
|------|------|
| **军团**（出征中的军队） | 据点出兵时随军带走，此时据点驻军没有将/精 |
| **驻军**（守城时） | 军团不存在或已被消灭，据点驻军出将/精 |

**这两个场景互斥，绝不同时出现。**

---

## 将/精的生命周期（`spawnGeneralUsed` / `spawnEliteUsed`）

```
据点出兵（makeArmyElite / attachFactionGeneralToArmy）
    ↓
spawnUsed = true  →  驻军不再出将/精

军团被消灭 or 城市易主
    ↓
spawnUsed 重置 = false  →  驻军恢复可出将/精
```

重置时机（两者均会重置）：
1. **城市易主**：`CityManager.updateCity` 在 `factionId` 变更时重置
2. **军团消灭**：`LegionManager.syncCitySpawnTierConsumption` 周期性扫描，重置所有城市后仅对存活军团重新标记

---

## 运行时检查顺序（`applySiegeGarrisonBoostIfNeeded`）

守城驻军能否出将/精，按以下顺序检查，任一为真则不出：

### 将领检查
```
① 守方军团已有该武将？（defendingLegions.some(l => l.generalId)）→ 不出
② 攻方军团已有同 generalId？（attackingLegions.some(l => l.generalId === anchoredGeneral.generalId)）→ 不出
③ city.spawnGeneralUsed === true？→ 不出
```

### 精锐检查（与将领完全对称）
```
① 守方军团已有精锐？（defendingLegions.some(l => l.isElite)）→ 不出
② 攻方军团已有同名精锐？（attackingLegions.some(l => l.isElite && l.name === eliteName)）→ 不出
   ※ army.name 由 makeArmyElite 写入精锐番号，按名字比对
③ city.spawnEliteUsed === true？→ 不出
```

**注意**：`spawnEliteUsed` 在城市易主时会被重置为 false。
如果军团带走精锐后城市被占，再被攻打时 `spawnEliteUsed` 已经是 false，
此时依靠②号检查（攻方已有同名精锐）来保证同场唯一性。

---

## 禁止事项（曾犯过的错）

| 禁止 | 原因 |
|------|------|
| ❌ 用 `city.factionId !== anchorFactionId` 来阻止守将出场 | 直接违反"以据点为基准"铁律 |
| ❌ 只检查 `defendingLegions`，不检查 `attackingLegions` 是否已有同名将/精 | 会导致同一战场两侧出现相同武将/精锐 |
| ❌ 精锐唯一性按势力判断而非按番号名 | 番号名存在 `army.name`，按名字更精准 |

---

## 战报 UI 显示规则（CombatUI）

| 单位类型 | 有无精锐 | 底部名称 | 技能徽章 |
|----------|---------|---------|---------|
| 军团（`unitType !== 'city'`）| 无精锐 | `城名` 军团 | — |
| 军团（`unitType !== 'city'`）| 有精锐（`isElite=true`）| 精锐番号（如`怯薛宿卫`）| `怯薛宿卫 ×1.2` |
| 驻军（`unitType === 'city'`）| 无精锐 | `城名` 驻军 | — |
| 驻军（`unitType === 'city'`）| 有精锐（`_siegeGarrisonEliteName`）| 精锐番号（如`大西老营`）| `大西老营 ×1.4` |

实现位置：`CombatUI.updateInfo → mapName()` / `buildWaveGroupedSideName` / `getLegionEliteBadgeName`

---

## 反例（真正的原因）

> 唐国攻长安（攻方带李世民），汉国守长安（守城城防又出李世民）。
>
> **真正的原因**：守城代码没有检查攻方是否已有该武将（缺少②号检查）。
> **不是**"没检查占城方是否等于锚点方"——那个检查是错误方向，会让据点永远无法出守将。

---

## 录入后必跑

```bash
npm run city:lookup -- <据点名>
```
