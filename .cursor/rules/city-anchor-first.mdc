---
description: 据点本位录入 — 势力/武将/精锐均以据点为唯一入口（禁止以人物或势力反推）
globs: src/data/cities_v2.ts,src/data/factions.ts,src/data/FactionGenerals.ts,src/data/*ExpeditionLegions.ts,src/data/StartingCapitals.ts,src/data/GeneralSkills.ts,src/combat/SiegeGarrisonTier.ts,src/legion/LegionSpawnTier.ts
alwaysApply: false
---

# 据点本位录入 + 武将/精锐唯一出场（2026-06-23 增补）

**真理源**：`AGENTS.md` §2.2.0（录入）、§12.2.1（运行时）。

## 设计四原则（主人定）

> **真实（尊重历史）、合理（能说明白）、简单（少 Bug）、随机（可玩性）**

## 铁律 A：录入本位（据点→势力→武将→精锐，不可倒推）

**一切以据点为基准。** 录入顺序：

1. **据点** — 坐标 + 1900 前旧称（此地有哪些称呼？取知名度大者）
2. **势力** — 按优先级定名；**势力名 ≠ 据点名**
3. **武将** — 在此战斗/驻守过、与据点相关
4. **精锐** — 在此战斗/驻守过、与武将相关；T0–T3 见 §12.3.1

## 铁律 B：武将/精锐 — 全局唯一出场（2026-06-23 主人定，禁止再犯）

> 一个据点，一个势力，一个武将，一个精锐。
> **据点出了武将，其他地方和其他情况就不能再出这个武将了。**
> **据点出了精锐，其他地方和其他情况就不能再出这个精锐了。**

| 规则 | 说明 |
|------|------|
| **将随城锚点，不随占城旗** | 武将锚定`录入时的 anchorFactionId`，**不跟着占城旗走** |
| **占城不过户** | 他势力占领据点后，该城的武将/精锐**不转让**给占城势力 |
| **守城不得重复** | 守城城防掷将时，若占城势力 ≠ 锚点势力 → **禁止出该武将** |
| **同一战场唯一** | 同一场攻防战，攻守双方的武将/精锐 ID **不得重复** |

### 运行时检查顺序（`SiegeGarrisonTier.applySiegeGarrisonBoostIfNeeded`）

将领和精锐的检查是**对称的**，必须同时执行：

#### 将领出场检查
```
① city.factionId === getCityAnchorFactionId(city.id)？
   → 否 → 不出武将（占城不过户）
② 守方军团已有该武将（defendingLegions.some(l => l.generalId)）？→ 不出（互斥）
③ 攻方军团已有同 generalId（attackingLegions.some(l => l.generalId === anchoredGeneral.generalId)）？→ 不出（同场唯一）
④ city.spawnGeneralUsed？→ 不出（消耗制）
```

#### 精锐出场检查（与将领对称，2026-06-25 补全）
```
① city.factionId === getCityAnchorFactionId(city.id)？
   → 否 → 不出精锐（占城不过户）— 与将领共享同一检查
② 守方军团已有精锐（defendingLegions.some(l => l.isElite)）？→ 不出（互斥）
③ 攻方军团已有同名精锐（attackingLegions.some(l => l.isElite && l.name === eliteName)）？→ 不出（同场唯一）
   ※ 精锐番号存在 army.name（由 makeArmyElite 写入），按名字比对比按势力更精准
④ city.spawnEliteUsed？→ 不出（消耗制）
```

> **关键实现细节**：`city.spawnEliteUsed` 在城市易主时会被 `CityManager` 重置为 `false`，
> 因此"占城不过户"（检查①）是阻止他势力驻军冒用精锐的**第一道防线**，不可省略。

### 禁止（代码层）

- ❌ 守城城防只检查 `defendingLegions`，不检查 `attackingLegions` 是否已有同 `generalId`
- ❌ 守城城防不检查 `city.factionId !== anchorFactionId` 就直接掷将/精
- ❌ 攻方军团与守城城防在同一场战斗里出现相同 `generalId` / 精锐番号
- ❌ 精锐唯一性检查按势力而非按番号名（一势力理论上只有一支精锐，但按名字更严谨）

## 势力优先级（无则向下）

民族 → 政权首都 → 州郡治所 → 家族 → 地名（战场/要塞/驿站等）

## 反例 vs 正例

| 据点 | 势力 | 武将 | 精锐 |
|------|------|------|------|
| 长子 | **上党** | 白起（上党战事） | 上党轻骑 |
| 天水 | **秦** | 嬴稷 | 秦之锐士 |

> **反例（禁止再犯）**：唐国攻长安，攻方出李世民；汉国守长安，守方城防又出李世民。  
> 原因：守城代码未检查占城方 ≠ 锚点方，导致同一武将同场重复出现。

## 录入后

`npm run city:lookup -- <据点名>`（**必跑**，一行看清文化区·势力·旗号·武将·精锐）  
`npm run add:check -- <据点名>` · `faction:sync-audit` · `expedition:triple-check` · `city:dossier`

## 查据点全档（禁止分文件拼凑）

回答或修改某据点前，**先**执行：

```bash
npm run city:lookup -- 长子
npm run city:lookup -- --one 天水    # 仅一行，适合粘贴进对话
```

或读 `scratch/city_dossier.json` 的 `byCityName` / `oneLine` 字段。  
**不要**分别 grep factions、FactionGenerals、ExpeditionLegions 后手工拼表。
