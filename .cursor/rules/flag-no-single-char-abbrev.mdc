---
description: 旗号须写完整史地专名，禁止单字截简称（与尾张/甲斐/百济同级）
alwaysApply: true
---

# 旗号完整专名 — 禁止单字简称

写入 `SandboxDisplayNames.ts` / `CityAssetManager.ts` 的**旗号**（玩家旗面 1–2 字）须为**完整史地专名**，不是行政区/国号的单字截略。

## 做什么

1. **只剥后缀**：仅去掉「国」「州」「族」「人」「氏」等**后缀**，保留专名本体几字写几字（1–2 字）。
2. **日本令制国**：与现有 **尾张、甲斐、伊贺、越后** 一致 → 国名去「国」后的**完整二字**作旗号。
3. **郡名 / 政权国号 / 民族专名**：同样写完整专名（如 **九江、百济、月氏、北条、今川**），不截成单字。
4. **势力全名**（`factions.ts` 的 `name`）可与旗号相同（如 `尾张`），须为完整称呼，**禁止单字**势力名。

## 禁止（单字简称 / 截字）

| 错误 ❌ | 正确 ✅ | 说明 |
|--------|--------|------|
| 远江 → `远` | **远江** | 令制国二字专名 |
| 常陆 → `陆` / `常` | **常陆** | 同上 |
| 安房 → `房` / `安` | **安房** | 同上 |
| 上总 → `总` | **上总** | 同上 |
| 三河 → `河` | **三河** | 同上 |
| 伊势 → `势` | **伊势** | 同上 |
| 百济 → `百` | **百济** | §4.4 正式国号 |
| 九江 → `九` / `江` | **九江** | §4.5 郡名 |

## 与中国「X州→单字」的关系

- **日本区令制国**：一律**完整二字**（去「国」），**不适用**单字截（无 `远`、`总` 等写法）。
- **中国区州名**：若 AGENTS §4.7 已有先例（如 **宿、庆**）且为史籍公认单字旗，可保留；**新增时优先写完整 2 字旧称**（如 **上洛、宣府**），避免再发明单字简称。
- **防重 / 禁字**：仍遵守 AGENTS §4.0（旗面禁「州」「国」）、旗号↔据点名防重；冲突时**改据点名，不改旗号**。

## 录入前自检

- [ ] 旗号是否为**完整专名**（非单字从二字名截出）？
- [ ] 日本令制国是否与 **尾张/甲斐** 同级写法？
- [ ] 全局检索 `SandboxDisplayNames` + `CityAssetManager` 无重复、无禁字？
