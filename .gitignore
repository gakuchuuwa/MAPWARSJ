# Dependencies
node_modules/
package-lock.json

# Build outputs
dist/
*.log

# IDE
.vscode/
.idea/

# AI tools
.claude/
.agents/
.cursorrules
.codex/
.cache/

# Misc
*.bak
*_backup.*

# 立绘调校滚动备份（本地保险，数据文件本身已随小时提交入库，备份不必进 git）
src/data/portrait_adjust_backups/

# OS
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.swp

# Large resource folders (not needed in version control)
# 本地 Google Terrain 瓦片金字塔（zoom 8–11，合计约 3.2GB / 11 万个文件）。
# 缺失时 GameMap 透明兜底、由 HillshadeLayer 补位，线上从来就没有这批瓦片。
# 【2026-08-01】10/11dixingtu 于 7-26 误入库，造出单个 2.4GB 提交，直接推爆 GitHub 上限
# 并撑爆 Vercel 上传，导致异地备份与线上部署同时停摆 6 天。四级一律不入库。
public/8dixingtu/
public/9dixingtu/
public/10dixingtu/
public/11dixingtu/
# 根目录 map/ 是同四套瓦片的重复副本（另 3.2GB），src 无任何引用、Vite 也不 serve，同样不入库。
# 必须写成 /map/ 锁死根目录——写 map/ 会连 src/map/ 一起匹配掉（2026-08-01 踩过）。
/map/
san10pk_99026/

.vercel

# Large data files and models
*.geojson
*.traineddata
close_cities_report.json
*.pdf
史料/

# Root garbage files
scratch/*
# 例外：factionTierClassify.ts 运行时依赖这 4 个 audit json，必须随仓库部署
!scratch/ethnic_homeland_audit.json
!scratch/regime_capital_audit.json
!scratch/family_state_audit.json
!scratch/county_seat_audit.json
# 例外：目标选择 / 反雪球的验证与跑分脚本（结论都靠它们复现，别再丢）
!scratch/verify_leader_hunt.mts
!scratch/verify_leader_tie.mts
!scratch/verify_pool_pathcalls.mts
!scratch/bench_picktarget_real.mts
!scratch/verify_recapture_return.mts
!scratch/verify_six_class_separation.mts
# ⚠️ 这条不带斜杠前缀，匹配**任意层级**的 scripts 目录（含 src/scripts、AnimationTools/scripts）。
# 别改成 scripts/*：那样只匹配根目录，另外两个会被放出来一并提交（2026-08-05 踩过）。
# 无头推演器 scripts/sim/headless.ts 是调参唯一可靠的标尺，已用 `git add -f` 纳入跟踪——
# 文件一旦被跟踪，忽略规则就不再生效，无需在此开白名单。
scripts/
/*.cjs
/*.mjs
/*.ps1
/*.py
/*.txt
/*.png
/*.jpg
temp*.ts
tmp*.ts
test*.ts
check*.ts
update*.ts
/*.js
/*.ts
/*.html
NOT
console.log*
m\[*
backup/
juji/

!/package.json
!/package-lock.json
!/vite.config.ts
!/index.html
start_server.bat
.auto_deploy_state.json
